# 구현 현황 — 신규 액티브 스킬 3/4: 조제 (2026-09-16)

## 실제 구현
- **신규 액티브 스킬 3/4(조제) — 열 번째 왕국 구역(무너진 약방)**: 위 패턴을 다시 재사용해 조사→정리(120초, 확정 회수품 나무 12/돌 10)→부분 자재 납품(나무 26/나무 판자 12)→복원(140초)→조제 영구 해금·복원도 10을 구현했다. 조제는 목공과 마찬가지로 **제작류 스킬**로 설계했다 — 전체 방향 문서의 나열 순서가 벌목↔목공·채광↔대장작업 같은 "채집 스킬 – 그 산출물을 소비하는 제작 스킬" 쌍을 이룬다는 점에 착안해, 야외 채집↔조제도 같은 쌍으로 판단했다. 채집의 4개 티어(산딸기/들버섯/산약초/영지버섯)를 소비해 4개의 물약류 가공품(산딸기 물약/들버섯 연고/산약초 영약/영지버섯 묘약)을 제작하며, 시간/경험치/판매가는 목공의 같은 티어 수치를 그대로 재사용했다(채집=벌목 수치 동일이라 안전). 직전 구역(무너진 제재소)이 연 목공의 실제 산출물(나무 판자)을 납품 자재로 써서 재료 의존 체인 원칙을 재적용했다. 엔진·UI 코드는 이번에도 한 줄도 바뀌지 않았다(10번째 재사용 확인). `save.ts`의 스킬/도구 복원 루프에 `if (version < 19 && id === 'apothecary') continue;`를 추가했다(v19 이전 저장엔 해당 키가 없음). D022~D028 체크리스트를 세 번째로 착수 시점부터 적용했음에도, 직전 라운드가 새로 추가한 boundary 테스트에 또 같은 유형의 암묵적 버전 의존 버그가 있어 착수 시점에 먼저 발견·수정 — 이 버그 유형이 "새로 추가한 바로 그 라운드에서는 드러나지 않는" 구조적 패턴임을 세 번째로 확인했다. `npm test` 139/139를 첫 실행에서 통과했다. 상세 근거는 DECISIONS.md D029 참고.
- **신규 액티브 스킬 2/4(목공) — 아홉 번째 왕국 구역(무너진 제재소)**: 위 패턴을 다시 재사용해 조사→정리(110초, 확정 회수품 나무 11/돌 9)→부분 자재 납품(나무 24/산딸기 14)→복원(130초)→목공 영구 해금·복원도 9를 구현했다. 목공은 채집과 달리 **대장작업과 같은 제작류 스킬**로 설계했다 — 벌목의 4개 티어(나무/참나무/단단한 나무/마법 나무)를 소비해 4개의 판자류 가공품(나무 판자/참나무 판자/단단한 들보/마법 골조)을 제작하며, 시간/경험치/판매가는 대장작업의 같은 티어 수치를 참고해 맞췄다. 직전 구역(무성해진 숲길)이 연 채집의 실제 산출물(산딸기)을 납품 자재로 써서 재료 의존 체인 원칙을 재적용했다. 엔진·UI 코드는 이번에도 한 줄도 바뀌지 않았다(9번째 재사용 확인, 제작류 스킬 경로 첫 실사용). `save.ts`의 스킬/도구 복원 루프에 `if (version < 18 && id === 'woodworking') continue;`를 추가했다(v18 이전 저장엔 해당 키가 없음). D022~D027 체크리스트(신규 프로젝트 키 삭제 + boundary 테스트 버전 명시 고정)를 착수 시점부터 함께 적용해 `npm test` 137/137을 첫 실행에서 통과했다. 상세 근거는 DECISIONS.md D028 참고.
- **신규 액티브 스킬 1/4(채집) — 여덟 번째 왕국 구역(무성해진 숲길)**: 계획된 왕국 구역 7개가 끝난 뒤 사용자가 지시한 "신규 액티브 스킬 4종(야외 채집/목공/조제/재봉)" 중 첫 번째로, 위 패턴을 다시 재사용해 조사→정리(100초, 확정 회수품 나무 10/돌 8)→부분 자재 납품(나무 22/스톤급 마법부여석 6)→복원(120초)→채집 영구 해금·복원도 8을 구현했다. 채집 자체는 벌목/채광/낚시와 동일한 레시피 없는 순수 채집 스킬이며(4단계: 산딸기 Lv1/들버섯 Lv10/산약초 Lv30/영지버섯 Lv50), 수치는 벌목의 나무/참나무/단단한 나무/마법 나무와 1:1로 동일하게 맞춰 기존 밸런스 곡선을 벗어나지 않게 했다. 직전 구역(쓰러진 마법탑)이 연 마법의 실제 산출물(스톤급 마법부여석)을 납품 자재로 써서 D024/D025의 재료 의존 체인 원칙을 재적용했다. 엔진·UI 코드는 이번에도 한 줄도 바뀌지 않았다(8번째 재사용 확인). `save.ts`의 스킬/도구 복원 루프에 `if (version < 17 && id === 'foraging') continue;`를 추가했다(v17 이전 저장엔 해당 키가 없음). D022~D025 체크리스트를 네 번째로 사전 적용했으나, boundary 테스트가 `raw.version`을 암묵적 현재 버전에 의존하던 문제와 결정론적 일일 퀘스트 시드 테스트가 신규 레벨1 원재료로 영향받는 문제를 새로 발견·수정했다. 상세 근거는 DECISIONS.md D027 참고.
- **반대 방향 저장 검증**: `save.ts`의 프로젝트별(정방향) 검증 뒤에, 버전과 무관하게 `ProjectDB` 전체를 대상으로 완료된 프로젝트가 주는 스킬·기능을 모아 "실제로 해금 가능한 전체 집합"을 계산하고, `unlockedSkills`/`unlockedFeatures`의 모든 항목이 시작 스킬/기능이거나 이 집합에 포함되는지 검사한다. 기존 정방향 검사는 그 저장 버전에 이미 도입된 프로젝트에 대해서만 돌아서, 아직 도입되지 않은 구역(예: v14 저장의 `fallen_tower`, `introducedVersion: 16`)의 스킬을 `unlockedSkills`에 직접 끼워 넣어도 걸러지지 않는 빈틈이 있었다 — 체크섬은 재계산 가능한 평문 해시라 이 지점까지는 막지 못한다(D012/D013). 새 저장 스키마 필드는 없어 `Model.version`은 그대로다. `unlockedGame()`(정의된 모든 프로젝트를 완료 처리해서 만들어지는 테스트 전용 완전 해금 기준)과의 충돌 여부를 실제로 `npm test`로 확인했고, 왕국 구역 로드맵이 D025에서 완결돼 모든 스킬·기능에 실제 부여 프로젝트가 있는 지금은 충돌 없이 통과함을 확인했다(D020 당시엔 구역이 2개뿐이라 충돌했었음). 상세 근거는 DECISIONS.md D026 참고.
- **왕국 복원 첫 수직 슬라이스(대장간)**: 기본 화면을 왕국으로 전환하고, 폐허가 된 대장간의 조사→30초 정리→확정 회수품(나무 3/돌 4)→부분 자재 납품(나무 12/돌 10)→45초 복원→대장작업·도구 제작 해금을 구현했다. 폐허 정리와 복원은 스킬 레벨 없이 생산과 같은 전역 액티브 슬롯을 쓰며, 다른 작업으로 전환해도 프로젝트 진행도는 보존된다. 왕국 화면은 폐허 회수/복원/완료 구역으로 상태를 나누고 새 게임 사이드바에는 벌목·채광과 공용 화면만 표시한다.
- **왕국 복원 두 번째 구역(부서진 다리)**: 대장간과 동일한 조사→정리(40초, 확정 회수품 나무 4/돌 2)→부분 자재 납품(나무 10/벽돌 6)→복원(60초)→낚시 영구 해금·복원도 2 패턴을 재사용해 구현했다. 납품 자재에 벽돌(대장작업 산출물)을 포함시켜 대장간을 먼저 끝내야 실제로 완료할 수 있게 자연히 유도한다. `ProjectDef.introducedVersion`으로 저장 검증이 "이 저장 버전에 등장해야 할 구역 집합"을 동적으로 계산한다. `KingdomView.tsx`의 완료/복원 힌트 문구를 프로젝트별 하드코딩에서 `unlockSkills`/`unlockFeatures` 기반으로 일반화했다. 상세 근거는 DECISIONS.md D020 참고.
- **왕국 복원 세 번째 구역(무너진 식당)**: 위 패턴을 그대로 재사용해 조사→정리(50초, 확정 회수품 나무 5/돌 3)→부분 자재 납품(나무 12/피라미 8)→복원(70초)→요리 영구 해금·복원도 3을 구현했다. 피라미(낚시 산출물)를 납품 자재에 넣어 다리를 먼저 끝내야 유도되게 한 것도 다리가 벽돌로 대장간을 유도한 것과 동일한 설계. **엔진(`model.ts`)·UI(`KingdomView.tsx`) 코드는 한 줄도 바뀌지 않았고**, 콘텐츠 항목 추가 + 저장 버전 배열 확장(v11→v12)만으로 끝났다 — `introducedVersion` 기반 검증과 문구 일반화가 실제로 반복 재사용됨을 확인. 상세 근거는 DECISIONS.md D021 참고.
- **왕국 복원 네 번째 구역(먼지 쌓인 길드 회관)**: 위 패턴을 다시 재사용해 조사→정리(60초, 확정 회수품 나무 6/돌 4)→부분 자재 납품(나무 14/구운 생선 5)→복원(80초)→`guild` 기능 영구 해금·복원도 4를 구현했다. 이번엔 **새 스킬이 아니라 이미 구현·병합돼 있었지만 어떤 프로젝트도 부여하지 않아 신규 저장에서 도달 불가능했던 기존 기능(`guild`)을 처음으로 연결**한 사례다(`unlockSkills: []`, `unlockFeatures: ['guild']`). 구운 생선(요리 산출물)을 납품 자재에 넣어 식당을 먼저 끝내야 유도되게 한 것도 동일한 설계. 엔진·UI 코드는 이번에도 한 줄도 바뀌지 않았다. 상세 근거는 DECISIONS.md D022 참고.
- **왕국 복원 다섯 번째 구역(버려진 밭)**: 위 패턴을 다시 재사용해 조사→정리(70초, 확정 회수품 나무 7/돌 5)→부분 자재 납품(나무 16/돌 12)→복원(90초)→농사 영구 해금·복원도 5를 구현했다. 이번엔 **재료 의존 체인이 처음으로 끊겼다** — 직전 구역(길드 회관)은 서비스형 기능(`guild`)만 열어 재사용할 채집·가공 산출물이 없어서, 첫 구역(대장간)과 같은 나무·돌 자재로 되돌아갔다. 엔진·UI 코드는 이번에도 한 줄도 바뀌지 않았고, D022가 남긴 "모든 낮은-버전 마이그레이션 테스트에서 새 프로젝트 키 삭제 확인" 체크리스트를 구현 착수 시점에 미리 적용해 처음부터 깨뜨리지 않고 통과시켰다. 상세 근거는 DECISIONS.md D023 참고.
- **왕국 복원 여섯 번째 구역(낡은 축사)**: 위 패턴을 다시 재사용해 조사→정리(80초, 확정 회수품 나무 8/돌 6)→부분 자재 납품(나무 18/밀 10)→복원(100초)→목장 영구 해금·복원도 6을 구현했다. 밀(농사 산출물이자 기존 목장 시스템의 닭·양 사료)을 납품 자재에 넣어 D023에서 끊겼던 재료 의존 체인을 다시 이었다 — 재료 선택 자체가 기존 콘텐츠(사료 아이템)와 실제로 맞물린다. 엔진·UI 코드는 이번에도 한 줄도 바뀌지 않았고, D022/D023 체크리스트를 구현 착수 시점에 다시 적용해 두 번 연속으로 결함 없이 첫 실행에서 테스트를 통과시켰다. 상세 근거는 DECISIONS.md D024 참고.
- **왕국 복원 일곱 번째 구역(쓰러진 마법탑, 계획된 마지막 왕국 구역)**: 위 패턴을 다시 재사용해 조사→정리(90초, 확정 회수품 나무 9/돌 7)→부분 자재 납품(나무 20/양털 12)→복원(110초)→마법 영구 해금 + 장신구 화면(`equipment`) 영구 해금·복원도 7을 구현했다. 길드 회관 이후 두 번째로 스킬과 기능을 동시에 여는 구역이다. 양털(목장 산출물)을 납품 자재에 넣어 D024가 다시 이은 체인을 한 번 더 이었다 — 마법 자체의 산출물(마법부여석)은 이 구역이 열리기 전까지 존재할 수 없어 자기 자신을 참조할 수 없으므로, 기존 원칙대로 직전 구역(목장)의 실제 산출물을 썼다. 엔진·UI 코드는 이번에도 한 줄도 바뀌지 않았고, D022~D024 체크리스트를 세 번째로 사전 적용해 세 번 연속으로 결함 없이 첫 실행에서 131/131을 통과시켰다. 일곱 구역 모두 처음부터 함께 조사 가능 상태로 노출된다(구역 간 하드 선행조건 없음, D020~D025). **`docs/implementation_plan.md` §4에 계획된 왕국 구역 로드맵이 이 구역으로 완결됐다** — 완료 시점에 `unlockedGame()`이 미리 열어주는 모든 스킬·기능이 전부 실제 프로젝트로 설명 가능해져, D020/D021이 보류해 온 반대 방향 저장 검증을 이제 안전하게 추가할 수 있다(범위 밖, 별도 작업으로 분리). 상세 근거는 DECISIONS.md D025 참고.
- **성장 곡선 CLI**: `npm run simulate`가 실제 `ResourceDB`, 도구 티어, 엔진과 공유하는 레벨 경험치·속도 공식을 사용해 8개 스킬의 Lv10·30·50·99 도달 시간과 모든 자원의 시간당 생산·경험치·총 판매가를 Markdown 표로 출력한다. 무도구/해금 도구/도구+속도 음식 10%/도구+음식+경험치 18%의 네 시나리오를 비교한다. 실제 레시피와 도구 비용을 재귀적으로 펼쳐 필요한 원재료·가공 횟수·스킬별 작업 시간을 계산하는 초반 생산 경로도 함께 출력한다. 레벨 보고서는 가공 입력과 도구 제작 비용을 무한 공급으로 가정한 스킬 자체 기준선이며 실제 완주 시간은 아니다. `docs/progression_analysis.md`에 첫 결과와 조정 순서를 기록했다.
- 벌목, 채광, 벽돌·구리/철/금 주괴 제작, 재료 보관 및 1개/10개 판매.
- 낚시터 3곳: 마을 개울(피라미 Lv1), 갈대 호수(붕어 Lv10), 상류 여울(연어 Lv30).
- 요리 8종: 생선 3종(구운 생선 낚시+10%/10분, 민물 생선탕 벌목·채광·낚시+5%/15분, 훈제 연어 대장작업+15%/15분) + 농사·목장 재료 5종(야채죽/나무꾼 도시락/광부의 스튜/대장장이 정식/축제 요리).
- 스킬별 돌/구리/철 도구 제작 및 즉시 적용. 낚싯대·조리도구·괭이·사료통·마법봉 포함.
- 음식은 한 종류만 적용. 같은 음식 1개/5개 사용 시 지속시간 연장. 다른 음식은 UI에서 남은 효과 폐기 안내 후 교체. 자동 섭취는 없음.
- 전체 화면에서 현재 작업, 진행률, 음식 효과, 밭·목장 상태와 남은 시간 표시.
- 순수 데이터 엔진으로 경과 시간 일괄 정산. 음식 만료 전후를 구분해 계산하며 재료 부족 시 제작 중단.
- **농사(패시브) 최소 루프**: 작물 3종(밀 Lv1, 감자 Lv10, 당근 Lv25)을 골드로 구매 → 밭 1칸에 파종(씨앗 1개 소모) → 액티브 작업·화면 이동·접속 종료와 무관하게 경과 시간만큼 성장 → 다 자라면 수확 버튼으로 같은 작물 3개 회수 + 농사 경험치. 다 자란 뒤에는 수확 전까지 더 진행되지 않는다. 신규 저장에서는 버려진 밭 복원을 마쳐야 농사가 해금된다(왕국 복원 다섯 번째 구역).
- **목장(패시브) 최소 루프**: 동물 3종(닭 Lv1/500G, 양 Lv15/2,000G, 소 Lv30/5,000G)을 골드로 구매하면 즉시 사육 시작(영구). 사료(닭·양=밀, 소=당근)가 있으면 접속 여부와 무관하게 주기마다(30/45/60분) 자동 산출·사료 소비. 사료 부족 시 주기 1회분에서 대기, 채우면 즉시 재개. 신규 저장에서는 낡은 축사 복원을 마쳐야 목장이 해금된다(왕국 복원 여섯 번째 구역).
- 농사·목장 → 요리 연결: 감자·당근·달걀·우유를 재료로 쓰는 요리 5종. "전 스킬" 효과는 패시브 스킬(농사/목장)에도 그대로 적용됨.
- **길드 — 원재료 구매/환전**:
  - 원재료 구매: 가공품(recipe 있는 아이템)이 아닌 모든 아이템(나무/돌/광석/생선/작물/축산물)을 스킬 레벨로 이미 해금했다면 골드로 즉시 구매 가능(`buy` 가격, 판매가의 5배). 기존 농사 "씨앗 구매"도 동일 함수 사용으로 통합(`buyCrop`→`buyResource` 일반화, 스킬 레벨 검증을 엔진 단에도 추가 — 이전엔 UI에서만 막고 있었음).
  - 원재료 환전: 상위 티어 원재료(참나무/단단한나무/마법나무/철광석/붕어/연어)를 같은 스킬의 최하위(Lv1) 재료로 환전. 티어 차이 1단계당 1.5배씩 복리(내림) 적용 — 예: 단단한나무(2단계 차이) 4개 → 나무 9개.
  - 길드 등급: 3단계(초급→중급 3,000G→상급 15,000G, 골드만 소모). 등급이 오를수록 환전 가능한 티어 차이가 늘어남(1→2→3단계). 길드 레벨업은 후원(골드 지불)으로 단순화했고 일일 퀘스트는 아래 규칙으로 구현했다.
  - 순환 거래 방지: 환전은 항상 스킬 최하위 재료로만 향하고(역방향 경로 없음), 모든 환전 항목이 "환전 후 되팔기"가 "직접 팔기"보다 항상 손해가 되도록 설계·테스트됨.
  - 상세 설계 근거는 DECISIONS.md D010 참고.
  - **일일 퀘스트**: 매일(UTC 날짜 기준) 이미 해금한 원재료 중에서 중복 없이 3개를 뽑아 "N개 납품" 퀘스트로 갱신한다(완료 여부와 무관하게 다음 날 교체, 이월/연체 없음). 완료 시 인벤토리에서 소모하고, 판매가의 2배에 판매가 장신구 보너스를 반영한 금액을 골드로 보상한다(`sell()`과 같은 규칙, `dailyQuestReward()`로 엔진·화면이 공유). 완료 확인은 인덱스뿐 아니라 화면에 표시된 재료 id도 함께 맞춰봐서, 날짜 경계에 클릭이 겹쳐 퀘스트가 통째로 갱신돼도 다른 퀘스트를 잘못 완료하지 않는다. 원안(game_design.md §3)의 "골드 + 길드 경험치" 보상은 별도 화폐를 새로 만들지 않고 골드로만 단순화(D010의 길드 레벨업 단순화와 같은 방향). 퀘스트 내용은 `Math.random()`이 아니라 날짜 id를 시드로 한 결정론적 의사난수로 생성되어, 같은 날·같은 해금 상태면 항상 같은 퀘스트가 나온다(이 프로젝트의 다른 시간 정산과 동일한 결정론 원칙 — 시간이 거슬러 온 호출에서도 생산 정산 시계와 같은 날짜 기준을 쓴다). 후보를 뽑을 때마다 풀에서 제거해 난수값이 반복돼도 생성이 유한하게 끝난다. 이전 저장(v1~v7) 마이그레이션은 스킬을 복원한 뒤에 실제 레벨로 퀘스트를 다시 뽑으며, v8 저장은 정확히 3개·수량 5~15개·현재 레벨에서 해금된 원재료만 허용한다. 상세 근거와 독립 리뷰에서 발견·수정한 내용은 DECISIONS.md D015 참고.
  - **마일스톤 퀘스트**: 현재 시스템으로 판별 가능한 온보딩 목표 9개를 기획 순서대로 길드 화면에 표시한다. 레벨·도구·스킬 경험치·목장 보유 상태에서 달성을 계산하고 수령한 id만 저장하며, 첫 환전만 성공 시 별도 영구 플래그를 남긴다. 미달성/수령 가능/수령 완료를 구분하고, 달성한 목표의 고정 골드는 직접 한 번만 수령할 수 있다. 도감(정착민) 첫 등록 목표는 시스템이 미승인·미구현이라 제외했다. 상세 근거는 DECISIONS.md D016 참고.
- **마법부여·장신구**:
  - 공급 루프: 농사 약초 4종(캐모마일/쑥/마법쑥/신비 허브), 채광 마나석·금 광석, 대장작업 금 주괴를 재료로 마법 스킬에서 스톤/구리/철/금급 마법부여석 제작. 신규 저장에서는 쓰러진 마법탑 복원을 마쳐야 마법과 장신구 화면이 함께 해금된다(왕국 복원 일곱 번째 구역). 스톤급 마법부여석은 여덟 번째 구역(무성해진 숲길)의 납품 자재로도 쓰인다.
  - 공용 장신구 3슬롯(개척자의 왕관/장인의 목걸이/풍요의 반지). 슬롯마다 하나만 스톤 재질로 제작하며 구리→철→금으로 승급. 승급은 기존 옵션과 희귀도를 그대로 유지한다.
  - 리롤: 현재 장신구 재질 이상 등급의 마법부여석 1개를 사용해 옵션 1개(전 스킬 속도/경험치/판매가)와 희귀도(하급~전설)를 교체. 재질 상한 밖 희귀도는 확률 풀에서 제외하고 나머지를 재정규화한다.
  - 장신구 효과는 시간 정산·경험치·판매 엔진에 적용되며 세 슬롯 효과는 합산한다. 확률은 사용자 리롤 순간에만 사용하므로 온라인/오프라인 생산 정산은 결정론을 유지한다.
  - 비용/옵션 수치와 직접 채광 마나석은 임시 설계. 상세 근거는 DECISIONS.md D011 참고.
- **세이브 백업/복원**: 설정 화면에서 현재 저장 전체를 JSON 파일로 내려받거나 클립보드에 복사할 수 있고(다운로드 시 브라우저가 저장 위치를 물어보는 대화상자는 브라우저 자체 동작이라 앱에서 제어할 수 없어 클립보드 복사를 대안으로 제공), 파일을 선택해 저장을 되돌릴 수 있다. 복원 전 확인을 거치며, `decodeSave`로 먼저 검증에 성공한 파일만 적용된다(버전·형식이 잘못된 파일은 기존 저장을 건드리지 않고 오류 메시지만 표시). 저장 손상으로 자동 저장이 중단된 상태(`blocked`)도 유효한 백업을 복원하면 자동 저장이 다시 시작된다. 복원은 `advance()`로 백업 시점 이후 경과 시간을 정산해 밀린 생산을 안내에 반영한다. 저장에는 체크섬(FNV-1a, 키 정렬 후 해시)이 함께 저장되어 값을 하나라도 바꾼 뒤 그대로 복원을 시도하면 거부된다 — 단, 암호학적 보안이 아니라 텍스트 편집기로 값을 슬쩍 바꾸는 것을 막는 수준의 마찰이며, 체크섬이 없던 v7 이전 저장은 그대로 허용된다(하위 호환). 체크섬 추가 자체는 저장 버전을 올리지 않았고, 현재 저장 스키마는 왕국 프로젝트(대장간·다리·식당·길드 회관·밭·축사·마법탑·숲길) 상태 추가로 v17이다. 설계 근거는 DECISIONS.md D012·D013 참고.
- **다중 탭 저장 충돌 방지**: `src/state/gameState.ts`에 `storage` 이벤트 리스너를 추가했다. 다른 탭이 저장(localStorage의 `melvorlike_save` 키)을 갱신하면 — 이 이벤트는 값을 쓴 탭 자신에게는 발생하지 않고 다른 탭에서만 발생한다 — 받는 탭이 `document.visibilityState`로 자신이 보이는 탭인지 확인한다. 보이지 않는(방치된) 탭만 `decodeSave`로 검증 후 `advance()`로 경과 시간을 정산해 그 내용을 자신의 상태로 즉시 반영하며, 저장 손상으로 자동 저장이 막힌(blocked) 탭이었다면 함께 복구된다. 보이는(사용 중인) 탭은 그 값을 받아들이지 않고 대신 자신의 현재 상태를 즉시 다시 저장해 되찾는다(blocked 상태면 저장이 그대로 무시되어 손상 보호도 유지됨). 이 구분이 없던 1차 구현은 독립 리뷰에서 반대 방향 문제(방치된 탭의 오래된 자동 저장이 사용 중인 탭의 최신 조작을 덮어쓸 수 있음, 특히 탭을 전환하는 순간의 `visibilitychange` 플러시와 겹쳐 실제로 자주 발생 가능)가 발견되어 이 방향 구분을 추가했다. 완전한 동시 편집 병합(진짜 멀티탭 지원)은 아니며 "어느 방향이든 조용한 덮어쓰기 방지"가 목적이다. 설계 근거와 검토한 대안(리더 선출 방식, `lastSaveTime`/`hasFocus()` 비교 방식)은 DECISIONS.md D014 참고.
- 두 패시브 스킬(농사/목장)은 액티브 단일 작업 슬롯과 별개로 항상 병행 진행되며, 스킬 레벨/도구 체계를 그대로 재사용한다.
- 버전 19 저장: 인벤토리, 도구, 생산/프로젝트 현재 작업과 부분 진행, 스킬·시설 해금, 프로젝트(대장간·다리·식당·길드 회관·밭·축사·마법탑·숲길·제재소·약방) 단계·납품량, 음식 효과, 밭 상태, 목장 사육 상태, 길드 등급, 마법 스킬/도구, 장신구 3슬롯, 일일 퀘스트와 마일스톤 수령/환전 상태 포함.
- v10(대장간만) → v11(+다리) → v12(+식당) → v13(+길드 회관) → v14(+밭) → v15(+축사) → v16(+마법탑) → v17(+숲길) → v18(+제재소) → v19(+약방) 순차 이전: 각 단계에서 이미 진행한 구역 상태·해금은 그대로 보존되고, 새로 추가되는 구역만 미시작(조사 가능)으로 나타난다 — 회수품 재지급 없음.
- v1~v9(왕국 시스템 자체가 없던) 저장 이전 지원: 기존 데이터와 진행 중 생산을 유지하고 모든 기존 스킬·시설 및 대장간·다리·식당·길드 회관·밭·축사·마법탑·숲길·제재소·약방 완료 상태를 부여한다. 이전 과정에서 회수품은 지급하지 않는다.
- 손상된 저장은 자동 저장을 중단하여 원본 보호.

## 임시 밸런스 및 기획과 차이
- 구리는 채광/제련 Lv1, 철은 Lv10 해금. 주괴 연료는 현재 목재. 석탄 보너스/광맥은 미구현.
- 다음 레벨 경험치: 100 × 1.12^(레벨-1), 최대 레벨 99.
- 도구는 돌 +15%, 구리 +35%, 철 +65% 작업 속도. 농사·목장 도구도 같은 표를 재사용.
- 음식과 도구의 속도 보너스는 합산. 속도가 변해도 작업/밭/목장 진행률은 보존.
- 오프라인 시간 상한은 현재 없음.
- 낚시는 우선 확정 획득. 꽝/미끼 시스템 없음.
- 농사 씨앗은 "구매 = 파종 재료" 하나의 아이템. 밭은 1칸만 존재. 목장은 동물종당 1마리만.
- 길드 환전은 항상 "그 스킬의 Lv1 재료"로만 향함(예: 철광석→돌, 구리광석 아님). 임의의 두 티어 사이 환전(예: 단단한나무→참나무)은 미구현 — content_spec.md의 N×N 환전표 대신 단순화된 "최하위로 수렴" 규칙.
- 길드 레벨업은 퀘스트가 아닌 골드 후원으로 단순화(기획서는 퀘스트+자재 소모). 일일·마일스톤 퀘스트 보상은 골드 전용(길드 경험치 별도 화폐 없음). 마일스톤 엔진·저장·길드 화면 연결까지 구현됐다.
- 새 게임은 벌목·채광만 표시하며 대장간 복원으로 대장작업과 도구 제작이, 다리 복원으로 낚시가, 식당 복원으로 요리가, 길드 회관 복원으로 길드(환전·일일 퀘스트·마일스톤)가, 밭 복원으로 농사가, 축사 복원으로 목장이, 마법탑 복원으로 마법과 장신구가, 숲길 복원으로 채집이, 제재소 복원으로 목공이, 약방 복원으로 조제가 열린다. 계획된 왕국 구역 7개가 모두 구현됐고, 이후 신규 액티브 스킬 4종 중 채집·목공·조제가 여덟·아홉·열 번째 구역으로 추가됐다. 이전 저장은 기존 접근 권한을 모두 유지한다.
- 마법부여석 재료인 마나석은 광맥 발견 이벤트가 아직 없어 채광 Lv30 직접 자원으로 임시 제공. 장신구 옵션은 슬롯당 1개이며 세 슬롯이 같은 옵션 풀을 공유한다.
- 장신구 제작/승급 비용과 옵션 수치는 플레이테스트 전 임시값. 상한을 넘는 희귀도 확률을 버리지 않고 남은 가중치로 재정규화한다.

## 미구현
계획된 왕국 복원 구역 7개는 모두 구현됐다(대장간·다리·식당·길드 회관·밭·축사·마법탑). 신규 액티브 스킬 4종(야외 채집/목공/조제/재봉) 중 채집·목공·조제가 각각 여덟·아홉·열 번째 구역(무성해진 숲길·무너진 제재소·무너진 약방)으로 구현됐다 — 재봉 1개는 아직 미착수. 그 외 남은 항목: 장신구 다중 옵션/고유 옵션 풀, 광맥/석탄, 업적·수집 목표, 레벨 해금·보유 재화·패시브 일정을 합친 전체 진행 경로 시뮬레이션, 계정 기반 클라우드 저장 동기화, 농사 비료·다중 플롯·단일 슬롯 편의 기능, 목장 마릿수 상한·비료류 보조 아이템, 경험치 배율형 음식 효과(달걀찜). 확률 정착민과 병렬 자동 채집 장치는 기획에서 제외됐다(D018). 왕국 구역 간 하드 선행조건(잠금) 시스템도 아직 없다 — 지금은 재료 의존성으로만 순서를 유도했고, 서비스형 기능 뒤에서 한 번 끊겼던 체인이 실제 산출물이 있는 구역들에서 다시 이어져 로드맵 끝까지 이어졌다(D020~D025, D027~D029). 해금 목록의 반대 방향 저장 검증(모든 unlockedSkills/Features가 시작 스킬이거나 완료된 프로젝트로 설명 가능해야 함)은 구현 완료(D026).
기획서의 확정 표현과 별개로, 제안만 된 기능과 수치는 추가 검토 대상.

## 검증
- 신규 액티브 스킬 3/4(조제, 열 번째 왕국 구역 무너진 약방, 2026-09-16): `npm test` 139/139 PASS(기존 137개 + 약방 관련 신규 테스트: 도입 전 저장 이전, 저장 버전별 구역 집합 검증(v18/v19 boundary 포함), 완료·조제 영구 해금·복원도 10 — D022~D028 체크리스트를 세 번째로 착수 시점부터 적용했으나, 직전 라운드가 새로 추가한 boundary 테스트에 또 같은 유형의 암묵적 버전 의존 버그가 있어 착수 시점에 먼저 발견·수정했다 — DECISIONS.md D029 참고), `npm run build` PASS, `npm run simulate` PASS(`npx tsx`, 조제 4종이 목공과 동일한 시간당 효율로 출력됨을 확인). 브라우저 QA: 아홉 구역을 `mutate()`로 완료 처리한 뒤 약방을 실제 엔진 함수로 조사→정리→납품→복원까지 진행 — "무너진 약방 복원 완료 · 조제 해금" 알림과 복원도 10 갱신, 사이드바 "조제" 항목 신규 등장, 4개 레시피가 재료 비용과 함께 올바르게 렌더링됨을 확인. "제작 시작" 클릭 후 엔진 시계를 진행시켜 산딸기 물약 제작·재료 소비·경험치 획득이 실제 상태와 DOM에 일치함을 확인. 콘솔 경고·오류 없음.
- 신규 액티브 스킬 2/4(목공, 아홉 번째 왕국 구역 무너진 제재소, 2026-09-16): `npm test` 137/137 PASS(기존 135개 + 제재소 관련 신규 테스트: 도입 전 저장 이전, 저장 버전별 구역 집합 검증(v17/v18 boundary 포함), 완료·목공 영구 해금·복원도 9 — D022~D027 체크리스트를 착수 시점부터 적용해 첫 실행에서 통과했다. 직전 라운드가 남긴 "v17 저장은..." boundary 테스트의 암묵적 버전 의존 문제를 이번 라운드 시작 시점에 먼저 발견·수정했고, 목공의 신규 자원이 전부 레시피가 있어 `guild-quest.test.ts`의 결정론적 시드 테스트는 영향받지 않았다 — DECISIONS.md D028 참고), `npm run build` PASS, `npm run simulate` PASS(`npx tsx`, 목공 4종이 대장작업과 유사한 시간당 효율로 출력됨을 확인). 브라우저 QA: 여덟 구역을 `mutate()`로 완료 처리한 뒤 제재소를 실제 엔진 함수로 조사→정리→납품→복원까지 진행 — "무너진 제재소 복원 완료 · 목공 해금" 알림과 복원도 9 갱신, 사이드바 "목공" 항목 신규 등장, 4개 레시피가 재료 비용과 함께 "제작 시작" 버튼으로 올바르게 렌더링됨을 확인. "제작 시작" 클릭 후 엔진 시계를 진행시켜 나무 판자 제작·재료 소비·경험치 획득이 실제 상태와 DOM에 일치함을 확인. 콘솔 경고·오류 없음.
- 신규 액티브 스킬 1/4(채집, 여덟 번째 왕국 구역 무성해진 숲길, 2026-09-16): `npm test` 135/135 PASS(기존 133개 + 숲길 관련 신규/수정 테스트: 도입 전 저장 이전, 저장 버전별 구역 집합 검증(v16/v17 boundary 포함), 완료·채집 영구 해금·복원도 8 — D022~D025 체크리스트를 네 번째로 사전 적용했으나 두 가지를 추가로 발견·수정했다: "v16 저장은..." 테스트가 `raw.version`을 암묵적 현재 버전에 의존하던 문제(`raw.version = 16` 명시 고정으로 수정)와, `guild-quest.test.ts`의 결정론적 일일 퀘스트 시드 테스트가 신규 레벨1 원재료(산딸기)로 후보 풀이 넓어지며 실제 추첨 결과가 바뀌어 깨진 문제(특정 재료에 의존하지 않는 `.not.toEqual` 비교로 교체) — DECISIONS.md D027 참고), `npm run build` PASS, `npm run simulate` PASS(`npx tsx`, 채집 4종이 벌목과 동일한 시간당 효율로 출력됨을 확인). 브라우저 QA: `localStorage` 직접 주입이 `gameLoop.ts`의 `pagehide` 자동 저장 플러시에 덮어써지는 것을 발견해, 대신 살아있는 앱의 `state`를 `mutate()`로 직접 변형하는 방식으로 일곱 구역을 완료 처리한 뒤 숲길을 실제 엔진 함수로 조사→정리→납품→복원까지 진행 — "무성해진 숲길 복원 완료 · 채집 해금" 알림과 복원도 8 갱신, 사이드바 "채집" 항목 신규 등장, 4단계 원재료·레벨 게이팅이 올바르게 렌더링됨을 확인. 콘솔 경고·오류 없음.
- 반대 방향 저장 검증(2026-09-16): `npm test` 133/133 PASS(기존 131개 + 신규 2개: 아직 도입되지 않은 구역의 스킬·기능을 `unlockedSkills`/`unlockedFeatures`에 직접 끼워 넣으면 거부되는지, `unlockedGame()` 왕복이 계속 무손실로 통과하는지). `npm run build` PASS, `npm run simulate` PASS(`npx tsx`). 회귀 없음을 실제 테스트 실행으로 확인(이론상 안전 근거는 DECISIONS.md D026 참고). 브라우저 QA: 새 저장으로 대장간 조사 → 새로고침(실제 저장/복원 왕복 경로) → 진행 상태 보존, 콘솔 경고·오류 없음 확인 — 일반 플레이 흐름에서 새 검증이 거짓 양성으로 막지 않음을 직접 확인.
- 왕국 복원 일곱 번째 구역(쓰러진 마법탑, 계획된 마지막 왕국 구역, 2026-09-16): `npm test` 131/131 PASS(기존 128개 + 마법탑 3개: 도입 전 저장 이전, 저장 버전별 구역 집합 검증, 완료·마법 영구 해금+장신구 화면 영구 해금·복원도 7 — D022~D024 체크리스트를 구현 착수 시점에 미리 세 번째로 적용해 기존 v10/v11(무너진 식당 도입 전)/v11(대장간+다리 시절)/v12/v13/v14/v15(낡은 축사 도입 전) 저-버전 마이그레이션·검증 테스트 7곳 전부에 `delete raw.projects.fallen_tower`를 선제적으로 추가한 뒤 실행, 131/131을 첫 실행에서 통과시킴 — 세 번 연속 무결함 적용, DECISIONS.md D025 참고), `npm run build` PASS, `npm run simulate` PASS(`npx tsx`로 재실행 — 이전 구역부터 있던 이 워크트리 계열의 PATH 해석 문제, 콘텐츠와 무관). 브라우저에서 빈 저장의 왕국 화면에 일곱 구역(대장간·다리·식당·길드 회관·밭·축사·마법탑)이 동시에 조사 가능 상태로 표시됨을 확인 → 마법탑 조사 클릭 시 "정리 시작" 상태와 정확한 회수품 문구("나무 9개 · 돌 7개")로 전환됨을 확인. 콘솔 경고·오류 없음. (마법탑의 조사→정리→납품→복원 전체 사이클은 대장간과 동일한 `advanceProjectAction` 코드 경로라 라이브로는 조사 전환까지만 확인했고, 완료·해금(스킬+기능 동시)은 단위 테스트로 정밀 검증)
- 왕국 복원 여섯 번째 구역(낡은 축사, 2026-09-16): `npm test` 128/128 PASS(기존 125개 + 축사 3개: 도입 전 저장 이전, 저장 버전별 구역 집합 검증, 완료·목장 영구 해금·복원도 6 — D022/D023 체크리스트를 구현 착수 시점에 미리 적용해 기존 v10/v11(무너진 식당 도입 전)/v11(대장간+다리 시절)/v12/v13/v14(버려진 밭 도입 전) 저-버전 마이그레이션·검증 테스트 6곳 전부에 `delete raw.projects.worn_out_barn`을 선제적으로 추가한 뒤 실행, 128/128을 첫 실행에서 통과시킴 — 두 번 연속 무결함 적용, DECISIONS.md D024 참고), `npm run build` PASS, `npm run simulate` PASS(`npx tsx`로 재실행 — 이전 구역부터 있던 이 워크트리 계열의 PATH 해석 문제, 콘텐츠와 무관). 브라우저에서 빈 저장의 왕국 화면에 여섯 구역(대장간·다리·식당·길드 회관·밭·축사)이 동시에 조사 가능 상태로 표시됨을 확인 → 축사 조사 클릭 시 "정리 시작" 상태와 정확한 회수품 문구("나무 8개 · 돌 6개")로 전환됨을 확인. 콘솔 경고·오류 없음. (축사의 조사→정리→납품→복원 전체 사이클은 대장간과 동일한 `advanceProjectAction` 코드 경로라 라이브로는 조사 전환까지만 확인했고, 완료·해금은 단위 테스트로 정밀 검증)
- 왕국 복원 다섯 번째 구역(버려진 밭, 2026-09-16): `npm test` 125/125 PASS(기존 122개 + 밭 3개: 도입 전 저장 이전, 저장 버전별 구역 집합 검증, 완료·농사 영구 해금·복원도 5 — 구현 착수 전 D022 체크리스트를 미리 적용해 기존 v10/v11(무너진 식당 도입 전)/v11(대장간+다리 시절)/v12/v13 저-버전 마이그레이션·검증 테스트 5곳 전부에 `delete raw.projects.abandoned_field`를 선제적으로 추가, DECISIONS.md D023 참고), `npm run build` PASS, `npm run simulate` PASS(`npx tsx`로 재실행 — 이전 구역부터 있던 이 워크트리 계열의 PATH 해석 문제, 콘텐츠와 무관). 브라우저에서 빈 저장의 왕국 화면에 다섯 구역(대장간·다리·식당·길드 회관·밭)이 동시에 조사 가능 상태로 표시됨을 확인 → 밭 조사 클릭 시 "정리 시작" 상태와 정확한 회수품 문구("나무 7개 · 돌 5개")로 전환됨을 확인. 콘솔 경고·오류 없음. (밭의 조사→정리→납품→복원 전체 사이클은 대장간과 동일한 `advanceProjectAction` 코드 경로라 라이브로는 조사 전환까지만 확인했고, 완료·해금은 단위 테스트로 정밀 검증)
- 왕국 복원 네 번째 구역(먼지 쌓인 길드 회관, 2026-09-16): `npm test` 122/122 PASS(기존 119개 + 길드 회관 3개: 도입 전 저장 이전, 저장 버전별 구역 집합 검증, 완료·`guild` 기능 영구 해금·복원도 4 — 이 과정에서 기존 v10/v11/v11(대장간+다리 시절) 마이그레이션 테스트 3개가 `raw.projects`에 새로 생긴 `guild_hall` 키를 삭제하지 않은 채 낮은 버전으로 되돌리던 잠재 결함을 발견해 함께 수정, DECISIONS.md D022 참고), `npm run build` PASS, `npm run simulate` PASS(이 워크트리에서 `npm run simulate`가 `tsx` PATH 해석 문제로 직접 실패해 `npx tsx scripts/progression-report.ts`로 동일하게 재실행해 확인 — 콘텐츠 변경과 무관한 환경 문제). 브라우저에서 빈 저장의 왕국 화면에 네 구역(대장간·다리·식당·길드 회관)이 동시에 조사 가능 상태로 표시됨을 확인 → 길드 회관 조사 클릭 시 "정리 시작" 상태와 정확한 회수품 문구("나무 6개 · 돌 4개")로 전환됨을 확인. 콘솔 경고·오류 없음. (길드 회관의 조사→정리→납품→복원 전체 사이클은 대장간·다리·식당과 동일한 `advanceProjectAction` 코드 경로라 라이브로는 조사 전환까지만 확인했고, 완료·해금은 단위 테스트로 정밀 검증 — `unlockFeatures`만으로 완료를 검증하는 첫 사례라 `featureUnlocked` 기반 신규 단위 테스트로 별도 확인)
- 왕국 복원 세 번째 구역(무너진 식당, 2026-09-16): `npm test` 119/119 PASS(기존 116개 + 식당 4개: 도입 전/후 저장 이전, 저장 버전별 구역 집합 검증, 완료·복원도 3, v11 전용 검증 테스트 정확도 보강), `npm run build` PASS, `npm run simulate` PASS. 브라우저에서 빈 저장의 왕국 화면에 세 구역(대장간·다리·식당)이 동시에 조사 가능 상태로 표시됨을 확인 → 식당 조사 클릭 시 "정리 시작" 상태와 정확한 회수품 문구("나무 5개 · 돌 3개")로 전환됨을 확인. 콘솔 경고·오류 없음. (식당의 조사→정리→납품→복원 전체 사이클은 대장간·다리와 동일한 `advanceProjectAction` 코드 경로라 라이브로는 조사 전환까지만 확인했고, 완료·해금은 단위 테스트로 정밀 검증)
- 왕국 복원 두 번째 구역(부서진 다리, 2026-09-15): `npm test` 116/116 PASS(기존 113개 + 다리 3개: 도입 전/후 저장 이전, 저장 버전별 구역 집합 검증, 완료·복원도 2), `npm run build` PASS, `npm run simulate` PASS. 브라우저에서 빈 저장의 왕국 화면에 대장간·다리가 동시에 조사 가능 상태로 표시됨을 확인 → 대장간을 끝까지 진행(조사→정리→나무/돌 납품→복원)해 완료 카드가 "폐허가 된 대장간 복원 완료 · 대장작업 · 도구 제작 이용 가능"으로 정확히 표시됨을 확인(프로젝트별 문구 일반화가 실제로 동작) → 사이드바에 대장작업이 새로 나타나고 낚시는 여전히 없음을 확인 → 새로고침 후 복원도 1과 완료 상태가 그대로 보존됨을 확인. 콘솔 경고·오류 없음. (다리 자체의 조사→정리→납품→복원 전체 사이클은 코드가 대장간과 동일한 `advanceProjectAction`을 그대로 타므로 라이브로는 목록 노출·문구까지만 확인했고, 완료·해금까지는 단위 테스트로 정밀 검증)
- 왕국 복원 첫 수직 슬라이스 후보(2026-09-15): `npm test` 113/113 PASS, `npm run build` PASS, `npm run simulate` PASS, `git diff --check` PASS. 브라우저의 빈 저장에서 왕국이 기본 페이지이고 벌목·채광만 표시되는 것을 확인했다. 대장간 조사→정리 도중 벌목으로 전환→27%에서 재개→회수품 나무 3·돌 4를 포함한 보유분 부분 납품→부족분 채집→복원 공사 완료를 직접 진행했고, 복원도 1·대장작업·도구 제작 해금과 새 탭 재접속 보존을 확인했다. 390×844 좁은 화면에서 한 열 배치와 조작 요소를 확인했으며 브라우저 경고·오류 로그는 없었다.
- 길드 마일스톤 통합 상태: `npm test` 105/105 PASS(기존 99개 + 마일스톤 6개), `npm run build` PASS, `npm run simulate` PASS, `git diff --check` PASS. 브라우저에서 9개 목표 순서와 초기 미달성 상태를 확인하고, 나무 첫 채집 후 첫 목표만 수령 가능으로 전환 → 보상 수령 시 1,000→1,100G → 완료 버튼 비활성화 → 새로고침 후 1,100G와 수령 완료 상태 보존을 확인했다. 경고·오류 로그는 없었다.
- 통합 검증: `npm test` 99/99 PASS(기존 81개 + 성장 곡선/생산 경로 10개 + 길드 일일 퀘스트 8개), `npm run build` PASS, `npm run simulate` PASS, `git diff --check` PASS. 브라우저에서 일일 의뢰 3개 표시, 구리 10개 구매(1,000→850G), 6개 납품(850→886G), 재고 10→4, 완료 상태와 재접속 보존을 확인했고 경고·오류 로그는 없었다.
- 성장 곡선 시뮬레이터 단독 체크포인트: `npm test` 91/91 PASS(기존 81개 + 성장 공식/생산 효율/수확량/도달 시간/보너스/오류 경계·생산 경로 10개), `npm run build` PASS, `npm run simulate` PASS. 첫 기준선에서 해금 도구+속도 음식 10%+경험치 18% 기준 액티브 Lv99는 약 9.4~17.6일, 농사는 약 1,111일, 목장은 약 3,733일로 확인됐다. 무도구 생산 경로는 돌 도구 51초, 구리 도구 2.5분, 철 도구 5.5분, 구운 생선 12초다.
- 길드 일일 퀘스트 단독 체크포인트: `npm test` 89/89 PASS(기존 81개 + 길드 일일 퀘스트 8개). 해금 재료 선정, 날짜 갱신, 납품·보상·재납품 방지, 날짜 경계 식별, v1~v8 저장 이전·검증과 유한 생성 경계를 포함한다.
- (이전) 테스트 81개 통과: 기존 기능 75개 + 세이브 백업/복원 6개(내보내기 후 복원 시 저장 시점 상태와 완전히 동일, 형식/버전이 잘못된 백업은 거부, 체크섬이 있는 저장은 값이 바뀌면 거부되고 체크섬이 없던 옛 저장은 그대로 허용, 체크섬이 문자열이 아니어도 검증을 건너뛰지 않고 거부, 값은 그대로 두고 키 순서만 바뀐 저장은 통과(정렬 직렬화 검증), 필드가 undefined여도 스스로 내보낸 값은 다시 읽힘).
- (이전) 테스트 75개 통과: 기존 기능 65개 + 마법부여·장신구 10개(슬롯 단일 소유/비용/레벨 제한, 승급 시 옵션 보존, 낮은 등급 부여석 거부, 재질 상한 확률 재정규화 경계, 정확한 1개 소비와 실패 원자성, 속도·경험치 온라인/오프라인 동일성, 판매 보너스, 마법 제작, v6→v7 이전/왕복/손상 거부).
- 음식 만료를 포함한 긴 시간 일괄 정산과 짧은 틱의 동일성, 진행률 보존, 재료 부족, 적용 대상, 음식 중첩 방지, 저장 이전, 재접속 중복 지급 방지.
- 첫 생산 루프/낚시·요리/농사/목장/농사·목장 요리 연결 브라우저 검증: 이전 작업들에서 완료(세부 내용은 이전 커밋 로그와 PROGRESS.md 참고).
- 길드 브라우저 검증(2026-09-11): 재료 시드(참나무4/단단한나무4/마법나무2/철광석4, 30,000G) → 길드 탭에 원재료 16종이 해금 상태별로 정확히 표시(해금됨: "개당 X G", 미해금: "OOO Lv.N에 해금") → 나무 10개 구매(골드 -50, 재고 반영) → 참나무 4개 환전(1단계, 나무 +6개 = floor(4×1.5), 정확) → 길드 승급(골드 -3,000, 알림 "중급 길드 승급 · 환전 가능 티어 2단계까지 확대", 화면 전체 갱신) → 승급 후 잠겨있던 단단한나무 환전(2단계)이 열림 확인 → 단단한나무 4개 환전(나무 +9개 = floor(4×2.25), 정확) → 마법나무(3단계)는 "상급 길드 필요"로 계속 잠김 확인.
- 장신구 브라우저 검증(2026-09-11): 마법 Lv50/대장작업 Lv50 및 재료를 넣은 일회성 QA 상태에서 장신구 탭 렌더링 확인 → 왕관 제작 시 500G·돌 벽돌 5개 정확히 차감 → 스톤 재질 상한이 중급으로 표시되고 금급 부여석 확률이 하급 43%/중급 57%로 재정규화됨 확인 → 금급 부여석 1개 사용 후 보유량 10→9, 중급 신속 +4% 결과와 총 효과 즉시 반영 → 구리 승급 시 2,000G·구리 주괴 5개 차감, 같은 중급 신속 +4% 옵션 유지 → 스톤급 부여석 버튼 비활성화, 구리 재질 확률표(40/35/25) 표시 → 마법 탭에 4등급 제작법/레벨/재료/보유량 표시 확인. QA 전용 임시 시드 코드는 검증 직후 제거함.
- 세이브 백업/복원 브라우저 검증(2026-09-12): 설정 탭에서 "백업 파일 다운로드" 클릭 시 콘솔 오류 없이 다운로드 트리거 확인 → 현재 저장의 gold를 42424로 바꾼 JSON을 백업 파일로 선택 후 확인 대화상자 승인 → 헤더 골드 표시와 localStorage 저장이 즉시 42,424로 갱신, 화면 알림과 설정 페이지 메시지 모두 "백업에서 복원했습니다." 표시 확인 → version:999로 조작한 파일을 복원 시도하면 "복원 실패: 지원하지 않는 저장 버전" 메시지만 뜨고 저장은 그대로 42,424 유지(미적용) 확인 → 확인 대화상자에서 취소하면 파일 내용이 전혀 반영되지 않음(골드 그대로) 확인.
- 리뷰 수정 재검증(2026-09-12, `b59a1ff`): 오래된 lastSaveTime과 진행 중인 벌목 작업이 포함된 백업을 복원하면 "백업에서 복원했습니다. 자리를 비운 동안 33회 작업했습니다."가 표시되고 벌목 레벨이 실제로 1→7로 오름(advance() 반영 확인) → 문법이 깨진 JSON 파일은 "복원 실패: 저장 파일 형식이 올바르지 않습니다"(한글)로 거부 → 다운로드 버튼 클릭 시 콘솔 오류 없음(앵커 DOM 부착 + 지연 URL 해제 확인).
- 체크섬/클립보드 검증(2026-09-12): 이 자동화 브라우저는 클립보드 쓰기 권한이 없어 `navigator.clipboard.writeText` 호출이 `NotAllowedError`로 실패 → 앱이 이를 잡아 "클립보드 복사에 실패했습니다..." 한글 메시지를 정상 표시함을 확인(실제 사용자 브라우저에서는 성공 경로 동작 예상, 코드상 성공/실패 모두 처리됨). 저장의 gold를 999999999로 바꾼 JSON을 복원 시도 → "복원 실패: 저장 데이터가 손상되었거나 수정되었습니다"로 거부되고 기존 저장(gold 7,777) 그대로 유지 확인 → 변조하지 않은 정상 백업(현재 localStorage 값 그대로)은 정상적으로 "백업에서 복원했습니다..." 표시와 함께 복원됨을 확인.
- 2라운드 리뷰 수정 재검증(2026-09-12): `checksum`을 문자열이 아닌 숫자(12345)로 깨뜨린 파일을 복원 시도 → 이전 코드였다면 타입 체크에 걸려 검증을 건너뛰고 통과했을 케이스가 정확히 "복원 실패: 저장 데이터가 손상되었거나 수정되었습니다"로 거부되고 기존 저장 그대로 유지됨을 확인 → 변조하지 않은 정상 백업은 여전히 정상 복원됨과 콘솔 오류 없음을 재확인.
- 다중 탭 저장 동기화 브라우저 검증(2026-09-12, 실제 두 탭으로 진행): 새 탭(전혀 조작하지 않음)이 다른 탭에서 시작한 나무 채집 작업을 `storage` 이벤트만으로 그대로 반영해 자체적으로 계속 진행함을 확인(레벨업까지 정확히 반영) → 한 탭에서 직접 주입한 값(gold 424242, 다른 탭에서 쓴 것처럼 시뮬레이션)을 수신 탭이 그대로 흡수해 화면과 저장에 즉시 반영, 이후 그 탭 자신의 강제 자동 저장으로도 값이 되돌아가지 않음을 확인(콘솔 디버그 로그로 `decodeSave` 성공·`setState` 호출을 직접 확인 후 제거) → 이는 개발 중 목격했던 "방치된 탭의 자동 저장이 최신 저장을 덮어쓰는" 문제가 반대로 재현되지 않음을 보여준다. `blocked` 탭이 다른 탭의 유효한 쓰기로 복구되는 경로는 코드가 `restoreFromBackup`과 동일한 성공 경로(디코딩 성공 시 `blocked = false`)를 타는 것으로 확인했으나, 살아있는 탭의 `pagehide` 플러시가 손상 주입을 곧바로 되돌려버려 완전히 격리된 재현은 이번 세션에서는 만들지 못했다(DECISIONS.md D014에 기록).
- 리뷰 수정(가시성 구분) 재검증(2026-09-12, 실제 두 탭, `document.visibilityState`를 각각 강제로 `visible`/`hidden`으로 지정해 결정론적으로 검증): "보이는" 탭에서 나무 채집을 시작 → "숨겨진" 탭에서 그 저장을 오래된 값(gold 111, currentAction null)으로 직접 덮어쓰기 시도(다른 탭의 오래된 자동 저장을 시뮬레이션) → "보이는" 탭이 그 값을 받아들이지 않고 즉시 자신의 현재 상태(gold 424242, 진행 중인 채집)를 다시 저장해 되찾음을 확인(localStorage가 되돌아간 값이 아니라 "보이는" 탭의 값으로 유지됨) → 이어서 "보이는" 탭에서 다른 작업(나무 채집)을 시작하자 "숨겨진" 탭이 여전히 그 내용을 정상적으로 흡수함을 재확인(원래 방향은 계속 동작). 이는 1차 구현에서 독립 리뷰가 지적한 "반대 방향 덮어쓰기"가 실제로 수정되었음을 보여준다.
- 길드 일일 퀘스트 브라우저 검증(2026-09-12): 길드 탭에 3개의 "N개 납품" 퀘스트가 보상과 함께 표시 확인 → 필요한 원재료(돌 9개)를 구매 후 "납품" 클릭 → 골드 +18(=9×1×2) 정확히 반영, 인벤토리 소모, 알림 "일일 퀘스트 완료 · 돌 9개 납품 · 18 G 획득" 표시, 버튼이 "완료됨"으로 바뀌고 재납품 불가 확인 → `Date.now()`를 실제보다 2일 뒤로 임시 오버라이드한 뒤 자동 저장을 강제로 트리거하자, 완료 여부와 무관하게 완전히 새로운 3개 퀘스트(전부 미완료)로 교체됨을 확인 — 방금 완료했던 퀘스트의 완료 표시가 새 세트에 남지 않음을 눈으로 확인.
- 리뷰 수정 재검증(2026-09-12): 캐모마일 10개를 구매한 뒤 8개 납품 퀘스트를 완료 → 골드가 정확히 48(=8×3×2, 장신구 미착용이라 보너스 0%) 증가함을 확인해 `Math.floor`+`accessoryBonus` 반영 수식이 실제로 그 값을 낸다는 것을 재확인. 인덱스+재료 불일치 방지, 마이그레이션 시 실제 레벨 반영, 중복 재료 거부는 `pagehide`로 인한 자체 복구(D014에서 목격한 동일 현상)로 실시간 재현이 어려워 새 단위 테스트(`guild-quest.test.ts`)로 검증을 대체함 — 89/89 PASS.
