# Current handoff

- Current goal: fixed a content-consistency gap found during a user-requested full-codebase audit ("전체적으로 잘못된곳이나 문제되는곳 없는지 조사하고 분석해줘", then "고쳐줘" to authorize the fix). `src/content/guild.ts`'s `ExchangeDB` had a high-tier→lowest-tier exchange path for every other pure-gather skill (logging/mining/fishing) but **none for foraging**, despite foraging being structurally identical to logging (D027: "채집은 벌목과 1:1로 동일한 구조").
- Branch: `fix/foraging-exchange-tiers`, created from freshly verified `origin/main` `a08fb0b81a0a2e8d03ebe0e945f3d5e156ed75b7` on 2026-09-16 KST. The remote feature ref is absent (not yet pushed).
- Checkpoint type: not yet checkpointed in this turn — code, tests and docs are complete and verified locally, but no commit/push has been made yet. This HANDOFF captures the state immediately before that checkpoint.
- What was fixed: added `wild_mushroom: {targetId: 'wild_berry', tierGap: 1}`, `wild_herb: {targetId: 'wild_berry', tierGap: 2}`, `rare_mushroom: {targetId: 'wild_berry', tierGap: 3}` to `ExchangeDB` — exactly mirroring logging's `oak`/`hardwood`/`magic_wood`→`wood` pattern. Root cause: when D027 added foraging's 4 `ResourceDB` rows, the matching `ExchangeDB` entries were never added (no record of this being a deliberate exclusion anywhere in DECISIONS.md). **Zero engine or UI code changed** — `exchangeResource()` in `model.ts` and `GuildView.tsx`'s "환전" section both already iterate `ExchangeDB` generically, so the new entries took effect with a pure data change.
- Tests: added one dedicated positive-path test (들버섯→산딸기 exchange, mirroring the existing oak→wood test) and extended the existing "최하위 재료는 환전 대상이 아니다" test to include `wild_berry`. Two pre-existing generic invariant tests (`Object.entries(ExchangeDB)`-driven: anti-circular-profit check, same-skill/lower-tier check) needed no changes and automatically cover the 3 new entries.
- Verification: `npm test` 142/142 PASS (141 existing + 1 new, first run, zero defects), `npm run build` PASS. Re-ran the cross-content-reference consistency script used during the audit (checks every recipe/project-material/animal/tool/accessory reference against `ResourceDB`, plus exchange-coverage-per-multi-tier-gather-skill) — confirms zero remaining gaps of this kind anywhere in the content data. Browser QA: unlocked foraging (Lv30) + guild via live `gameState.mutate()`, seeded 12 `wild_mushroom` + 9 `wild_herb`, confirmed the Guild screen's 환전 section renders the 3 new rows with correct guild-tier gating (identical shape to logging/mining/fishing rows), then clicked the actual "전부 환전" button for 들버섯 → confirmed 12 wild_mushroom exactly became 18 wild_berry (12 × 1.5, matching `exchangeRate`) in both live state and rendered inventory. No console errors.
- Docs updated this round: DECISIONS.md (D031), PROGRESS.md (new "버그 수정 — 채집 환전 경로 누락" section). README.md/docs/implementation_status.md/docs/implementation_plan.md were not touched — none of them enumerate individual `ExchangeDB` entries, so nothing in them was inaccurate.
- Known risks/limits: none. Pure data addition matching an established, well-tested pattern; no save-version bump needed (ExchangeDB membership isn't part of the save schema — `exchangeResource` looks it up live from content, not from saved state).
- Scope note: the same audit also flagged two lower-priority, non-blocking items that were explicitly **not** included in this fix per the user's instruction (only this item was authorized): (1) `src/engine/formulas.ts`'s `getActualDurationMs`/`MIN_ACTION_DURATION_MS`/`clampProbabilityPercent` are fully unit-tested but never called by any live code path; (2) `kingdom.test.ts`'s "v20 저장은..." test still doesn't pin `raw.version` explicitly (already recorded in D027–D030 as a recurring pattern). Neither was touched.
- Review/merge: no independent review exists yet for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
- Exact next action: commit this checkpoint (guild.ts + guild.test.ts + DECISIONS.md + PROGRESS.md), push to `origin/fix/foraging-exchange-tiers`, verify remote SHA equality. Then stop and report to the user — do not review/merge or start further work without explicit instruction.
