# Current handoff

- Current goal: ranching minimal loop (PROGRESS Planned item 2), implemented, reviewed (round 1), and fixed. Re-review runs against this snapshot.
- Feature branch: `feature/ranching-loop`, created from fresh `main` (`08c37b0`, which already includes the merged farming loop). Configured remote: `origin` (`https://github.com/PlantJelly/Melvorlike.git`).
- Checkpoint type: Stable — implementation complete, `npm test`/`npm run build` pass, browser-verified, review round 1 findings fixed.
- Review round 1 (`9161cf8`, self-review at high effort) found 3 non-blocking findings, all closed in this snapshot: (1)+(2) two test-coverage gaps — the "partially affordable cycle" branch in `advanceRanch` (some but not all pending cycles payable) and the "two animals sharing one feed item under scarcity" scenario PROGRESS's acceptance criteria explicitly named — both now covered in `src/engine/ranch.test.ts` (manually traced expected values matched actual test results exactly: 2 eggs/2 wheat left for the partial case; chicken consumes first and sheep starves for the shared-feed case). (3) `ranchStarved(state(), a.id)` was called twice in adjacent `Show` blocks in `RanchingView.tsx` — hoisted to a single `starved()` accessor.
- Known pre-checkpoint parent: `08c37b072bbae63e276c5ac66c416e989975b297` (main, post farming-loop merge + docs).
- Snapshot commit: this HANDOFF update is committed together with the implementation in one commit on `feature/ranching-loop`; resolve with `git log -1 -- HANDOFF.md`.
- Completed this session: buy animal → feed consumption → automatic output on a repeating cycle → safe stop when feed runs out → automatic resume, for 3 animals (닭 Lv1/500G/밀, 양 Lv15/2,000G/밀, 소 Lv30/5,000G/당근) producing 달걀/양털/우유. `ranching` added as a full `SkillId`/`playable` entry (same free level/tool-tier reuse as farming, D005). Generalized the farming-only `r.skill === 'farming'` checks in `begin()` and `decodeSave`'s `currentAction` validator into a shared `passiveSkills` list (`src/content/resources.ts`) covering both `farming` and `ranching` — proactively closing the class of gap that farming review round 2 found (see DECISIONS D005's resolution note, D007, D008). Save schema v5 with v1–v4 migration in `src/engine/save.ts`. New `src/content/animals.ts`, `src/ui/RanchingView.tsx` (detail page + global `RanchStatus` strip), wiring in `src/App.tsx`.
- In progress at snapshot: none — round 1 review and its fixes are done; this checkpoint is being recorded, and re-review (round 2) follows against the resulting commit.
- Incomplete: connecting farming/ranching output to cooking, guild, enchanting/accessories (PROGRESS Planned items 3–5); ranching extensions (headcount cap per animal, feed-related consumables) noted as future work in `docs/implementation_status.md`.
- Tests: `npm test` — 47/47 PASS (36 prior + 11 ranching tests in `src/engine/ranch.test.ts`, including the 2 added for round-1 findings; two existing version-3/4 assertions in `food.test.ts`/`farm.test.ts` updated to 5 for the schema bump; the content-reference test extended to validate every `AnimalDB` entry's feed/product references). `npm run build` PASS. Both run on the exact code being checkpointed.
- Browser verification (2026-09-11, local `npm run dev`): bought a chicken (gold -500, notice shown, global status showed live progress) → with insufficient feed, confirmed the starved state ("사료가 부족해 대기 중입니다") both in the global strip and the detail card → refilled feed, confirmed resume → with sufficient feed (20 wheat) and enough elapsed real time, confirmed via direct save-state inspection that production fired exactly once as expected (wheat 20→15, egg 1, ranching exp +60, cycle progress reset) → confirmed the egg appears and is sellable in Inventory → confirmed a "목장 · 사료통" tool-upgrade card appears automatically on the Tools page. Note: `navigate()` to the same URL was unreliable for forcing a fresh reload in this session's browser tooling (a stray leftover tab from an earlier session was also briefly a confound); `location.reload()` plus closing stray tabs was what actually worked — not an application bug, a tooling quirk worth remembering for future sessions.
- Checks not run: no lint script or tracked GitHub Actions workflow exists.
- Failed tests/core error: none. Two test-authoring mistakes were caught and fixed while writing `ranch.test.ts` (using `advance()`'s absolute-time semantics incorrectly once, and insufficient starting gold for a two-animal purchase test) — not application bugs.
- Known issues carried over: multi-tab save contention unsupported (and was directly observed in this session — a lingering background tab's autosave interval can clobber a foreground tab's localStorage writes; not a new regression, just newly witnessed firsthand); backup/recovery UI absent; farming/ranching balance provisional; single farm plot, one animal per type (no headcount), no fertilizers/consumables, no auto-plant/harvest, crops/animal products don't yet feed any cooking recipe.
- Uncertainty: exact ranching balance (cycle time, buy price, feed amount) untested against real play sessions.
- Exact first action next session: read AGENTS → HANDOFF → PROGRESS → DECISIONS → README, inspect branch/status/HEAD, fetch origin and compare `feature/ranching-loop` and `main` against this snapshot. If a review PASS for the current exact HEAD isn't in evidence, run `/review` before anything else — do not merge on completion alone. If already reviewed and PASSed and the user wants it merged, that still requires an explicit `/merge` request.
- Prerequisites: repository-root PowerShell (Node/npm are not on the default Bash PATH in this environment — prefix commands with `$env:Path += ";C:\Program Files\nodejs"` in PowerShell), existing Git authentication. No new global config, hooks, CI or background services were added.
- Remote preservation: push explicitly to `origin feature/ranching-loop` after this commit and compare local HEAD to a fresh `git ls-remote --heads origin refs/heads/feature/ranching-loop`.
- Review evidence: none yet for this branch. The farming-loop review reports are gone (each round overwrote the last, and none were ever committed, per AGENTS.md's no-self-referencing-review-commit rule) — that PASS is attested only by the merge commit `510386f` and PROGRESS.md's summary now, not by a live file.
