# Current handoff

- Current goal: completed the independent review and main integration of the kingdom-restoration fourth district (guild hall → `guild` feature), requested by the user in one step ("리뷰하고 문제없으면 머지하고 다음 개발 진행해줘"). Next development (fifth district) follows in this same continuation.
- Branch: `main`. Feature branch `feature/kingdom-guild-hall` and `origin/main` were both verified at `d5b0f6f3815fe2ca83add25d2ce0ba818c5060ed` after the fast-forward push on 2026-09-16 KST.
- Checkpoint type: Stable integration record. Known pre-checkpoint parent: `ba993a8`. Locate the snapshot commit with `git log -1 -- HANDOFF.md`.
- Review: independent full merge-base-to-HEAD diff review (this session, re-reading the diff fresh rather than trusting the implementing turn's own summary) covering `projects.ts`, `model.ts`, `save.ts`, `kingdom.test.ts` (line by line, including the three latent-bug fixes), all fixture tests, and doc diffs. No blocking findings; `KingdomView.tsx`/`App.tsx` had zero diff, and the first-ever empty-`unlockSkills` project was specifically checked against `unlockSummary()`'s array-spread-then-join logic (handles cleanly). PASS. Report stored outside the worktree at `git rev-parse --git-path development-workflow-review.md`.
- Verification (independently re-run this session, not reused): `npm test` 122/122, `npm run build`, `npm run simulate` (via `npx tsx` — this worktree's `npm run simulate` can't resolve the `tsx` binary via PATH, a pre-existing environment quirk unrelated to this diff), `git diff --check`, `git merge-tree` (no conflicts — target IS the merge-base, pure fast-forward) all PASS on reviewed SHA `d5b0f6f`, reconfirmed again on the fast-forwarded `main` tip (same SHA). Browser QA against a dev server pointed explicitly at the feature worktree: fresh save shows all four districts surveyable with no console errors; live-clicked the guild hall's own survey action at the exact reviewed SHA and confirmed the correct salvage text and phase transition. Did not repeat the guild hall's full clear/restore timers live in this review pass (same code path already exercised live for the forge across three prior reviews; the guild hall's own full completion is covered by 3 new unit tests) — judged proportionate.
- CI/policy: 0 GitHub Actions workflows repo-wide (re-queried fresh, unauthenticated REST API), 0 check-runs for reviewed SHA `d5b0f6f` → NOT CONFIGURED. Branch protection not re-queryable (still requires auth unavailable here); no change from prior finding.
- Implemented (unchanged from feature branch): `guild_hall` project — survey → 60s clearing (salvage wood 6/stone 4) → deliver wood 14/grilled_fish 5 → 80s restoration → permanent `guild` feature unlock (no new skill), restoration score 4 total. All four districts surveyable from a fresh save with no hard prerequisite; grilled_fish (a cooking product) naturally nudges finishing the restaurant first. Save v13 via `ProjectDef.introducedVersion`-based version gating (same generic logic as v11/v12, unchanged). `src/engine/model.ts` and `src/ui/KingdomView.tsx` needed zero changes — confirms the `introducedVersion` pattern (D020/D021/D022) is reusable even for a feature-only, skill-less unlock. Third recurrence of a migration-test staleness bug (missing `delete raw.projects.<new_id>` in lower-version fixtures) was self-caught and fixed during implementation; documented as a forward-looking checklist item in DECISIONS.md D022.
- Known risks (unchanged, disclosed): no hard prerequisite/lock system between districts — order only softly enforced via material dependency. New saves have no progression path past forge+bridge+restaurant+guild_hall until further districts are built. All new numbers are unplaytested. The reverse-direction save-validation check remains deliberately deferred (D020/D021) until farming/ranching/magic each have a real unlocking project.
- Exact next action: pick the next kingdom district per `docs/implementation_plan.md` §4 order (버려진 밭 — unlocks farming) on its own fresh feature branch, after repository freshness checks. Remember D022's caution when adding the new project: every existing lower-version migration test in `kingdom.test.ts` that builds `raw` via `encodeSave(s)` needs a `delete raw.projects.<new_id>` added, or its final "downgraded save still decodes" assertion may silently break for the wrong reason.
