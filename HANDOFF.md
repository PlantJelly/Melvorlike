# Current handoff

- Current goal: implement the kingdom-restoration sixth district, "낡은 축사" (worn-out barn), permanently unlocking the `ranching` skill, following the plan order in `docs/implementation_plan.md` §4 (after 버려진 밭).
- Branch: `feature/kingdom-worn-barn`, created from freshly verified `origin/main` `31662b391359e0530cdcb4a140afe62f3da65378` on 2026-09-16 KST. The remote feature ref was absent at creation.
- Checkpoint type: Stable. This district restarts the material-dependency chain that D023 found broken at guild_hall: delivery materials include wheat (a farming product, and already the existing feed item for chickens/sheep in the ranching system) to nudge finishing the field first. `src/engine/model.ts` and `src/ui/KingdomView.tsx` needed zero changes again.
- Implemented: `worn_out_barn` project (survey → 80s clearing, salvage wood 8/stone 6 → deliver wood 18/wheat 10 → 100s restoration → permanent `ranching` unlock, restoration score +1 = 6 total). All six districts surveyable from a fresh save with no hard prerequisite. Save bumped to v15 with the same generic `introducedVersion`-based validation; a v14 (forge+bridge+restaurant+guild_hall+abandoned_field) save migrating to v15 keeps all five prior states and gets the barn as a fresh 'surveyable' entry; a pre-kingdom v1-v9 save still gets all six auto-completed via `unlockedGame()`.
- Verification: `npm test` 128/128 (125 prior + 3 new: v14→v15 migration keeps all five prior districts/defaults barn unstarted, v15 project-set validation, full barn completion grants `skillUnlocked(s,'ranching')` + restoration score 6), `npm run build` PASS, `npm run simulate` PASS via `npx tsx` (same pre-existing `node_modules/.bin` PATH quirk, unrelated to this diff). Browser QA against a dev server pointed explicitly at this worktree: fresh save shows all six districts surveyable simultaneously; clicked the barn's own survey action and confirmed the correct salvage text ("나무 8개 · 돌 6개") and phase transition to 폐허 정리, no console errors. Did not run the barn's own full 80s+100s clear/restore timers live — same `advanceProjectAction` code path already exercised live for the forge (across five prior reviews) and now covered by 3 dedicated unit tests; judged proportionate, consistent with prior districts.
- Applied proactively (second time in a row, see DECISIONS.md D024): D022/D023's checklist was applied *before* running tests — added `delete raw.projects.worn_out_barn` to all six existing lower-version fixtures (v10-only, v11 "무너진 식당 도입 전", v11 "대장간+다리 시절", v12, v13, v14 "버려진 밭 도입 전") up front. Tests passed 128/128 on first run without needing a follow-up fix, confirming this checklist is a reliably repeatable procedure, not a one-off lesson.
- Known risks/limits (disclosed, unchanged from D020-D023, restated in D024): no hard prerequisite/lock system between districts — order only softly enforced via material dependency; that chain broke once at guild_hall (a service feature with no output) and has now been restarted using wheat. New saves have no progression path past these six districts until further districts are built. All new numbers are unplaytested. The reverse-direction save-validation check remains deliberately deferred — magic still has no project granting it; once the seventh district (쓰러진 마법탑) exists, every skill `unlockedGame()` pre-unlocks will finally have a real granting project, making this check safe to add for the first time (flagged in D024 as something to revisit then).
- Review/merge: no independent review exists yet for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
- Exact next action: commit this checkpoint (content/save changes + doc updates), push to `origin/feature/kingdom-worn-barn`, verify remote SHA equality. Then either continue to the seventh and final planned district (쓰러진 마법탑 — unlocks magic/accessories) on its own feature branch, or perform the independent review + merge if the user explicitly requests it. When starting that district, consider whether an enchant stone (a magic output) can continue the material-dependency chain, and revisit D024's note about finally adding the reverse-direction save-validation check once magic has a real unlocking project.
