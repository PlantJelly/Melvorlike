# Current handoff

- Current goal: implement the kingdom-restoration seventh district, "쓰러진 마법탑" (fallen magic tower), the last district planned in `docs/implementation_plan.md` §4, permanently unlocking both the `magic` skill and the `equipment` (accessory screen) feature.
- Branch: `feature/kingdom-fallen-tower`, created from freshly verified `origin/main` `b176256146b9de5580c1512b19430dc440d3f2c4` on 2026-09-16 KST. The remote feature ref was absent at creation.
- Checkpoint type: Stable. Second district (after guild_hall) to unlock a skill and a feature simultaneously (`unlockSkills: ['magic']`, `unlockFeatures: ['equipment']`). Delivery materials include wool (a ranching product) to continue the material-dependency chain D024 restarted — magic's own output (enchant stones) can't gate its own unlocking project (circular: no magic skill exists yet to produce them), so the chain uses the immediately-preceding district's real output instead, consistent with every prior district. `src/engine/model.ts` and `src/ui/KingdomView.tsx` needed zero changes again.
- Implemented: `fallen_tower` project (survey → 90s clearing, salvage wood 9/stone 7 → deliver wood 20/wool 12 → 110s restoration → permanent `magic` + `equipment` unlock, restoration score +1 = 7 total). All seven districts surveyable from a fresh save with no hard prerequisite. Save bumped to v16 with the same generic `introducedVersion`-based validation; a v15 (six prior districts) save migrating to v16 keeps all six prior states and gets the tower as a fresh 'surveyable' entry; a pre-kingdom v1-v9 save still gets all seven auto-completed via `unlockedGame()`.
- Verification: `npm test` 131/131 (128 prior + 3 new: v15→v16 migration keeps all six prior districts/defaults tower unstarted, v16 project-set validation, full tower completion grants `skillUnlocked(s,'magic')` + `featureUnlocked(s,'equipment')` + restoration score 7), `npm run build` PASS, `npm run simulate` PASS via `npx tsx` (same pre-existing `node_modules/.bin` PATH quirk). Browser QA against a dev server pointed explicitly at this worktree: fresh save shows all seven districts surveyable simultaneously; clicked the tower's own survey action and confirmed the correct salvage text ("나무 9개 · 돌 7개") and phase transition to 폐허 정리, no console errors.
- Applied proactively for the **third consecutive round** (see DECISIONS.md D025): D022-D024's checklist was applied before running tests — added `delete raw.projects.fallen_tower` to all seven existing lower-version fixtures (v10-only, v11 "무너진 식당 도입 전", v11 "대장간+다리 시절", v12, v13, v14, v15 "낡은 축사 도입 전") up front. Tests passed 131/131 on first run with zero staleness bugs — three consecutive clean rounds confirm this is a stable, repeatable procedure.
- **This is the last district planned in `docs/implementation_plan.md` §4.** Its completion means every skill `unlockedGame()` pre-unlocks (cooking, farming, ranching, magic) and every feature (tools, equipment, guild) now has a real granting project. This makes it safe, for the first time, to add the reverse-direction save-validation check that D020/D021 deliberately deferred (every `unlockedSkills`/`unlockedFeatures` entry must be explainable by a starter or a completed project) — but this is a `save.ts` core-validation change, not content, so it was deliberately kept out of this checkpoint as a separate atomic task (D025). Flagged in PROGRESS.md for the user to request explicitly.
- Known risks/limits (disclosed, unchanged from D020-D024, restated in D025): no hard prerequisite/lock system between districts — order only softly enforced via material dependency. All new numbers are unplaytested.
- Review/merge: no independent review exists yet for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
- Exact next action: commit this checkpoint (content/save changes + doc updates), push to `origin/feature/kingdom-fallen-tower`, verify remote SHA equality. Then, since the kingdom district roadmap is now complete, the next development direction is open and needs explicit user direction: (a) the four new active skills (야외 채집/목공/조제/재봉) from `docs/implementation_plan.md`'s original skill list, (b) the reverse-direction save-validation check flagged in D025, or (c) overall production-network balance pass. Do not assume which one without asking, unlike the kingdom-district chain where "다음 개발" had an unambiguous next item.
