# Current handoff

- Current goal: implemented the third of the four user-requested new active skills — 조제(apothecary) — as the tenth kingdom district ("무너진 약방"/"Ruined Apothecary"), continuing directly from the reviewed+merged woodworking round per the user's chained instruction "1번 진행해줘" (선택지 1: 리뷰하고 문제없으면 머지 후 조제 계속 진행).
- Branch: `feature/kingdom-ruined-apothecary`, created from freshly verified `origin/main` `fbb724a07f2c688cc240688834a977bfd6b92b0b` on 2026-09-16 KST. The remote feature ref is absent (not yet pushed).
- Checkpoint type: not yet checkpointed in this turn — code, tests and docs are complete and verified locally, but no commit/push has been made yet. This HANDOFF captures the state immediately before that checkpoint.
- What was added: `ruined_apothecary` project (`introducedVersion: 19`, `unlockSkills: ['apothecary']`, materials wood 26 + `plank` 12 — the immediately-preceding district's (woodworking's) real output, continuing the material-dependency-chain principle for the sixth time). New `SkillId` `'apothecary'` — a crafting skill like woodworking, but paired with **foraging** rather than logging: the overall active-skill list order (벌목/채광/낚시/야외채집/대장작업/목공/요리/조제/재봉/마법) forms "gather skill → the crafting skill that consumes it" pairs (logging↔woodworking, mining↔blacksmithing), so foraging↔apothecary was designed the same way. 4 recipes (산딸기 물약/들버섯 연고/산약초 영약/영지버섯 묘약) consume foraging's 4 tiers 1:1, with numbers directly reused from woodworking's same-tier recipes (safe since foraging's raw numbers are themselves 1:1 copies of logging's, per D027). `Model.version` 18→19. `save.ts`'s skill/tool restoration loops gained `if (version < 19 && id === 'apothecary') continue;` (same pattern as woodworking's `< 18` guard). **Zero changes to `model.ts`/`App.tsx`/`KingdomView.tsx`/`ProductionView.tsx`/`ToolsView.tsx`** — 10th confirmation the architecture is fully generic.
- Recurring bug pattern confirmed a third time: the *previous* round's newly-added "v18 저장은..." boundary test again had the implicit-ambient-version bug (same class D027/D028 found). Found and fixed it at the start of this round (also added the positive-pass assertion it was missing). This is now a **confirmed structural pattern**, documented in DECISIONS.md D029: a newly-written boundary test can never fail on this bug in the same round it's added (ambient version happens to match by construction), so it can only ever be caught retroactively in the *next* round. The next implementer (재봉/sewing round) must proactively check this round's own new "v19 저장은..." test at the very start, before writing any new code.
- `guild-quest.test.ts` was again unaffected (apothecary's 4 new resources all have `recipe` set, confirming D027's caution a second time — this file only needs re-checking when a new *recipe-less* gather resource is added).
- Verification: `npm test` 139/139 PASS (first run, zero defects — third consecutive clean round since the full D022–D028 checklist has been applied from the start), `npm run build` PASS, `npm run simulate` PASS (via `npx tsx`, apothecary's 4 tiers report efficiency identical to woodworking's, as designed). Browser QA: completed all 9 prior districts via `gs.mutate()`, drove `ruined_apothecary` through survey→clear→deliver→restore via the real engine functions. Confirmed: "무너진 약방 복원 완료 · 조제 해금" notice, restoration count 10, new "조제" sidebar entry, all 4 recipes rendering correctly with ingredient costs. Crafted `berry_tonic` via the actual UI button, advanced the engine clock 8.5s, and confirmed both live state and rendered DOM show the correct increment (2 tonics crafted, 6 wild_berry consumed, 60 exp gained). No console errors.
- Docs updated this round: DECISIONS.md (D029), PROGRESS.md (new "신규 액티브 스킬 3/4" section + roadmap item 8 + Milestones line), README.md (district list + save-version line, v18→v19), docs/implementation_status.md (title, new bullet, version/migration lines, 미구현/검증 sections), docs/implementation_plan.md (§5 apothecary entry + open caution for sewing).
- Known risks/limits: none new. Purely additive content + one save-schema version bump; no engine logic changed.
- Review/merge: no independent review exists yet for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
- Exact next action: commit this checkpoint (all listed source/test/doc changes), push to `origin/feature/kingdom-ruined-apothecary`, verify remote SHA equality. Then either continue to 재봉(sewing) as the next (and final) atomic task in the new-skills roadmap, or perform the independent review + merge, per explicit user instruction — do not assume which. If continuing to sewing, the implementer must first decide its material-dependency pairing (no natural "gather↔craft" partner remains in the active-skill list order — see DECISIONS.md D029's caution) before writing any code.
