# Current handoff

- Current goal: implement the kingdom-restoration third district, "무너진 식당" (ruined restaurant), unlocking cooking, following the plan order in `docs/implementation_plan.md` §4 (after 부서진 다리).
- Branch: `feature/kingdom-restaurant`, created from freshly verified `origin/main` `a20dcd792680ea129c8fc82b4c0cfb22d0d1ddc6` on 2026-09-16 KST. The remote feature ref was absent at creation.
- Checkpoint type: Stable. This confirms the `introducedVersion` pattern from D020/D021 generalizes cleanly a second time: **`src/engine/model.ts` and `src/ui/KingdomView.tsx` needed zero changes.** Only `src/content/projects.ts` (new `ruined_restaurant` entry), `src/engine/save.ts` (version array/type union extended to 12 — validation logic itself is already generic and untouched), and version-number bumps in fixture tests changed.
- Implemented: `ruined_restaurant` project (survey → 50s clearing, salvage wood 5/stone 3 → deliver wood 12/fish_small 8 → 70s restoration → permanent cooking unlock, restoration score +1 = 3 total). All three districts surveyable from a fresh save with no hard prerequisite; fish_small (a fishing product) in the restaurant's delivery requirement naturally nudges finishing the bridge first, mirroring how the bridge's brick requirement nudges finishing the forge. Save bumped to v12 with the same `introducedVersion`-based validation already built for v11 — a v11 (forge+bridge only) save migrating to v12 keeps both prior states and gets the restaurant as a fresh 'surveyable' entry; a pre-kingdom v1-v9 save still gets all three auto-completed via `unlockedGame()`.
- Verification: `npm test` 119/119 (116 prior + 4 new: v11→v12 migration keeps forge+bridge/defaults restaurant unstarted, v11-pinned project-set validation tightened to explicitly exclude the not-yet-introduced restaurant, full restaurant completion grants cooking + restoration score 3), `npm run build` PASS, `npm run simulate` PASS on 2026-09-16. Browser QA against a dev server pointed explicitly at this worktree: fresh save shows all three districts surveyable simultaneously; clicked the restaurant's own survey action and confirmed the correct salvage text ("나무 5개 · 돌 3개") and phase transition to 폐허 정리, no console errors. Did not run the restaurant's own full 50s+70s clear/restore timers live — same `advanceProjectAction` code path already exercised live for the forge (twice, across two prior reviews) and now covered by 4 dedicated unit tests; judged proportionate.
- Fixed during this task (test accuracy, not a product defect): the prior "v11 저장은 두 프로젝트를 모두 검증" test implicitly relied on whatever `encodeSave` produces for "current version" rather than pinning to v11 explicitly — after this branch bumped the live version to 12, that test would have silently started exercising v12 behavior under a stale v11-labeled name. Rewrote it to explicitly construct a v11-shaped save (matching its name and comments) before asserting.
- Known risks/limits (disclosed, unchanged from D020/D021): no hard prerequisite/lock system between districts — order only softly enforced via material dependency. New saves have no progression path past forge+bridge+restaurant until further districts are built. All new numbers are unplaytested. The reverse-direction save-validation check (every unlock explained by a starter or completed project) remains deliberately deferred — farming/ranching/magic still have no project granting them, so it would still conflict with `unlockedGame()`'s test-only full-unlock convention.
- Review/merge: no independent review exists yet for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
- Exact next action: commit this checkpoint (content/save changes + doc updates), push to `origin/feature/kingdom-restaurant`, verify remote SHA equality. Then either continue to the next district (길드 회관, or reorder per user preference) on its own feature branch, or perform the independent review + merge if the user explicitly requests it.
