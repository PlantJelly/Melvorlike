# Current handoff

- Current goal: implement the kingdom-restoration fourth district, "먼지 쌓인 길드 회관" (dusty guild hall), permanently unlocking the `guild` feature, following the plan order in `docs/implementation_plan.md` §4 (after 무너진 식당).
- Branch: `feature/kingdom-guild-hall`, created from freshly verified `origin/main` `ba993a83ee8f110a5f36a313d116954166b79794` on 2026-09-16 KST. The remote feature ref was absent at creation.
- Checkpoint type: Stable. This is the first district that unlocks an already-fully-implemented feature (`guild`, from the previously merged `feature/guild-daily-quests`/`feature/guild-milestones` work) rather than a new skill: `unlockSkills: []`, `unlockFeatures: ['guild']`. Confirms `src/engine/model.ts` and `src/ui/KingdomView.tsx` needed zero changes again, even for a skill-less project. Only `src/content/projects.ts` (new `guild_hall` entry), `src/engine/save.ts` (version array/type union extended to 13), and version-number bumps in fixture tests changed.
- Implemented: `guild_hall` project (survey → 60s clearing, salvage wood 6/stone 4 → deliver wood 14/grilled_fish 5 → 80s restoration → permanent `guild` unlock, restoration score +1 = 4 total). All four districts surveyable from a fresh save with no hard prerequisite; grilled_fish (a cooking product) in the delivery requirement naturally nudges finishing the restaurant first, mirroring the brick/fish_small pattern from the prior two districts. Save bumped to v13 with the same `introducedVersion`-based validation already built for v11/v12; a v12 (forge+bridge+restaurant) save migrating to v13 keeps all three prior states and gets the guild hall as a fresh 'surveyable' entry; a pre-kingdom v1-v9 save still gets all four auto-completed via `unlockedGame()` (which already granted `guild` unconditionally, so this is a no-op behavior change for legacy saves).
- Verification: `npm test` 122/122 (119 prior + 3 new: v12→v13 migration keeps forge+bridge+restaurant/defaults guild hall unstarted, v13 project-set validation, full guild-hall completion grants `featureUnlocked(s,'guild')` + restoration score 4), `npm run build` PASS. `npm run simulate` failed to resolve `tsx` via `npm run` in this specific worktree (a `node_modules/.bin` PATH-resolution quirk, not a code issue) — re-ran successfully via `npx tsx scripts/progression-report.ts` instead, same output, content-independent. Browser QA against a dev server pointed explicitly at this worktree: fresh save shows all four districts surveyable simultaneously; clicked the guild hall's own survey action and confirmed the correct salvage text ("나무 6개 · 돌 4개") and phase transition to 폐허 정리, no console errors. Did not run the guild hall's own full 60s+80s clear/restore timers live — same `advanceProjectAction` code path already exercised live for the forge (across three prior reviews) and now covered by 3 dedicated unit tests; judged proportionate, consistent with prior districts.
- Fixed during this task (test accuracy, not a product defect — third recurrence of the same class of bug, see DECISIONS.md D022): three existing lower-version migration tests (v10-only, v11 "무너진 식당 도입 전", v11 "대장간+다리 시절") built their `raw` fixture via `encodeSave(s)` at the ambient current version (now 13, so `raw.projects` contains all 4 project keys) and only deleted the *previously*-new project keys before down-casting `raw.version`, never the brand-new `guild_hall` key. Left as-is, the final "downgraded save decodes successfully" assertions in two of these tests would have failed for an unrelated reason (an unexpected `guild_hall` key present at a version where it shouldn't exist yet). Added `delete raw.projects.guild_hall` to all three.
- Known risks/limits (disclosed, unchanged from D020/D021, restated in D022): no hard prerequisite/lock system between districts — order only softly enforced via material dependency. New saves have no progression path past forge+bridge+restaurant+guild_hall until further districts are built. All new numbers are unplaytested. The reverse-direction save-validation check remains deliberately deferred — farming/ranching/magic still have no project granting them.
- Review/merge: no independent review exists yet for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
- Exact next action: commit this checkpoint (content/save changes + doc updates), push to `origin/feature/kingdom-guild-hall`, verify remote SHA equality. Then either continue to the next district (버려진 밭/농사, or reorder per user preference) on its own feature branch, or perform the independent review + merge if the user explicitly requests it. When starting the next district, remember D022's caution: check every lower-version migration test for a missing `delete raw.projects.<new_id>` before trusting a "downgraded save still decodes" assertion.
