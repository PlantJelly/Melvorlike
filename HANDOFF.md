# Current handoff

- Current goal: implement the nine approved, currently representable guild milestone quests as one-time onboarding rewards without introducing guild experience or the unapproved collection/settler system.
- Branch: `feature/guild-milestones`, created cleanly from verified `origin/main` `76cfdb8b172e7b3fe8f99054184a7314df537cb6`. The remote feature ref was verified absent before creation.
- Checkpoint type: Stable candidate for atomic task 1. Last verified Stable main integration is `76cfdb8`.
- Combined implementation: the prior save v8 daily quests and shared progression formulas remain intact. Atomic task 1 advances the schema to v9 with milestone claims and successful-exchange history while deriving all other readiness from existing permanent progress.
- Milestone architecture: nine approved onboarding goals live in pure guild content; the engine owns readiness and one-time reward mutation. The unapproved collection/settler goal is excluded, and no guild experience or new currency is introduced.
- Verification: exact feature SHA and fast-forwarded main both passed `npm test` 99/99, `npm run build`, and `npm run simulate`; the feature also passed `git diff --check`. Browser QA showed three quests, bought 10 copper for 150G, delivered the 6-copper quest, verified gold 850→886 and inventory 10→4, then confirmed the disabled completion state and values survived reload; browser warning/error logs were empty. GitHub had no configured Actions runs, required checks, branch protection, or rulesets.
- Review corrections: exact-HEAD review found two vacuous assertions in `guild-quest.test.ts`: the migration case inspected only the available pool, and the date-boundary case could skip every assertion behind a conditional. The tests now require an actually generated high-level resource and a deterministically changed quest slot. A subsequent documentation pass found that D015 still described the pre-review migration/validation behavior; it now matches save v8's implemented invariants. Production code was already correct.
- Implemented: nine milestone definitions; readiness derived from permanent progress; one-time gold claims; successful-exchange tracking; save v9 migration/validation; six focused regression tests; all older version expectations updated to v9.
- Verification: `npm test` 105/105 PASS, `npm run build` PASS, and `git diff --check` PASS on the complete atomic-task-1 state.
- Incomplete: Guild milestone UI/action connection, implementation/README updates, and targeted browser QA. Onboarding skill unlocks, multi-plot/capacity systems, achievements, and other items in PROGRESS remain future work.
- Exact next action: checkpoint and push atomic task 1 after fresh remote verification, then connect the reviewed engine API to the Guild screen as atomic task 2.
- Known risks: quest amount/reward balance and all progression estimates are provisional. Past-day quest identity still depends on ResourceDB ordering. The simulator's fastest-resource policy ignores material availability unless using the separate production-route report.
