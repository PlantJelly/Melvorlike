# Current handoff

- Current goal: implement the nine approved, currently representable guild milestone quests as one-time onboarding rewards without introducing guild experience or the unapproved collection/settler system.
- Branch: `feature/guild-milestones`, created cleanly from verified `origin/main` `76cfdb8b172e7b3fe8f99054184a7314df537cb6`. The remote feature ref was verified absent before creation.
- Checkpoint type: Stable. Atomic task 1 is remotely verified at `b265677`; the completed feature is remotely verified at `dafe8cd`; last verified Stable main integration is `76cfdb8`.
- Combined implementation: the prior save v8 daily quests and shared progression formulas remain intact. Atomic task 1 advances the schema to v9 with milestone claims and successful-exchange history while deriving all other readiness from existing permanent progress.
- Milestone architecture: nine approved onboarding goals live in pure guild content; the engine owns readiness and one-time reward mutation. The unapproved collection/settler goal is excluded, and no guild experience or new currency is introduced.
- Implemented: nine milestone definitions; readiness derived from permanent progress; one-time gold claims; successful-exchange tracking; save v9 migration/validation; six focused regression tests; Guild status/claim UI and save action; README and implementation status updates.
- Verification: complete feature state passes `npm test` 105/105, `npm run build`, `npm run simulate`, and `git diff --check`. Browser QA verified all nine initial states, first-gather readiness, exact 100G reward (1,000→1,100), disabled completed state, reload persistence, and no warning/error logs.
- Review status: exact-HEAD merge review is in progress under the user's explicit review-and-merge request. The first pass found stale completion wording in this recovery snapshot and `docs/implementation_status.md`; code findings remain empty so far. Any resulting documentation commit requires the full review and verification gates to restart on its new SHA.
- Incomplete: finish the exact-SHA review, verify GitHub checks/policy and the conflict preview, then fast-forward and push `main` only if every gate passes. Onboarding skill unlocks, multi-plot/capacity systems, achievements, and other items in PROGRESS remain future work.
- Exact next action: checkpoint and push the corrected documentation, then review the new feature HEAD against freshly fetched `origin/main` before integration.
- Known risks: quest amount/reward balance and all progression estimates are provisional. Past-day quest identity still depends on ResourceDB ordering. The simulator's fastest-resource policy ignores material availability unless using the separate production-route report.
