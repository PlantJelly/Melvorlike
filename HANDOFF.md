# Current handoff

- Current goal: farming minimal loop (seed purchase → plant → grow → harvest) from PROGRESS's Planned item 1, plus the fixes for the findings raised by the review of `e1cb7ba`. Re-review runs against this snapshot.
- Feature branch: `feature/farming-loop`, created from `main` after merging `feature/development-workflow` (commit `a4522c97f8748b2e0209b7a2f39a9d278fed11ca`). Configured remote: `origin` (`https://github.com/PlantJelly/Melvorlike.git`).
- Checkpoint type: Stable — the atomic task (farming minimal loop) is functionally complete, `npm test` and `npm run build` pass, and a browser walkthrough verified the full cycle. Commit/push verification follows this snapshot; do not infer remote success from prose.
- Known pre-checkpoint parent: `a4522c97f8748b2e0209b7a2f39a9d278fed11ca` (main, post workflow-doc merge). This is the parent, not the future snapshot SHA.
- Snapshot commit: resolve with `git log -1 -- HANDOFF.md` and verify ancestry from the parent above.
- Last verified Stable checkpoint before this one: `07b6f9a8bc0012d9d83d7878930a833c32f1dc81` (workflow adoption), then `a4522c9` merged workflow docs into `main` (user-requested, docs-only, tests/build reverified).
- Completed this session: `a4522c9` merged `feature/development-workflow` into `main` and pushed (verified remote SHA equality). Then on `feature/farming-loop`: added `farming` as a full `SkillId` (reuses the existing skill/level/tool-tier engine rather than a bespoke system), 3 crops (wheat Lv1, potato Lv10, carrot Lv25) in `ResourceDB`, a single background farm plot (`Model.farmPlot`) that advances every tick independent of the one active-action slot and caps at grow duration until harvested, `buyCrop`/`plant`/`harvest` in `src/engine/model.ts`, save schema v4 with v1/v2/v3 migration in `src/engine/save.ts`, `src/ui/FarmingView.tsx` (detail page + global `FarmStatus` strip), and wiring in `src/App.tsx`.
- Review round 1 (`e1cb7ba`) found 5 issues, all fixed in `e1912b8`: (1) the "N분 후 수확 가능" estimate now divides by speed via `farmRemainingMs()`; (2) `begin()` rejects `skill:'farming'` ids; (3) harvest readiness centralised in `farmReady()` with the `1e-7` tolerance `advanceSegment` uses; (4) `plot`/`crop`/`ready` accessors deduplicated to module scope in `FarmingView.tsx`; (5) content-reference test asserts every `skill:'farming'` row has a `cropYield` entry. Review round 2 (`e1912b8`) PASSed with one non-blocking hardening note: `decodeSave`'s `currentAction` validation didn't reject `skill:'farming'` ids the way `begin()` and the `farmPlot` validation do. Fixed in this snapshot with a matching regression test.
- In progress at snapshot: none — implementation and both rounds of review fixes are done; this checkpoint is being recorded, and re-review (round 3) follows against the resulting commit.
- Incomplete: independent review and merge to `main` (not yet requested); ranching, guild, enchanting/accessories and the rest of PROGRESS's Planned list; farming extensions (multiple plots, fertilizers, auto-plant/harvest) noted as future work in `docs/implementation_status.md`.
- Tests: `npm test` — 36/36 PASS (25 prior + 11 farming tests in `src/engine/farm.test.ts`; one existing assertion in `food.test.ts` updated from `version 3` to `version 4` for the schema bump, and its content-reference test extended to cover `cropYield`). `npm run build` (`tsc -b && vite build`) PASS. Both run on the exact code being checkpointed, 2026-09-11 ~19:50 KST.
- Browser verification (2026-09-11, local `npm run dev` on :5173): bought 1 wheat seed (gold 1000→990) → planted (global farm status showed live progress) → switched to the Logging tab and confirmed the plot kept advancing in the background → reloaded the page and confirmed progress persisted/advanced further (offline-style elapsed accounting) → fast-forwarded `lastSaveTime` via devtools to reach "ready" → harvested (got 3 wheat, +30 farming exp, notice shown, plot cleared) → confirmed the 3 wheat appear and are sellable in Inventory → confirmed a "농사 · 괭이" tool-upgrade card appears automatically on the Tools page (reuses the existing tool-tier system with zero special-casing).
- Browser re-verification of the fix (2026-09-11): with a 돌 괭이 (+15%) equipped and the plot at 56%, the panel shows "1.1분 후 수확 가능" — the speed-adjusted value (the pre-fix code would have shown 1.3분) and consistent with the crop card's "성장 2.6분".
- Checks not run: no lint script or tracked GitHub Actions workflow exists.
- Failed tests/core error: none. One pre-existing test assertion needed updating (see Tests line above) — expected consequence of the version bump, not a defect.
- Known issues carried over: multi-tab save contention unsupported; backup/recovery UI absent; balance provisional (farming grow times/yields/prices are a first playtestable draft, not validated). Farming-specific: single plot only, no fertilizers, no auto-plant/harvest, seed and harvested produce are the same item (no separate seed item), crop items don't yet feed any cooking recipe.
- Confirmed causes: n/a (feature addition, not a bug fix).
- Uncertainty: exact farming balance (grow time, yield, price) is untested against real play sessions; remote branch protection/required-check policy on `origin` remains unverified.
- Exact first action next session: read AGENTS → HANDOFF → PROGRESS → DECISIONS → README, inspect branch/status/HEAD, fetch origin and compare `feature/farming-loop` and `main` against this snapshot. If the snapshot commit is pushed and the tree is clean, this checkpoint is complete. If the user wants this reviewed or merged, that requires an explicit `/review` or `/merge` request — do not infer it from completion. Otherwise continue with PROGRESS's Planned item 2 (ranching minimal loop) on a new `feature/<topic>` branch.
- Prerequisites: repository-root PowerShell (Node/npm are not on the default Bash PATH in this environment — prefix commands with `$env:Path += ";C:\Program Files\nodejs"` in PowerShell, or otherwise ensure Node is resolvable), existing Git authentication. No new global config, hooks, CI or background services were added.
- Remote preservation: after commit, push explicitly to `origin feature/farming-loop`; compare local HEAD to a fresh `git ls-remote --heads origin refs/heads/feature/farming-loop`. On resume, repeat this verification. This document cannot attest its own future push.
- Review evidence: none yet. Future local attestation goes to the path returned by `git rev-parse --git-path development-workflow-review.md`; re-review if no trusted matching report survives.
