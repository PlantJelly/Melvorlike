# Current handoff

- Current goal: implement the kingdom-restoration foundation and the first ruined-forge vertical slice agreed with the user.
- Branch: `feature/kingdom-restoration-foundation`, created from freshly verified `origin/main` `13e5317ec82354aff837e643a5a9e228e6bab414` on 2026-09-15 KST. The remote feature ref was absent at creation.
- Checkpoint type: Stable UI candidate. Last remotely verified Stable checkpoint is engine/save commit `fbe89f0f40a51c2b78f315152eca9b42819bba98`; this snapshot's known pre-checkpoint parent is `fbe89f0`.
- Implemented: the full first ruined-forge slice from project data and save v10 through the kingdom default screen. The UI has 폐허 회수/복원/완료 lanes, survey/clear/partial delivery/restore controls, global project progress in the header, restoration score, and sidebar skill/facility gates. New games show logging/mining only; finishing the forge reveals blacksmithing and tools. Existing saves retain all prior access without duplicate salvage.
- Verification: exact UI/docs candidate passed `npm test` 113/113, `npm run build`, `npm run simulate` (approved unsandboxed execution because sandbox Node previously failed at `uv_os_get_passwd` ENOMEM), and `git diff --check` on 2026-09-15. Browser QA used a fresh localhost origin and completed survey, interrupted/resumed clearing, one-time salvage, partial delivery, missing-material gathering, restoration, unlock, new-tab persistence, and 390×844 responsive checks; no browser warnings/errors.
- Exact next action: inspect and checkpoint the UI/docs slice, push and verify remote equality, then perform an independent full-feature review in a later authorized continuation.
- Acceptance criteria: see `docs/implementation_plan.md` and the current section in PROGRESS. Main points are one global active slot, deterministic one-time salvage, partial delivery, permanent blacksmithing unlock, exact interruption/offline behavior, and lossless v1-v9 migration.
- Known risks: new saves stop after the forge slice because later unlock projects are intentionally not implemented yet. Existing-user migration and engine entry-point locks are covered by tests; this UI QA used a fresh save, while old-save migration behavior remains executable-test evidence rather than a separate browser fixture.
- Review/merge: no review exists for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
