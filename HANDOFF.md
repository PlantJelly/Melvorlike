# Current handoff

- Current goal: implement the kingdom-restoration foundation and the first ruined-forge vertical slice agreed with the user.
- Branch: `feature/kingdom-restoration-foundation`, created from freshly verified `origin/main` `13e5317ec82354aff837e643a5a9e228e6bab414` on 2026-09-15 KST. The remote feature ref was absent at creation.
- Checkpoint type: Stable engine/save candidate. Last remotely verified Stable checkpoint is documentation commit `b8ea1d69aee771d1760bd64ab4762a47ccb165c0`; this snapshot's known pre-checkpoint parent is `b8ea1d6`.
- Implemented: `src/content/projects.ts` and the ruined-forge data; project survey/clearing/deterministic salvage/partial delivery/restoration/unlock engine; production/project `currentAction` union; skill and facility gates; kingdom restoration score; save v10 validation and v1-v9 lossless migration; 8 focused project/migration tests. `initial()` now creates the real locked new-game state and `unlockedGame()` provides the explicit full-system test/simulator fixture.
- Verification: exact working state passed `npm test` 113/113, `npm run build`, `npm run simulate` (first sandbox attempt failed at Node `uv_os_get_passwd` ENOMEM; approved unsandboxed retry passed), and `git diff --check`. Browser QA is deferred to the next UI checkpoint because no kingdom screen exists yet.
- Exact next action: inspect and checkpoint this engine/save slice, then add project actions to the state adapter and build the kingdom default screen, unlocked sidebar, project progress/material controls, and targeted browser QA.
- Acceptance criteria: see `docs/implementation_plan.md` and the current section in PROGRESS. Main points are one global active slot, deterministic one-time salvage, partial delivery, permanent blacksmithing unlock, exact interruption/offline behavior, and lossless v1-v9 migration.
- Known risks: the current sidebar still renders locked skills and has no kingdom page, so a new player sees inaccessible buttons until the next UI task. New saves stop after the forge slice because later unlock projects are intentionally not implemented yet. Existing-user migration and engine entry-point locks are covered by tests, but browser migration will be checked with the UI.
- Review/merge: no review exists for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
