# Current handoff

- Current goal: the deterministic progression simulator now includes both skill-level curves and recursive recipe/tool production routes on `feature/progression-simulator`.
- Branch: `feature/progression-simulator`, created from freshly verified `origin/main` at `9294915d89a8ac4c5e71f2a56a651060c7163e63`. The remote feature ref was absent when the branch was created.
- Checkpoint type: Stable candidate for the second atomic task. Resolve the commit containing this snapshot with `git log -1 -- HANDOFF.md`.
- Last verified Stable checkpoint: `d18cd4b` on this branch, pushed and verified equal to `origin/feature/progression-simulator`. The independent guild-daily-quests branch is also preserved at verified checkpoint `3ad2589`; neither branch is merged.
- Completed: `src/engine/formulas.ts` owns the shared Lv1~99 experience formula and speed-bonus composition used by the game engine and simulator. `src/engine/progression.ts` calculates resource rates, simulates the fastest unlocked resource at each level under four scenarios, and recursively expands target items/tool costs into raw requirements, processing actions, produced units, per-skill time, and single-active-slot total. `scripts/progression-report.ts` renders all 8 skills × Lv10/30/50/99, every resource's hourly rate, and four onboarding production routes through `npm run simulate`. Farming output reflects the actual three-item harvest.
- Scope and limitation: level curves still treat crafting inputs, feed, seed cost, and tool crafting time as immediately available. Production routes include recipe inputs and tool material costs but do not yet combine the level grind or gold purchases needed to unlock them. No game balance, save schema, or player UI changed.
- Result: the strongest included scenario puts active-skill Lv99 around 9.4–17.6 days, but farming at about 1,111 days and ranching at about 3,733 days. Active Lv10 is only 2.0–3.3 minutes. This supports adjusting passive progression after multi-plot/capacity design and using action milestones for onboarding rather than changing the global experience curve first. See `docs/progression_analysis.md`.
- Verification: `npm test` 91/91 PASS, `npm run build` PASS, `npm run simulate` PASS, all on the complete pre-checkpoint code state. The CLI required the ordinary host execution environment because the command sandbox's Windows user-info lookup returned ENOMEM; the same command completed normally outside that sandbox. No lint script or tracked CI exists.
- Incomplete: production routes do not include level-up time, starting inventory/gold, guild purchases, or passive scheduling. Target milestone comparison and automatic balance recommendations are not implemented. The separate guild daily quest branch remains unmerged.
- Exact next action: checkpoint and push the production-route extension after fresh remote checks. Before changing balance or starting onboarding, decide integration order for the independently preserved guild-daily-quests and progression-simulator branches; main integration remains explicitly user-authorized only.
- Known risks: the "fastest resource" policy optimizes skill XP/hour only and can choose recipes that are impractical once input costs are counted. Tool scenarios assume immediate ownership at the required level. Static analysis text is dated; rerun `npm run simulate` after content changes.
