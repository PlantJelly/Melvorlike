# Current handoff

- Current goal: the first deterministic progression simulator is implemented on `feature/progression-simulator`; this snapshot prepares its Stable checkpoint.
- Branch: `feature/progression-simulator`, created from freshly verified `origin/main` at `9294915d89a8ac4c5e71f2a56a651060c7163e63`. The remote feature ref was absent when the branch was created.
- Checkpoint type: Stable candidate. Resolve the commit containing this snapshot with `git log -1 -- HANDOFF.md`; the pre-checkpoint parent is `9294915`.
- Last verified Stable checkpoint: `3ad2589` on `feature/guild-daily-quests`, pushed and verified equal to its remote branch. It is not merged and remains independent from this branch.
- Completed: `src/engine/formulas.ts` now owns the shared Lv1~99 experience formula and speed-bonus composition used by the game engine and simulator. `src/engine/progression.ts` calculates resource rates and simulates the fastest unlocked resource at each level under four configurable tool/food/experience scenarios. `scripts/progression-report.ts` renders all 8 skills × Lv10/30/50/99 plus every resource's hourly rate as Markdown through `npm run simulate`. Farming output reflects the actual three-item harvest.
- Scope and limitation: crafting inputs, feed, seed cost, and tool crafting time are treated as immediately available. The report measures each skill's theoretical growth rate, not total account completion time. No game balance, save schema, or player UI changed.
- Result: the strongest included scenario puts active-skill Lv99 around 9.4–17.6 days, but farming at about 1,111 days and ranching at about 3,733 days. Active Lv10 is only 2.0–3.3 minutes. This supports adjusting passive progression after multi-plot/capacity design and using action milestones for onboarding rather than changing the global experience curve first. See `docs/progression_analysis.md`.
- Verification: `npm test` 87/87 PASS, `npm run build` PASS, `npm run simulate` PASS, all on the complete pre-checkpoint code state. The CLI required the ordinary host execution environment because the command sandbox's Windows user-info lookup returned ENOMEM; the same command completed normally outside that sandbox. No lint script or tracked CI exists.
- Incomplete: resource acquisition and recipe-input time are not simulated. Target milestone comparison and automatic balance recommendations are not implemented. The separate guild daily quest branch remains unmerged.
- Exact next action: inspect and checkpoint only the simulator files, refresh `origin/main` and the absent/present feature ref immediately before push, push `feature/progression-simulator`, and verify remote SHA equality. Afterward, extend the simulator with end-to-end resource-route costs as a separate atomic task before changing balance.
- Known risks: the "fastest resource" policy optimizes skill XP/hour only and can choose recipes that are impractical once input costs are counted. Tool scenarios assume immediate ownership at the required level. Static analysis text is dated; rerun `npm run simulate` after content changes.
