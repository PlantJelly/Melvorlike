# Current handoff

- Current goal: integrate the reviewed progression simulator and guild daily quests into `main`, then continue from a clean combined baseline.
- Branch: `feature/guild-daily-quests`, reconciling verified guild checkpoint `3ad2589f975fe3e44f8d4d3947de86951dfb272a` with `origin/main` at `12465d35c7c8d05f19394781f9fa32425b68ec0d` after the progression simulator was fast-forwarded to main.
- Checkpoint type: Stable candidate for the reconciliation checkpoint. Last verified Stable guild checkpoint is `3ad2589`; last verified main integration is `12465d3`.
- Combined implementation: save v8 daily quests remain intact while the model now also uses the shared level and speed formulas from the progression simulator. `npm run simulate` reports all skill checkpoints, resource rates, and recursive early production routes. Guild daily quests generate three deterministic unlocked-resource deliveries per UTC day, validate v8 saves, consume inventory, and pay the shared sale-bonus-aware reward.
- Conflict resolution: `src/engine/model.ts` keeps save v8 and daily quest initialization while replacing the literal initial XP threshold with `experienceToNextLevel(1)`. HANDOFF, PROGRESS, and implementation status are being rewritten around the combined state instead of choosing one branch's stale snapshot.
- Verification: `npm test` 99/99 PASS, `npm run build` PASS, `npm run simulate` PASS, and `git diff --check` PASS on the complete pre-checkpoint state. Browser QA on port 5175 showed three quests, bought 10 copper for 150G, delivered the 6-copper quest, verified gold 850→886 and inventory 10→4, then confirmed the disabled completion state and values survived reload; browser warning/error logs were empty.
- Incomplete: the reconciliation merge must still be committed, pushed, and independently reviewed at its exact SHA against unchanged `main` before main integration. Milestone quests, onboarding skill unlocks, multi-plot/capacity systems, achievements, and other items in PROGRESS remain future work.
- Exact next action: checkpoint and push the reconciled feature branch after fresh remote checks, then review that exact SHA against the unchanged main target and merge only on PASS.
- Known risks: quest amount/reward balance and all progression estimates are provisional. Past-day quest identity still depends on ResourceDB ordering. The simulator's fastest-resource policy ignores material availability unless using the separate production-route report.
