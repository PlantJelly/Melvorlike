# Current handoff

- Current goal: implement the kingdom-restoration fifth district, "버려진 밭" (abandoned field), permanently unlocking the `farming` skill, following the plan order in `docs/implementation_plan.md` §4 (after 먼지 쌓인 길드 회관).
- Branch: `feature/kingdom-abandoned-field`, created from freshly verified `origin/main` `b773d22bf8f387c57dcc4c997a978e9822d7414a` on 2026-09-16 KST. The remote feature ref was absent at creation.
- Checkpoint type: Stable. This is the first district where the material-dependency chain (D020/D021/D022) breaks: the immediately preceding district (guild hall) unlocks a service feature (`guild`) with no producible good, so there's nothing to soft-gate on. Fell back to plain wood/stone materials, same as the very first district (forge). `src/engine/model.ts` and `src/ui/KingdomView.tsx` needed zero changes again.
- Implemented: `abandoned_field` project (survey → 70s clearing, salvage wood 7/stone 5 → deliver wood 16/stone 12 → 90s restoration → permanent `farming` unlock, restoration score +1 = 5 total). All five districts surveyable from a fresh save with no hard prerequisite. Save bumped to v14 with the same `introducedVersion`-based validation already built for v11/v12/v13; a v13 (forge+bridge+restaurant+guild_hall) save migrating to v14 keeps all four prior states and gets the field as a fresh 'surveyable' entry; a pre-kingdom v1-v9 save still gets all five auto-completed via `unlockedGame()`.
- Verification: `npm test` 125/125 (122 prior + 3 new: v13→v14 migration keeps forge+bridge+restaurant+guild_hall/defaults field unstarted, v14 project-set validation, full field completion grants `skillUnlocked(s,'farming')` + restoration score 5), `npm run build` PASS, `npm run simulate` PASS via `npx tsx` (same `node_modules/.bin` PATH quirk as the guild-hall checkpoint, unrelated to this diff). Browser QA against a dev server pointed explicitly at this worktree: fresh save shows all five districts surveyable simultaneously; clicked the field's own survey action and confirmed the correct salvage text ("나무 7개 · 돌 5개") and phase transition to 폐허 정리, no console errors. Did not run the field's own full 70s+90s clear/restore timers live — same `advanceProjectAction` code path already exercised live for the forge (across four prior reviews) and now covered by 3 dedicated unit tests; judged proportionate, consistent with prior districts.
- Applied proactively (not self-caught this time, unlike the prior two districts — see DECISIONS.md D023): D022's caution to check every lower-version migration test for a missing `delete raw.projects.<new_id>` was applied *before* running tests, by adding `delete raw.projects.abandoned_field` to all five existing lower-version fixtures (v10-only, v11 "무너진 식당 도입 전", v11 "대장간+다리 시절", v12, v13) up front. Tests passed on first run without needing a follow-up fix.
- Known risks/limits (disclosed, unchanged from D020/D021/D022, restated in D023): no hard prerequisite/lock system between districts — order only softly enforced via material dependency, and that chain has no natural continuation past a service-feature-only district. New saves have no progression path past these five districts until further districts are built. All new numbers are unplaytested. The reverse-direction save-validation check remains deliberately deferred — ranching/magic still have no project granting them.
- Review/merge: no independent review exists yet for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
- Exact next action: commit this checkpoint (content/save changes + doc updates), push to `origin/feature/kingdom-abandoned-field`, verify remote SHA equality. Then either continue to the next district (낡은 축사/목장 or 쓰러진 마법탑/마법·장신구, order undecided) on its own feature branch, or perform the independent review + merge if the user explicitly requests it. When starting the next district, consider whether ranching's or magic's actual outputs (eggs/wool/milk, or enchant stones) can restart the material-dependency chain that broke at guild_hall.
