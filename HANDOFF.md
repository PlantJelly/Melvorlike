# Current handoff

- Current goal: implemented the first of the four user-requested new active skills — 채집(foraging) — as the eighth kingdom district ("무성해진 숲길"/"Overgrown Trail"), per the user's explicit instruction "1번 개발 진행해줘" selecting option 1 (신규 액티브 스킬 4종: 야외 채집/목공/조제/재봉) from the two directions offered after the reverse-unlock-validation merge.
- Branch: `feature/kingdom-overgrown-trail`, created from freshly verified `origin/main` `fffdcc748080183fa46fa81663a8fb136cb092da` on 2026-09-16 KST. The remote feature ref is absent (not yet pushed).
- Checkpoint type: not yet checkpointed in this turn — code, tests and docs are complete and verified locally, but no commit/push has been made yet. This HANDOFF captures the state immediately before that checkpoint.
- What was added: `overgrown_trail` project (`introducedVersion: 17`, `unlockSkills: ['foraging']`, materials wood 22 + `enchant_stone_stone` 6 — the immediately-preceding district's (fallen_tower/magic) real output, continuing D024/D025's material-dependency-chain principle). New `SkillId` `'foraging'` with 4 tiers of pure-gather resources (산딸기/들버섯/산약초/영지버섯, numbers copied 1:1 from logging's wood/oak/hardwood/magic_wood tiers). `Model.version` 16→17. `save.ts`'s skill/tool restoration loops gained `if (version < 17 && id === 'foraging') continue;` (same pattern as magic's `< 7` guard). **Zero changes to `model.ts`/`App.tsx`/`KingdomView.tsx`/`ProductionView.tsx`/`ToolsView.tsx`** — the generic `playable`/`SkillId`-driven architecture absorbed the new skill exactly like the prior 7 rounds.
- Test/doc-fixture fallout found and fixed this round (two new bug classes beyond the established D022 checklist):
  1. The pre-existing "v16 저장은 일곱 프로젝트를..." boundary test in `kingdom.test.ts` relied on `encodeSave`'s *implicit* current version rather than pinning `raw.version` explicitly — harmless while ambient version happened to be 16, but silently wrong once it became 17 (test name says v16, would have actually exercised v17 data). Fixed by adding an explicit `raw.version = 16;`. **Any new boundary test must always pin the version it claims to test.**
  2. `guild-quest.test.ts`'s deterministic day-0-seed daily-quest test asserted a specific outcome ("a high-level item gets picked") that broke once the new level-1 `wild_berry` resource widened the candidate pool for that seed. Fixed by replacing the brittle assertion with a `.not.toEqual` comparison against what the pool would have produced at level 1 (mining unrestored) — proves the same intent (migration used the *restored* skill level) without depending on which specific resource wins the deterministic draw.
- Verification: `npm test` 135/135 PASS, `npm run build` PASS, `npm run simulate` PASS (via `npx tsx`, confirms foraging's 4 tiers report identical per-hour efficiency to logging's 4 tiers). Browser QA: direct `localStorage` injection was found unreliable (gameLoop.ts's `pagehide` autosave flush overwrites it with the stale in-memory state right before a reload completes) — worked around by calling `gs.mutate()` on the live app's state to complete all 7 prior districts, then drove `overgrown_trail` through survey→clear→deliver→restore via the real engine functions. Confirmed: "무성해진 숲길 복원 완료 · 채집 해금" notice, restoration count 8, new "채집" sidebar entry, and the 4-tier ProductionView rendering with correct level gating. Actual timed production tick (clicking "채집 시작" and watching inventory increment) could not be re-confirmed on screen due to background-tab timer throttling in this session's browser pane, but the underlying `advanceSegment` logic is unchanged, shared code already exercised by all 7 other active skills — low regression risk.
- Docs updated this round: DECISIONS.md (D027), PROGRESS.md (new "신규 액티브 스킬 1/4" section + roadmap item 8 + Planned/Milestones lines), README.md (district list + save-version line, v16→v17), docs/implementation_status.md (title, new bullet, magic-supply bullet, checksum/version lines, migration chain line, 미구현/검증 sections), docs/implementation_plan.md (new §5 for the 4 new skills, roadmap closing note).
- Known risks/limits: none new beyond the two test-fixture fallout classes above (both fixed and now documented as forward cautions in DECISIONS.md D027). This is purely additive content + one save-schema version bump; no engine logic changed.
- Review/merge: no independent review exists yet for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
- Exact next action: commit this checkpoint (all listed source/test/doc changes), push to `origin/feature/kingdom-overgrown-trail`, verify remote SHA equality. Then either continue to 목공(woodworking) as the next atomic task, or perform the independent review + merge, per explicit user instruction — do not assume which.
