# Current handoff

- Current goal: guild guide / low-tier material purchase-exchange (PROGRESS Planned item 4), implemented and browser-verified. Independent review has not run yet on this branch.
- Feature branch: `feature/guild-exchange`, created from fresh `main` (`7224cc3`, which already includes the merged farming/ranching/cooking loops). Configured remote: `origin` (`https://github.com/PlantJelly/Melvorlike.git`).
- Checkpoint type: WIP — implementation complete, tests/build pass, browser-verified, but not yet reviewed, not yet committed/pushed as of this snapshot being written. Review and commit follow immediately after this snapshot.
- Known pre-checkpoint parent: `7224cc3d6c59dfab0b8790c7e74952b9b773959c` (main, post cooking-loop merge + docs).
- Completed this session: new `src/content/guild.ts` (`ExchangeDB` — higher-tier raw material → its skill's Lv1 material, `tierGap`; `guildTiers` — 3-tier gold-only upgrade path mirroring `toolTiers`). New `Model.guild: number` (tier index). `buyResource` (generalizes farming's `buyCrop` — any raw material, i.e. any `ResourceDB` entry with no `recipe`, gated by the buyer's own skill `reqLevel` — a check `buyCrop` never had), `exchangeResource` (exchange N of a higher-tier item for `floor(N * 1.5^tierGap)` of the Lv1 baseline, gated by `tierGap <= guild + 1`), `upgradeGuild` (gold-only tier upgrade). New `src/ui/GuildView.tsx` wired into `App.tsx`'s "작업실" nav group (no per-page status strip needed — unlike farming/ranching, guild has no background timer). Save schema v6 with v1–v5 migration. Deliberately scoped OUT: the daily/milestone quest system that game_design.md's guild section also describes — guild leveling here is a flat gold "후원" purchase instead (see DECISIONS D010 for the full scoping rationale).
- In progress at snapshot: independent review has not run on this branch yet. Next action is `/review`, fix any findings, re-review to PASS, then (only on explicit user request) `/merge` into `main` — same sequence as the prior three merges.
- Incomplete: guild quests, enchanting/accessories (PROGRESS Planned item 5); arbitrary tier-pair exchange (currently everything converges to the skill's Lv1 material only), exp-boost food (달걀찜), farming/ranching extensions noted as future work in `docs/implementation_status.md`.
- Tests: `npm test` — 63/63 PASS (53 prior + 10 new tests in `src/engine/guild.test.ts`; farming's old buy-crop test was relocated there and adapted since `buyCrop` no longer exists as a separate function; three pre-existing `loaded.version` assertions across `farm.test.ts`/`ranch.test.ts`/`food.test.ts` bumped from 5 to 6 for the schema bump). `npm run build` PASS. Both run on the exact code being checkpointed.
- Browser verification (2026-09-11, local `npm run dev`, freshly-seeded save with 30,000 gold and oak/hardwood/magic_wood/iron in inventory): Guild page renders all 16 raw materials with correct locked/unlocked state and price → bought 10 wood (gold -50, inventory +10, confirming `buyResource` works for a non-farming item) → exchanged 4 oak → gained exactly 6 wood (`floor(4*1.5)`) → upgraded to 중급 길드 (gold -3,000, notice shown, page fully re-rendered with new tier name and depth) → previously-locked hardwood exchange (tierGap 2) became available and produced exactly 9 wood (`floor(4*2.25)`) → magic_wood (tierGap 3) correctly still shows "상급 길드 필요". Every computed quantity matched the hand-traced expected value exactly.
- Checks not run: no lint script or tracked GitHub Actions workflow exists.
- Failed tests/core error: none. One test-authoring mistake was caught and fixed while writing `guild.test.ts` (forgot to raise `s.gold` above the default 1000 before testing guild upgrades costing 3,000/15,000) — not an application bug.
- Known issues carried over: multi-tab save contention unsupported; backup/recovery UI absent; balance provisional and unplaytested as a full economy. Guild-specific: `buyResource` is a behavior change for farming crops too (added the `reqLevel` check `buyCrop` never had) — full test suite still passes but this wasn't purely additive, flagged in DECISIONS D010's caution. The anti-circular-trade property (exchanging then reselling always nets less than selling directly) holds for the *current* `ResourceDB` sell-price dataset and is regression-tested, but is not structurally enforced by the engine — a future rebalance of sell prices could silently break it if that test isn't kept green.
- Uncertainty: exact guild/exchange balance (tier-gap rate, upgrade gold costs) untested against real play sessions.
- Exact first action next session: read AGENTS → HANDOFF → PROGRESS → DECISIONS → README, inspect branch/status/HEAD, fetch origin and compare `feature/guild-exchange` and `main` against this snapshot. If a review PASS for the current exact HEAD isn't in evidence, run `/review` before anything else — do not merge on completion alone. If already reviewed and PASSed and the user wants it merged, that still requires an explicit `/merge` request.
- Prerequisites: repository-root PowerShell (Node/npm are not on the default Bash PATH in this environment — prefix commands with `$env:Path += ";C:\Program Files\nodejs"` in PowerShell), existing Git authentication. No new global config, hooks, CI or background services were added.
- Remote preservation: not yet pushed as of this snapshot. After commit, push explicitly to `origin feature/guild-exchange` and compare local HEAD to a fresh `git ls-remote --heads origin refs/heads/feature/guild-exchange`.
- Review evidence: none yet for this branch. Prior branches' review reports are gone (each round overwrote the last, and none were ever committed, per AGENTS.md's no-self-referencing-review-commit rule) — those PASSes are attested only by their merge commits (`510386f`, `a95abbe`, `355cafe`) and PROGRESS.md's summaries, not by a live file.
