# Current handoff

- Current goal: implement the kingdom-restoration second district, "부서진 다리" (broken bridge), unlocking fishing, following the plan order in `docs/implementation_plan.md` §4.
- Branch: `feature/kingdom-broken-bridge`, created from freshly verified `origin/main` `ec515bc3abecdf5d8d98d70ba8c42bebf49afd9c` on 2026-09-15 KST. The remote feature ref was absent at creation.
- Checkpoint type: Stable. This is a content-driven addition — `src/engine/model.ts`'s project state machine (`advanceProjectAction`, `surveyProject`, `startProjectWork`, `deliverProjectMaterial`, `kingdomRestoration`) is already generic over `ProjectDB`/`projectIds` and needed no changes; only `src/content/projects.ts` (new `broken_bridge` entry + `introducedVersion` field), `src/engine/save.ts` (version-gated project validation), and `src/ui/KingdomView.tsx`/`App.tsx` (generalizing text that was hardcoded to the forge) changed.
- Implemented: `broken_bridge` project (survey → 40s clearing, salvage wood 4/stone 2 → deliver wood 10/brick 6 → 60s restoration → permanent fishing unlock, restoration score +1 = 2 total). Both districts are surveyable from a fresh save with no hard prerequisite between them; brick (a blacksmithing product) in the bridge's delivery requirement naturally nudges players to finish the forge first. Save bumped to v11; `ProjectDef.introducedVersion` lets `decodeSave` compute the exact project-id set expected for a given save version, so a v10 (forge-only) save migrating to v11 keeps its forge state exactly and gets the bridge as a fresh, not-auto-completed 'surveyable' entry — while a pre-kingdom v1-v9 save (which predates all gating) still gets both projects auto-completed via the existing `unlockedGame()` base, consistent with the original "don't take away already-open access" rule. `KingdomView.tsx`'s completion/restoring-hint text, previously hardcoded to the forge (a non-blocking finding from the prior review), now derives from `unlockSkills`/`unlockFeatures` generically.
- Verification: `npm test` 116/116 (113 prior + 3 new: v10→v11 migration keeps forge/defaults bridge unstarted, v10/v11 project-set validation rejects wrong-version project keys, full bridge completion grants fishing + restoration score 2), `npm run build` PASS, `npm run simulate` PASS (unaffected inputs, ran anyway) on 2026-09-15. Browser QA against a dev server pointed explicitly at this worktree (not the shared root checkout, which sits on an unrelated stale branch): fresh save shows both districts surveyable simultaneously in the 폐허 회수 lane; played the forge through to completion live (survey → clear, interrupted by switching to logging mid-clear and resumed correctly → salvage once → partial then full delivery via gathering → 45s restoration) and confirmed the generalized completion card reads "폐허가 된 대장간 복원 완료 · 대장작업 · 도구 제작 이용 가능" (was hardcoded forge-only text before this branch) with blacksmithing appearing in the sidebar and fishing still absent; reloaded and confirmed restoration score 1 and forge-complete state persisted. No console warnings/errors at any point. Did not run the bridge's own full clear/restore timers live (60s+60s) — covered precisely by the new unit tests reusing the exact same `advanceProjectAction` code path already exercised live for the forge.
- Considered and reverted during this task: a reverse-direction save-validation check (every `unlockedSkills`/`unlockedFeatures` entry must be explained by a starter or a completed project) — correct in spirit, but conflicts with the established `unlockedGame()` test-helper convention (which pre-unlocks cooking/farming/ranching/magic for tests even though no project grants them yet, since those districts aren't built). Broke ~10 existing tests; reverted. Revisit once every skill has a real project-granted unlock path. Recorded in DECISIONS.md D020.
- Known risks/limits (disclosed): no hard prerequisite/lock system between districts yet — order is only softly enforced via material dependency (brick). New saves still have no progression path past forge+bridge (remaining districts unimplemented). All new numbers (bridge durations/materials/salvage) are unplaytested, consistent with the rest of the project.
- Review/merge: no independent review exists yet for this branch. Main integration has not been requested and is not authorized by this development continuation alone — this HANDOFF/PROGRESS/DECISIONS update and the following commit+push are the checkpoint; review and merge are separate future steps.
- Exact next action: commit this checkpoint (content/engine/save/UI changes + doc updates), push to `origin/feature/kingdom-broken-bridge`, verify remote SHA equality. Then either continue to the next district (무너진 식당/요리) on its own feature branch, or perform the independent review + merge if the user explicitly requests it.
