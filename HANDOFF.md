# Current handoff

- Current goal: completed the review and main integration of the nine approved, currently representable guild milestone quests without introducing guild experience or the unapproved collection/settler system.
- Branch: `main`. Feature branch `feature/guild-milestones` and `origin/main` were both verified at `48dbf68fdc0d5936f9f397b0d441a3c6e61b7962` after the fast-forward push on 2026-09-14 KST.
- Checkpoint type: Stable integration record. The known pre-checkpoint parent is the remotely verified integration `48dbf68`; locate the commit containing this snapshot with `git log -1 -- HANDOFF.md`.
- Implemented: nine milestone definitions; readiness derived from permanent progress; one-time gold claims; successful-exchange tracking; save v9 migration/validation; six focused regression tests; Guild status/claim UI and save action; README and implementation status updates. The unapproved collection/settler milestone remains excluded.
- Review: the first full-diff pass found stale completion wording in state documents and no code defects. The wording was corrected in `48dbf68`, then the exact feature HEAD was re-reviewed against `origin/main` `76cfdb8` with PASS. The review attestation is stored outside the worktree at the repository Git path `development-workflow-review.md`.
- Verification: exact feature and fast-forwarded main state passed `npm test` 105/105, `npm run build`, `npm run simulate`, and `git diff --check`. Browser QA on unchanged application code verified all nine initial states, first-gather readiness, exact 100G reward (1,000→1,100), disabled completed state, reload persistence, and no warning/error logs.
- CI/policy: no tracked GitHub Actions workflow, zero Actions runs and status contexts for reviewed SHA `48dbf68`, `main` protection disabled, required checks empty, and repository rulesets empty. The merge-tree conflict preview passed.
- Incomplete: no work remains for the requested milestone feature or merge. Onboarding skill unlocks, multi-plot/capacity systems, achievements, and other items in PROGRESS remain future work.
- Exact next action: select the next item from PROGRESS, then create or continue its own meaningful feature branch after the repository freshness checks.
- Known risks: milestone reward amounts and all progression estimates remain provisional. Migrated v1-v8 saves cannot infer exchanges completed before v9, so those players must perform one successful exchange to unlock that milestone. Past-day quest identity still depends on ResourceDB ordering.
