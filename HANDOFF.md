# Current handoff

- Current goal: implemented the first stage of user-requested "전체 생산망 밸런스 조정" (overall production-network balance pass): rescaled 농사(farming)/목장(ranching) — the two passive skills — `exp` and `sell` values so their time-to-level curve matches the already-consistent 10 active skills, without touching pacing (durations), consumption amounts, or the global exp formula.
- Branch: `feature/balance-passive-skills`, created from freshly verified `origin/main` `1ae75e2a4d89a37bc47507be074f234b28fd9375` on 2026-09-16 KST. The remote feature ref is absent (not yet pushed).
- Checkpoint type: not yet checkpointed in this turn — code, tests and docs are complete and verified locally, but no commit/push has been made yet. This HANDOFF captures the state immediately before that checkpoint.
- Root cause found: `docs/progression_analysis.md` (a 2026-09-14 baseline analysis, predating 4 of the current 12 skills) had already flagged that farming/ranching's Lv99 time (2,294 / 7,708 days) was wildly out of line with active skills (9-36 days at the time), but deliberately made **no numeric changes**, deferring the decision until "패시브 확장 방식과 목표 플레이 시간을 정한 뒤." Re-running `npm run simulate` fresh at the start of this round (now covering all 12 skills, including the 4 rounds added since that baseline) confirmed the 10 active skills already cluster tightly at 17.4–36.4 days (무도구 Lv99) — including the 4 newly-added skills, which land in that cluster automatically since they reuse existing active-skill numbers by design — while farming/ranching remained the sole, extreme outliers.
- What was changed: `src/content/resources.ts` — `exp`/`sell` fields for `wheat`/`potato`/`carrot`/`chamomile`/`mugwort`/`magic_mugwort`/`mystic_herb` (farming) and `egg`/`wool`/`milk` (ranching). Method: kept `baseDurationMs` (growth/husbandry cycle length — the intentional "long real-world wait, check back occasionally" passive-skill pacing) and the global `experienceToNextLevel` formula completely untouched, per `docs/progression_analysis.md`'s own recommendation #1. Computed per-reqLevel target exp/hour and sell/hour by averaging the 10 active skills' pure-gather (recipe-less) resources at each reqLevel tier (1/10/30/50), log-linearly interpolated for farming's/ranching's own reqLevels (1/10/15/25/30/40), then derived each resource's new `exp`/`sell` as `target_per_hour × (baseDurationMs / 3600000)`, rounded to clean numbers.
- Result: 무도구 Lv99 — farming 2,294.0일 → 26.5일, ranching 7,708.3일 → 34.3일, both now squarely inside the 10-active-skill cluster (17.4–36.4일). Lv10/30/50 checkpoints also land close to the active band, with one accepted exception: ranching's own Lv10 checkpoint (30.0분) stays well above the active cluster's ~3-5분, because ranching's *lowest* tier (egg) already has a 30-minute cycle — narrowing this further would require touching `baseDurationMs`, which this fix deliberately avoided, and the slower low-tier cadence is consistent with ranching's designed "long-cycle passive" feel.
- Explicitly out of scope for this round (documented, not silently dropped): (1) downstream recipes that consume farming/ranching outputs (cooking's 5 recipes, magic's 4 enchant stones, sewing's 4 recipes) now have crafted-good sell prices well below their raw-ingredient cost — judged acceptable since those items are consumed (buffs/rerolls), not resold, matching the existing pattern in other crafting skills (e.g. blacksmithing's ore→ingot markup direction, just not applied symmetrically to consumables); (2) overall gold economy (tool/guild/accessory costs vs. income rate) — not touched, no evidence it's broken, out of this round's evidenced scope; (3) multi-plot/multi-animal expansion — still unimplemented per `docs/progression_analysis.md`'s recommendation #2, so today's fix is scoped to the current 1-plot/1-per-species constraint; if that expansion is added later, this exp curve needs revisiting (flagged in both `docs/progression_analysis.md` and DECISIONS.md D032).
- Tests: 4 files had hardcoded pre-change expectations that broke and were fixed (not just numbers swapped — two required a real logic change since single-action exp now exceeds Lv1's `maxExp`, triggering multi-level-ups the old assertions never had to handle): `src/engine/guild.test.ts` (bumped a fixture's starting gold from 1,000 to 10,000 since wheat's buy price is now 375G × 5 = out of range for a 3-unit purchase at default gold, and swapped a hardcoded `2*5*3` for a live `ResourceDB.wheat.buy` reference), `src/engine/farm.test.ts` and `src/engine/ranch.test.ts` (added a `levelAfterExp()` test helper that replicates the engine's `addExperience` level-up cascade, since a single wheat harvest or egg cycle now jumps multiple levels instead of staying within Lv1's exp bar), `src/engine/progression.test.ts` (swapped a hardcoded gold figure for `ResourceDB.wheat.sell`-derived math). All other tests needed no changes — the majority already reference `ResourceDB` directly or are `Object.entries`-driven generic invariant tests.
- Verification: `npm test` 142/142 PASS (first run after the 4 fixture fixes, zero further defects), `npm run build` PASS, `npm run simulate` (via `npx tsx`) confirms the target Lv99/checkpoint numbers above. Browser QA on a live dev server: unlocked farming+ranching via `gameState.mutate()`, planted and harvested wheat via the real engine functions — confirmed UI (농사 screen) shows the new exp/sell/seed-price figures correctly and the notice/level/exp bar update correctly after a level-up cascade (Lv1→9 from one harvest); bought a chicken, fed it, advanced one 30-minute cycle — confirmed egg exp correctly cascades ranching to Lv25, and confirmed the actual inventory "1개 판매" button for egg increases gold by exactly 750 (the new sell price). No console errors.
- Docs updated this round: DECISIONS.md (D032), `docs/progression_analysis.md` (new "적용된 조정" section — this is the specific doc that had deferred this exact fix, now closes that loop), `docs/implementation_status.md` (임시 밸런스 bullet + 미구현 section narrowed to reflect this sub-item done, rest of "생산망 밸런스" still open), PROGRESS.md (new section). README.md needed no changes (doesn't cite specific exp/sell numbers).
- Known risks/limits: none new beyond what's explicitly documented as deferred above. Purely a content-data change (`resources.ts`) plus test-fixture fixes; no engine/UI/save-schema changes, no version bump needed.
- Review/merge: no independent review exists yet for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
- Exact next action: commit this checkpoint (resources.ts + the 4 test files + DECISIONS.md + docs/progression_analysis.md + docs/implementation_status.md + PROGRESS.md), push to `origin/feature/balance-passive-skills`, verify remote SHA equality. Then stop and report to the user — the remaining parts of "전체 생산망 밸런스 조정" (gold economy overall, crafted-good sell-price consistency) are explicitly not started; do not assume the user wants those next without asking.
