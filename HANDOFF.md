# Current handoff

- Current goal: implemented the second of the four user-requested new active skills — 목공(woodworking) — as the ninth kingdom district ("무너진 제재소"/"Ruined Sawmill"), continuing directly from the reviewed+merged foraging round per the user's chained instruction "리뷰하고 문제없으면 머지하고 다음 개발 진행해줘".
- Branch: `feature/kingdom-ruined-sawmill`, created from freshly verified `origin/main` `74f6578f6ebfa8e004a23c695ad9830836023414` on 2026-09-16 KST. The remote feature ref is absent (not yet pushed).
- Checkpoint type: not yet checkpointed in this turn — code, tests and docs are complete and verified locally, but no commit/push has been made yet. This HANDOFF captures the state immediately before that checkpoint.
- What was added: `ruined_sawmill` project (`introducedVersion: 18`, `unlockSkills: ['woodworking']`, materials wood 24 + `wild_berry` 14 — the immediately-preceding district's (foraging's) real output, continuing the material-dependency-chain principle for the fifth time). New `SkillId` `'woodworking'` — unlike foraging (a pure gather skill), this is a **crafting** skill (has recipes), designed as the logging-paired counterpart to blacksmithing (mining-paired): 4 recipes (나무 판자/참나무 판자/단단한 들보/마법 골조) consuming logging's 4 tiers (wood/oak/hardwood/magic_wood) 1:1, numbers modeled on blacksmithing's same-tier ingot recipes. `Model.version` 17→18. `save.ts`'s skill/tool restoration loops gained `if (version < 18 && id === 'woodworking') continue;` (same pattern as foraging's `< 17` guard). **Zero changes to `model.ts`/`App.tsx`/`KingdomView.tsx`/`ProductionView.tsx`/`ToolsView.tsx`** — confirms the generic architecture absorbs crafting-type new skills exactly like gather-type ones (first live confirmation of the crafting-recipe UI path for a *new* skill, since `ProductionView` already branches "제작 시작"/"채집 시작" purely on `r.recipe` presence).
- Fixture checklist this round: applied the full D022–D027 checklist (delete the new project key from all 16 lower-version `kingdom.test.ts` fixtures, pin `raw.version` explicitly on every boundary test) from the *start* of implementation rather than discovering gaps after a failing run. Result: `npm test` 137/137 passed on the **first run** this round — zero defects, the first clean round since D024/D025. Two things worth noting: (1) the *previous* round's "v17 저장은..." boundary test still had the implicit-ambient-version bug (same class D027 found in the v16 test) — found and fixed it at the start of this round rather than after a failure; (2) `guild-quest.test.ts`'s deterministic daily-quest-seed test was **not** affected this time, because woodworking's 4 new resources all have `recipe` set and are therefore excluded by `generateDailyQuests`'s `!r.recipe` filter — confirms D027's caution that only *recipe-less* new resources risk that particular fixture.
- Verification: `npm test` 137/137 PASS, `npm run build` PASS, `npm run simulate` PASS (via `npx tsx`, woodworking's 4 tiers report per-hour efficiency numbers modeled on blacksmithing's). Browser QA: completed all 8 prior districts via `gs.mutate()`, drove `ruined_sawmill` through survey→clear→deliver→restore via the real engine functions. Confirmed: "무너진 제재소 복원 완료 · 목공 해금" notice, restoration count 9, new "목공" sidebar entry, and — unlike foraging's plain gather screen — the ProductionView correctly rendering all 4 recipes with their ingredient costs and owned amounts, and a "제작 시작" (craft) button instead of "채집 시작" (gather). Clicked craft, then advanced the engine clock 8.5s and confirmed both live state and rendered DOM show the correct increment (2 planks crafted, 6 wood consumed, 60 exp gained). No console errors.
- Docs updated this round: DECISIONS.md (D028), PROGRESS.md (new "신규 액티브 스킬 2/4" section + roadmap item 8 + Milestones line), README.md (district list + save-version line, v17→v18), docs/implementation_status.md (title, new bullet, version/migration lines, 미구현/검증 sections), docs/implementation_plan.md (§5 woodworking entry).
- Known risks/limits: none new. Purely additive content + one save-schema version bump; no engine logic changed.
- Review/merge: no independent review exists yet for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
- Exact next action: commit this checkpoint (all listed source/test/doc changes), push to `origin/feature/kingdom-ruined-sawmill`, verify remote SHA equality. Then either continue to 조제(apothecary) as the next atomic task, or perform the independent review + merge, per explicit user instruction — do not assume which.
