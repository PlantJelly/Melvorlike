# Current handoff

- Current goal: implement the kingdom-restoration foundation and the first ruined-forge vertical slice agreed with the user.
- Branch: `feature/kingdom-restoration-foundation`, created from freshly verified `origin/main` `13e5317ec82354aff837e643a5a9e228e6bab414` on 2026-09-15 KST. The remote feature ref was absent at creation.
- Checkpoint type: Stable documentation/state-contract checkpoint. Known pre-checkpoint parent is `13e5317`; locate the commit containing this snapshot with `git log -1 -- HANDOFF.md`.
- Current changes: implementation plan rewritten around kingdom projects; game design/content spec corrected to remove Lv50 parallel auto-gathering and random settlers; active 10/passive 2 roster and first forge project specified; PROGRESS and DECISIONS record requirements and atomic order.
- Implemented application behavior: none yet on this branch. Application remains main's save v9, 8 skills, 105 tests state until the next checkpoint.
- Verification for this checkpoint: `git diff --check` PASS; required document paths exist; targeted search confirms current specs reject parallel auto-gathering and random settlers while historical decision records remain identified and superseded by D018. No application code/config changed, so code tests were not rerun for this documentation-only checkpoint. Existing 105/105/build/simulate evidence belongs to parent `13e5317` and is not reused after code changes begin.
- Exact next action: validate the documentation diff and references, create and remotely verify the documentation checkpoint, then implement `src/content/projects.ts`, project engine/current-action union, skill locking, save v10 migration and focused tests.
- Acceptance criteria: see `docs/implementation_plan.md` and the current section in PROGRESS. Main points are one global active slot, deterministic one-time salvage, partial delivery, permanent blacksmithing unlock, exact interruption/offline behavior, and lossless v1-v9 migration.
- Known risks: current UI and tests assume every `currentAction` has a resource id; save v10 must migrate the old untagged production action; daily quests and every engine entry point must respect skill locks; existing users must not receive project salvage or lose access.
- Review/merge: no review exists for this branch. Main integration has not been requested and is not authorized by this development continuation alone.
