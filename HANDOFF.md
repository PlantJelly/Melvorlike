# Current handoff

- Current goal: completed the independent review and main integration of the kingdom-restoration seventh district (fallen magic tower → `magic` + `equipment`), the last district planned in `docs/implementation_plan.md` §4. Requested by the user in one step ("리뷰하고 문제없으면 머지하고 반대방향 저장검증 진행해줘"). Next development (reverse-direction save-validation check, D025) follows in this same continuation.
- Branch: `main`. Feature branch `feature/kingdom-fallen-tower` and `origin/main` were both verified at `6f8af092eb82bcf228edf4396c0838e7f2cb290b` after the fast-forward push on 2026-09-16 KST.
- Checkpoint type: Stable integration record. Known pre-checkpoint parent: `b176256`. Locate the snapshot commit with `git log -1 -- HANDOFF.md`.
- Review: independent full merge-base-to-HEAD diff review (this session, re-reading the diff fresh), with the largest-yet `kingdom.test.ts` diff (256 lines) as the primary focus. Independently enumerated all 13 locations in the test file that build a `raw` save via `encodeSave(...)` (via `grep -n "const raw = JSON.parse(encodeSave"`) and inspected each one; confirmed the **third consecutive round with zero staleness bugs** in the recurring D022 checklist. Verified wool is the correct material choice (ranching's real output, since magic's own output can't gate its own unlocking project — a circular dependency D025 explicitly corrected from D024's imprecise phrasing). No blocking findings; `KingdomView.tsx`/`App.tsx` had zero diff. PASS. Report stored outside the worktree at `git rev-parse --git-path development-workflow-review.md`.
- Verification (independently re-run this session, not reused): `npm test` 131/131, `npm run build`, `npm run simulate` (via `npx tsx` — same pre-existing PATH quirk), `git diff --check`, `git merge-tree` (no conflicts — target IS the merge-base, pure fast-forward) all PASS on reviewed SHA `6f8af09`, reconfirmed again on the fast-forwarded `main` tip (same SHA). Browser QA against a dev server pointed explicitly at the feature worktree: fresh save shows all seven districts surveyable with no console errors; live-clicked the tower's own survey action at the exact reviewed SHA and confirmed the correct salvage text and phase transition. Did not repeat the tower's full clear/restore timers live in this review pass (same code path already exercised live for the forge across six prior reviews; the tower's own full completion is covered by 3 new unit tests) — judged proportionate.
- CI/policy: 0 GitHub Actions workflows repo-wide (re-queried fresh, unauthenticated REST API), 0 check-runs for reviewed SHA `6f8af09` → NOT CONFIGURED. Branch protection not re-queryable (still requires auth unavailable here); no change from prior finding.
- Implemented (unchanged from feature branch): `fallen_tower` project — survey → 90s clearing (salvage wood 9/stone 7) → deliver wood 20/wool 12 → 110s restoration → permanent `magic` skill unlock + `equipment` feature unlock, restoration score 7 total. All seven districts surveyable from a fresh save with no hard prerequisite. Save v16 via the same generic `introducedVersion`-based version gating. `src/engine/model.ts` and `src/ui/KingdomView.tsx` needed zero changes. **This completes the kingdom-restoration district roadmap planned in `docs/implementation_plan.md` §4** — every skill `unlockedGame()` pre-unlocks (cooking, farming, ranching, magic) and every feature (tools, equipment, guild) now has a real granting project.
- User's explicit follow-up instruction for this same continuation: after merge, implement the **reverse-direction save-validation check** (D025) — every `unlockedSkills`/`unlockedFeatures` entry in a v10+ save must be explainable by either a starter skill/feature or a project that is both introduced-at-that-version and complete. This closes a real gap: currently, a not-yet-introduced project's `unlockSkills`/`unlockFeatures` are never checked against that save version (since `projectIdsAtVersion` filters them out entirely), so a save whose checksum was recomputed after manual editing (checksum is a plain hash, not a keyed HMAC — documented as friction, not cryptographic security) could currently claim an impossible skill/feature is unlocked without being caught. This was explained to the user in conversation before they requested it; user confirmed understanding ("몰래 해금됐다고 넣는걸 방지하는 로직").
- Known risks (unchanged, disclosed): no hard prerequisite/lock system between districts — order only softly enforced via material dependency. All new numbers are unplaytested.
- Exact next action: implement the reverse-direction save-validation check in `src/engine/save.ts` on a fresh feature branch. Key design consideration: `unlockedGame()` (used both as the actual v1-v9 migration base AND as a widely-used test fixture helper across `accessory.test.ts`/`farm.test.ts`/`food.test.ts`/`guild.test.ts`/`milestone.test.ts`/`ranch.test.ts`/`kingdom.test.ts`) fully pre-unlocks every skill/feature — but those test-fixture usages construct saves with `version < 10` (which bypasses the entire project-validation block including this new check), so the new check should NOT conflict with them the way it did in D020's first attempt (back then, several skills genuinely had no granting project yet, which is no longer true). Verify this empirically by running the full suite after implementing, not just by the theoretical argument in D025.
