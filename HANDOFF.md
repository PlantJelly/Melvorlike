# Current handoff

- Current goal: completed the independent review and main integration of the apothecary skill / tenth kingdom district ("무너진 약방"), requested by the user in two steps ("1번 진행해줘" to implement, then "1번 진행해줘" again selecting "리뷰하고 문제없으면 머지 후 재봉(마지막 신규 스킬) 계속 진행"). Now starting the next atomic task: 재봉(sewing), the fourth and final of the four user-requested new active skills.
- Branch: `main`. Feature branch `feature/kingdom-ruined-apothecary` and `origin/main` were both verified at `39cb17f040b4a5f6eedb0bdb498f59b74e873e04` after the fast-forward push on 2026-09-16 KST.
- Checkpoint type: Stable integration record. Known pre-checkpoint parent: `fbb724a`. Locate the snapshot commit with `git log -1 -- HANDOFF.md`.
- Review: independent full merge-base-to-HEAD diff review (this session, re-reading the diff fresh), covering `projects.ts`/`resources.ts`/`types.ts` (new `ruined_apothecary` project + `apothecary` skill paired with foraging, recipe numbers independently confirmed byte-for-byte identical to woodworking's matching tiers), `model.ts`/`save.ts` (version bump + two new `< 19` guards), and the 257-line `kingdom.test.ts` diff (traced all 18 `delete raw.projects.ruined_apothecary` additions, verified the carried-over "v18 저장은..." test's implicit-ambient-version bug was found and fixed at the start of this round, verified the two new tests and the achievement-test extension to restoration count 10). One non-blocking, self-disclosed finding: this round's own newly-added "v19 저장은..." test carries the same latent version-pinning characteristic (already tracked in DECISIONS.md D029 and HANDOFF's own next-action as something the *next* round must fix first) — does not block PASS. Report stored outside the worktree at `git rev-parse --git-path development-workflow-review.md`.
- Verification (independently re-run this session, not reused): `npm test` 139/139, `npm run build`, `npm run simulate` (via `npx tsx`), `git diff --check`, `git merge-tree` (target IS the merge-base — pure fast-forward, no conflicts) all PASS on reviewed SHA `39cb17f`, reconfirmed again on the fast-forwarded `main` tip (same SHA). Browser QA went beyond the implementing turn's check: repeated the migration+reverse-validation interaction test (v18-style save with 9 districts complete → correct migration to v19 with `apothecary` locked, then confirmed D026 rejects a save tampered to sneak `'apothecary'` into `unlockedSkills`), and repeated last round's crafting-type-skill-specific check (material-affordability gate: `begin(state, 'berry_tonic')` with no `wild_berry` in inventory correctly returns `false`). No console errors.
- CI/policy: 0 GitHub Actions workflows repo-wide (re-queried fresh, unauthenticated REST API), 0 check-runs for reviewed SHA `39cb17f` → NOT CONFIGURED. Branch protection not re-queryable (still requires auth unavailable here); no change from prior finding.
- Implemented (unchanged from feature branch): tenth kingdom district "무너진 약방" (`ruined_apothecary`, `introducedVersion: 19`) unlocking a new active skill `apothecary` (조제) — a crafting skill paired with foraging the way woodworking pairs with logging: 4 recipes (산딸기 물약/들버섯 연고/산약초 영약/영지버섯 묘약) consuming foraging's 4 tiers 1:1, numbers directly reused from woodworking's matching tiers (safe since foraging's own raw numbers are themselves 1:1 copies of logging's, per D027). Material chain uses the immediately-preceding district's (woodworking's) real output (`plank`), continuing the established principle for the sixth time. Save `Model.version` 18→19. Zero engine/UI code changes beyond content + the version-gated skill/tool restoration guards in `save.ts` (10th confirmation the architecture is fully generic).
- Known risks (unchanged, disclosed): the recurring implicit-ambient-version bug pattern is now confirmed structural (3 consecutive rounds) — **the very next action for the 재봉/sewing round must be checking this round's own "v19 저장은..." boundary test first**, before writing any new code, since by the pattern's own nature it cannot have failed yet and will only break once the ambient version advances to 20. `guild-quest.test.ts` remains unaffected only because apothecary's resources are all recipe-bearing; this stays true for a hypothetical future recipe-bearing addition but must be re-checked if sewing (the next round) introduces any recipe-less resource.
- Design note carried into the next round: 재봉(sewing) has no natural "gather skill → crafting skill" pairing partner left in the active-skill list order (it sits between 조제 and 마법, immediately before the multi-source 마법 skill). DECISIONS.md D029's caution flags this — the next round must decide sewing's material design (working hypothesis, not yet committed: wool from ranching as primary material, since PROGRESS.md's earlier D027 caution speculated this and wool is otherwise completely unused outside kingdom-district gating).
- Exact next action: this turn continues directly into implementing 재봉(sewing) as the next atomic task — the eleventh kingdom district — per the user's "다음 개발 진행해줘"-equivalent instruction chained onto this review+merge request. This is the last of the four planned new active skills; once merged, the full "액티브 10개" roster from PROGRESS.md's "전체 방향" line will be complete. Following the same one-atomic-task-per-round rhythm as the prior 10 district rounds: implement, checkpoint (commit+push to a new feature branch), then stop for the user's next explicit review/merge instruction.
