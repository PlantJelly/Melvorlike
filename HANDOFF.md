# Current handoff

- Current goal: none active — the guild raw-material purchase/exchange (PROGRESS Planned item 4) is implemented, reviewed, fixed, and merged to `main`. This snapshot records that completion.
- Branch: `main`. Feature branches `feature/farming-loop`, `feature/ranching-loop`, `feature/farm-ranch-cooking`, and `feature/guild-exchange` still exist on `origin` (not deleted) but are all fully merged.
- Checkpoint type: Stable. `npm test` and `npm run build` pass on merged `main`, and this is the exact code pushed to `origin/main`.
- Snapshot commit: resolve with `git log -1 -- HANDOFF.md` and verify ancestry from parent `cf02d55461cb5a6d7adfcef33ee96c8c108302da`.
- Last verified Stable checkpoint before this one: `c53d666` on `feature/guild-exchange` (round-2 review PASS), merged into `main` as `cf02d55`.
- Completed this session: buy any already-unlocked raw material (no recipe) with gold; exchange a higher-tier raw material down to its skill's Lv1 baseline at `1.5^tierGap` (floored), gated by a new 3-tier gold-only guild upgrade path — on `feature/guild-exchange` (`d0b755d`); generalized `buyCrop` into `buyResource` (any raw material, plus a `reqLevel` check the old function never had); 2 rounds of self-review via the `code-review` skill (high effort) with a fix after round 1 (`c53d666`, a boundary-test gap) — round 2 PASSed with zero findings. Merged to `main` on explicit user request (`cf02d55`) and pushed, with remote SHA equality verified for the feature branch and `main` before and after the merge. Deliberately scoped out: the daily/milestone quest system game_design.md's guild section also describes — guild leveling here is a flat gold donation instead (DECISIONS D010 has the full rationale).
- Incomplete: guild quests, enchanting/accessories (PROGRESS Planned item 5); arbitrary tier-pair exchange (everything currently converges to the skill's Lv1 material only), exp-boost food (달걀찜), farming/ranching extensions noted as future work in `docs/implementation_status.md`.
- Tests: `npm test` — 65/65 PASS on merged `main` (`cf02d55`). `npm run build` PASS. Both re-run after the merge, not just reused from the feature branch.
- Browser verification: see docs/implementation_status.md for the full walkthrough (all 16 raw materials render with correct locked/unlocked state → bought wood → exchanged oak for exactly the hand-computed wood amount → upgraded guild tier → previously-locked hardwood exchange unlocked and produced exactly the hand-computed amount → magic_wood correctly stayed locked).
- Checks not run: no lint script or tracked GitHub Actions workflow exists. Both reviews this session were self-review (same session), not a second LLM/session.
- Failed tests/core error: none.
- Known issues carried over: multi-tab save contention unsupported; backup/recovery UI absent; balance provisional and unplaytested as a full economy. Guild-specific: `buyResource` is a behavior change for farming crops too (added the `reqLevel` check `buyCrop` never had) — full suite passes but this wasn't purely additive, flagged in DECISIONS D010. The anti-circular-trade property (exchanging then reselling always nets less than direct selling) holds for the *current* `ResourceDB` sell-price dataset and is regression-tested, but isn't structurally enforced — a future price rebalance could silently break it if that test isn't kept green.
- Uncertainty: exact guild/exchange balance (tier-gap rate, upgrade gold costs) untested against real play sessions; remote branch protection/required-check policy on `origin` remains unverified (still NOT CONFIGURED as far as this session can observe — no tracked CI).
- Exact first action next session: read AGENTS → HANDOFF → PROGRESS → DECISIONS → README, inspect branch/status/HEAD, fetch origin and compare `main` against this snapshot (expect `cf02d55` unless someone else has pushed). If equal and the tree is clean, this checkpoint is complete — the four merged feature branches may be deleted locally/remotely at the user's discretion (not done automatically). To continue development, start PROGRESS's Planned item 5 (enchanting/accessories) on a new `feature/<topic>` branch off fresh `main`.
- Prerequisites: repository-root PowerShell (Node/npm are not on the default Bash PATH in this environment — prefix commands with `$env:Path += ";C:\Program Files\nodejs"` in PowerShell), existing Git authentication. No new global config, hooks, CI or background services were added.
- Remote preservation: `main` pushed and verified equal to `origin/main` (`cf02d55`) at merge time. On resume, re-verify with a fresh `git ls-remote --heads origin refs/heads/main` before trusting this snapshot's SHA.
- Review evidence: the review report at `.git/development-workflow-review.md` is untracked (outside the worktree by design, per AGENTS.md's no-self-referencing-review-commit rule) and each round's report overwrote the last — only the round-2 (final, PASS) guild report currently exists there; it does not survive a fresh clone. If it's gone, there is no trusted review evidence and a re-review is needed before trusting any of the four merges' PASS claims — all are otherwise attested only by their merge commits (`510386f`, `a95abbe`, `355cafe`, `cf02d55`) and PROGRESS.md's summaries.
