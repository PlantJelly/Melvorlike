# Current handoff

- Current goal: completed the independent review and main integration of the kingdom-restoration third district (ruined restaurant → cooking), requested by the user in one step ("리뷰하고 문제없으면 머지하고 다음 개발 진행해줘"). Next development (fourth district) follows in this same continuation.
- Branch: `main`. Feature branch `feature/kingdom-restaurant` and `origin/main` were both verified at `cedabbdf0bb9cf821d09bea8ad3ae11795e94927` after the fast-forward push on 2026-09-16 KST.
- Checkpoint type: Stable integration record. Known pre-checkpoint parent: `a20dcd7`. Locate the snapshot commit with `git log -1 -- HANDOFF.md`.
- Review: independent full merge-base-to-HEAD diff review (this session, re-reading the diff fresh rather than trusting the implementing turn's own summary) covering `projects.ts`, `model.ts`, `save.ts`, `kingdom.test.ts`, all fixture tests, and doc diffs. No blocking findings; `KingdomView.tsx`/`App.tsx` had zero diff, confirming the generalized pattern from the bridge review is genuinely reused. PASS. Report stored outside the worktree at `git rev-parse --git-path development-workflow-review.md`.
- Verification (independently re-run this session, not reused): `npm test` 119/119, `npm run build`, `npm run simulate`, `git diff --check`, `git merge-tree` (no conflicts — target IS the merge-base, pure fast-forward) all PASS on reviewed SHA `cedabbd`, reconfirmed again on the fast-forwarded `main` tip (same SHA). Browser QA against a dev server pointed explicitly at the feature worktree: fresh save shows all three districts surveyable with no console errors; live-clicked the restaurant's own survey action at the exact reviewed SHA and confirmed the correct salvage text ("나무 5개 · 돌 3개") and phase transition. Did not repeat the restaurant's full clear/restore timers live in this review pass (same code path already exercised live for the forge in two prior reviews; the restaurant's own full completion is covered by 4 new unit tests) — judged proportionate.
- CI/policy: 0 GitHub Actions workflows repo-wide (re-queried fresh, unauthenticated REST API), 0 check-runs for reviewed SHA `cedabbd` → NOT CONFIGURED. Branch protection not re-queryable (still requires auth unavailable here); no change from prior finding.
- Implemented (unchanged from feature branch): `ruined_restaurant` project — survey → 50s clearing (salvage wood 5/stone 3) → deliver wood 12/fish_small 8 → 70s restoration → permanent cooking unlock, restoration score 3 total. All three districts surveyable from a fresh save with no hard prerequisite; fish_small (a fishing product) naturally nudges finishing the bridge first. Save v12 via `ProjectDef.introducedVersion`-based version gating (same generic logic as v11, unchanged). `src/engine/model.ts` and `src/ui/KingdomView.tsx` needed zero changes — confirms the `introducedVersion` pattern (D020/D021) is genuinely reusable.
- Known risks (unchanged, disclosed): no hard prerequisite/lock system between districts — order only softly enforced via material dependency. New saves have no progression path past forge+bridge+restaurant until further districts are built. All new numbers are unplaytested. The reverse-direction save-validation check remains deliberately deferred (D021) until farming/ranching/magic/guild each have a real unlocking project.
- Exact next action: pick the next kingdom district per `docs/implementation_plan.md` §4 order (길드 회관 — unlocks the already-implemented but currently unreachable `guild` feature, since no existing project grants `unlockFeatures: ['guild']` and `starterFeatures` is empty) on its own fresh feature branch, after repository freshness checks.
