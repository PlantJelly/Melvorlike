# Current handoff

- Current goal: continue development from the clean baseline containing the progression simulator and guild daily quests.
- Branch: `main`. The reviewed guild feature `f68447486e212b4047cdd2e9b11b1b989e2d5d34` was fast-forwarded from reviewed target `12465d35c7c8d05f19394781f9fa32425b68ec0d`; both `origin/main` and `origin/feature/guild-daily-quests` were verified at `f684474` after the push.
- Checkpoint type: Stable main integration. The exact feature review PASS is stored outside the worktree at `.git/worktrees/review-guild-daily/development-workflow-review.md`.
- Combined implementation: save v8 daily quests remain intact while the model now also uses the shared level and speed formulas from the progression simulator. `npm run simulate` reports all skill checkpoints, resource rates, and recursive early production routes. Guild daily quests generate three deterministic unlocked-resource deliveries per UTC day, validate v8 saves, consume inventory, and pay the shared sale-bonus-aware reward.
- Conflict resolution: `src/engine/model.ts` keeps save v8 and daily quest initialization while replacing the literal initial XP threshold with `experienceToNextLevel(1)`. HANDOFF, PROGRESS, and implementation status are being rewritten around the combined state instead of choosing one branch's stale snapshot.
- Verification: exact feature SHA and fast-forwarded main both passed `npm test` 99/99, `npm run build`, and `npm run simulate`; the feature also passed `git diff --check`. Browser QA showed three quests, bought 10 copper for 150G, delivered the 6-copper quest, verified gold 850→886 and inventory 10→4, then confirmed the disabled completion state and values survived reload; browser warning/error logs were empty. GitHub had no configured Actions runs, required checks, branch protection, or rulesets.
- Review corrections: exact-HEAD review found two vacuous assertions in `guild-quest.test.ts`: the migration case inspected only the available pool, and the date-boundary case could skip every assertion behind a conditional. The tests now require an actually generated high-level resource and a deterministically changed quest slot. A subsequent documentation pass found that D015 still described the pre-review migration/validation behavior; it now matches save v8's implemented invariants. Production code was already correct.
- Incomplete: milestone quests, onboarding skill unlocks, multi-plot/capacity systems, achievements, and other items in PROGRESS remain future work.
- Exact next action: choose the next atomic item from PROGRESS, create a meaningful feature branch from freshly verified `origin/main`, and define its acceptance criteria before implementation.
- Known risks: quest amount/reward balance and all progression estimates are provisional. Past-day quest identity still depends on ResourceDB ordering. The simulator's fastest-resource policy ignores material availability unless using the separate production-route report.
