# Project progress

## Original requirements and acceptance criteria

- 목적: Melvorlike 왕국 재건. 전투 없는 채집·제작 방치형으로 구 프로토타입을 재작성한다.
- 사용자 방향: 내구도/환생/마스터리 풀 제외, 관리 피로 최소화, 플레이어가 스킬 만렙·수집 완주 등 목표 선택.
- 장비 방향: 스킬별 고정 성능 주도구, 공용 장신구 3개와 마법부여석 리롤. 재질 승급 시 옵션 유지, 재질별 옵션 등급 상한. 장신구 최소 루프는 구현 완료.
- 현재 구현 수용 기준: 재료 획득/차감, 레벨 제한, 도구 효과, 음식 효과/만료, 재접속 정산 및 이전 저장 호환이 테스트로 검증되어야 한다.
- 기획 출처: docs/game_design.md, docs/content_spec.md. 이 문서의 '확정' 표현 중 제안만 된 내용이 섞여 있다. 실제 구현 범위/임시 수치는 docs/implementation_status.md와 코드를 확인한다. 정착민 및 상세 밸런스를 새 사용자 승인으로 간주하지 않는다.
- 현재 요청: 게임의 핵심 진행을 왕국 복원으로 전환하고 첫 수직 슬라이스인 폐허가 된 대장간을 구현한다. 폐허 정리/복원은 레벨 없는 왕국 활동이며 기존 전역 액티브 슬롯을 공유한다.
- 신규 저장 수용 기준: 벌목·채광만 시작 → 대장간 조사 → 30초 정리와 확정 회수품 → 나무 12·돌 10 부분 납품 → 45초 복원 → 대장작업 영구 해금과 복원도 1.
- 이전 저장 수용 기준: v1~v9의 기존 스킬과 모든 진행 상태를 보존하고 대장간을 보상 재지급 없이 완료 처리한다.
- 전체 방향: 액티브 10개(벌목/채광/낚시/야외 채집/대장작업/목공/요리/조제/재봉/마법), 패시브 2개(농사/목장). 병렬 자동 채집과 확률 정착민은 제외한다.

### 신규 액티브 스킬 4/4 — 재봉(마지막) — 열한 번째 왕국 구역 무너진 재봉소 (`feature/kingdom-ruined-tailor`)

- 요청: 사용자 명시 요청("1번 진행해줘" — 조제 리뷰+병합 뒤 이어서 "재봉 계속 진행"). 신규 액티브 스킬 4종 중 네 번째이자 마지막.
- 신규 저장 수용 기준: 열한 번째 구역 "무너진 재봉소"가 나머지 열 구역과 함께 처음부터 조사 가능 상태로 노출(하드 선행조건 없음, 기존 방침 유지) → 조사 → 정리(130초, 확정 회수품 나무 13/돌 11) → 나무 28·산딸기 물약 10 부분 납품 → 복원(150초) → 재봉 영구 해금, 복원도 11. 재봉은 목공/조제와 마찬가지로 **제작류 스킬**이지만, 목장(ranching)의 산출물 구조가 깔끔한 4단계 단일 재료가 아니라서(달걀 Lv1/양털 Lv15/우유 Lv30, 서로 다른 동물) 다른 패턴을 썼다 — 양털을 모든 티어의 주 재료로 쓰고(3/4/5/6개로 증가), 대장작업의 주괴(구리/철/금)를 티어 구분용 보조 재료로 붙였다. 시간/경험치/판매가는 목공/조제 수치를 그대로 재사용.
- 재료 의존 체인: 왕국 구역 납품 자재는 직전 구역(무너진 약방)이 연 조제의 실제 산출물(산딸기 물약)을 사용 — D024/D025/D027/D028/D029 원칙 재적용. 스킬 자체의 레시피 재료(양털)와는 별개 — 재봉은 이 둘이 서로 다른 재료 계열을 쓰는 첫 사례.
- 기존 저장 수용 기준: `ProjectDef.introducedVersion`(무너진 재봉소=20) 패턴 재사용 — 이 브랜치 이전의 v19(열 구역 모두 있는) 저장을 불러오면 열 구역 상태는 그대로 유지하고, 재봉소는 자동 완료 없이 미시작으로 이전한다. v1~v9(왕국 시스템 자체가 없던) 저장은 열한 구역 모두 이미 완료된 것으로 이전(기존 원칙 유지).
- 수용 기준: (1) 신규 저장에서 재봉소가 조사 가능 상태로 노출 (2) 정리 완료 시 회수품 1회 지급 (3) 자재 분할 납품 (4) 전 자재 납품 전엔 복원 불가 (5) 복원 완료 시 재봉 영구 해금·복원도 11, 사이드바에 재봉 탭 등장 (6) 기존 v19(열 구역 모두 있는) 저장은 상태 보존 + 재봉소는 미시작으로 이전 (7) v1~v9 저장은 열한 구역 모두 완료 상태로 이전(회귀) (8) 완료 카드 문구 정확 (9) 기존 회귀 전부 유지 (10) 신규 재봉 4개 레시피가 재료 소비·레벨 게이팅·경험치·도구 배속을 기존 제작류 스킬과 동일하게 따른다.
- 구현: 조사→정리(130초, 확정 회수품 나무 13/돌 11)→부분 자재 납품(나무 28/산딸기 물약 10)→복원(150초)→재봉 영구 해금·복원도 11. `introducedVersion: 20` 적용, 저장 v19→v20. **엔진·UI 코드 변경 없음** — 11번째 구역이자 이 패턴의 11번째 재사용 확인. `save.ts`의 스킬/도구 복원 루프 두 곳에 `if (version < 20 && id === 'sewing') continue;` 추가. D029의 caution대로 직전 라운드가 남긴 "v19 저장은..." boundary 테스트의 암묵적 버전 의존 버그를 착수 시점에 가장 먼저 고쳤다(네 번째 확인 — 이제 "매 라운드 착수 시 직전 boundary 테스트부터 점검"이 확립된 절차). `guild-quest.test.ts`는 영향 없음. 최종 `npm test` 141/141을 첫 실행에서 통과(네 번째 연속 무결함). 설계 근거는 DECISIONS.md D030 참고.
- 브라우저 QA: `gameState.mutate()`로 열 구역을 완료 처리한 뒤, 실제 엔진 함수로 재봉소를 조사→정리→납품→복원까지 진행해 "무너진 재봉소 복원 완료 · 재봉 해금" 알림·복원도 11 갱신을 확인. 사이드바에 "재봉" 항목이 새로 등장했고, 4개 레시피(양털+주괴 보조 재료)가 올바르게 렌더링됨을 확인. "제작 시작" 클릭 후 엔진 시계를 8.5초 진행시켜 양털 옷 2개 제작·양털 6개 소비·경험치 60 획득을 실제 상태와 DOM 양쪽에서 일치함을 확인. 콘솔 오류 없음.
- 검증: `npm test` 141/141, `npm run build` PASS, `npm run simulate`(`npx tsx`) PASS — 재봉 4종의 시간당 효율이 목공/조제와 동일한 수치로 출력됨을 확인.
- 리뷰/병합: 아직 진행하지 않음. 사용자의 명시적 리뷰/병합 지시가 있을 때 진행한다.
- **이 구역으로 신규 액티브 스킬 4종(채집/목공/조제/재봉)이 모두 구현됐다 — 전체 방향 문서(5번 항목)가 명시한 액티브 10개 로드맵이 완결됐다.** 남은 작업은 사용자가 처음 제시한 두 번째 선택지인 전체 생산망 밸런스 조정, 또는 다른 지시.

### 신규 액티브 스킬 3/4 — 조제 — 열 번째 왕국 구역 무너진 약방 — 완료, main 병합됨(`39cb17f`)

- 요청: 사용자 명시 요청("1번 진행해줘" — 목공 리뷰+병합 뒤 이어서 "조제 계속 진행"). 신규 액티브 스킬 4종 중 세 번째.
- 신규 저장 수용 기준: 열 번째 구역 "무너진 약방"이 나머지 아홉 구역과 함께 처음부터 조사 가능 상태로 노출(하드 선행조건 없음, 기존 방침 유지) → 조사 → 정리(120초, 확정 회수품 나무 12/돌 10) → 나무 26·나무 판자 12 부분 납품 → 복원(140초) → 조제 영구 해금, 복원도 10. 조제는 목공과 마찬가지로 **제작류 스킬**(레시피 있음)로 설계했다 — 채집의 4개 티어(산딸기/들버섯/산약초/영지버섯)를 소비해 4개의 물약류 가공품(산딸기 물약/들버섯 연고/산약초 영약/영지버섯 묘약)을 제작. 시간/경험치/판매가는 목공의 같은 티어 수치를 그대로 재사용(채집=벌목과 원재료 수치가 동일하므로 안전하게 재사용 가능).
- 재료 의존 체인: 직전 구역(무너진 제재소)이 연 목공의 실제 산출물(나무 판자)을 납품 자재로 사용 — D024/D025/D027/D028 원칙 재적용.
- 기존 저장 수용 기준: `ProjectDef.introducedVersion`(무너진 약방=19) 패턴 재사용 — 이 브랜치 이전의 v18(아홉 구역 모두 있는) 저장을 불러오면 아홉 구역 상태는 그대로 유지하고, 약방은 자동 완료 없이 미시작으로 이전한다. v1~v9(왕국 시스템 자체가 없던) 저장은 열 구역 모두 이미 완료된 것으로 이전(기존 원칙 유지).
- 수용 기준: (1) 신규 저장에서 약방이 조사 가능 상태로 노출 (2) 정리 완료 시 회수품 1회 지급 (3) 자재 분할 납품 (4) 전 자재 납품 전엔 복원 불가 (5) 복원 완료 시 조제 영구 해금·복원도 10, 사이드바에 조제 탭 등장 (6) 기존 v18(아홉 구역 모두 있는) 저장은 상태 보존 + 약방은 미시작으로 이전 (7) v1~v9 저장은 열 구역 모두 완료 상태로 이전(회귀) (8) 완료 카드 문구 정확 (9) 기존 회귀 전부 유지 (10) 신규 조제 4개 레시피가 재료 소비·레벨 게이팅·경험치·도구 배속을 기존 제작류 스킬(목공/대장작업)과 동일하게 따른다.
- 구현: 조사→정리(120초, 확정 회수품 나무 12/돌 10)→부분 자재 납품(나무 26/나무 판자 12)→복원(140초)→조제 영구 해금·복원도 10. `introducedVersion: 19` 적용, 저장 v18→v19. **엔진·UI 코드 변경 없음** — 10번째 구역이자 이 패턴의 10번째 재사용 확인. `save.ts`의 스킬/도구 복원 루프 두 곳에 `if (version < 19 && id === 'apothecary') continue;` 추가. D022~D028 체크리스트를 세 번째로 착수 시점부터 적용했음에도, 직전 라운드가 남긴 "v18 저장은..." boundary 테스트에 D027/D028과 같은 유형의 암묵적 버전 의존 버그가 또 있어 착수 시점에 먼저 찾아 고쳤다 — 이 버그 유형이 "새로 추가한 바로 그 라운드에서는 드러나지 않고 반드시 다음 라운드에서만 발견된다"는 구조적 패턴임을 세 번째로 확인했다. `guild-quest.test.ts`는 영향 없음(조제 신규 자원 전부 레시피 있음). 최종 `npm test` 139/139를 첫 실행에서 통과. 설계 근거는 DECISIONS.md D029 참고.
- 브라우저 QA: `gameState.mutate()`로 아홉 구역을 완료 처리한 뒤, 실제 엔진 함수로 약방을 조사→정리→납품→복원까지 진행해 "무너진 약방 복원 완료 · 조제 해금" 알림·복원도 10 갱신을 확인. 사이드바에 "조제" 항목이 새로 등장했고, 4개 레시피가 재료 비용과 함께 올바르게 렌더링됨을 확인. "제작 시작" 클릭 후 엔진 시계를 8.5초 진행시켜 산딸기 물약 2개 제작·산딸기 6개 소비·경험치 60 획득을 실제 상태와 DOM 양쪽에서 일치함을 확인. 콘솔 오류 없음.
- 검증: `npm test` 139/139, `npm run build` PASS, `npm run simulate`(`npx tsx`) PASS — 조제 4종의 시간당 효율이 목공과 동일한 수치로 출력됨을 확인.
- 독립 리뷰(자체 리뷰, 구현 세션과 독립적으로 diff 재검토): merge-base(`fbb724a`)~HEAD 전체 diff 확인, blocking 발견 없음. `kingdom.test.ts`(257줄 diff)의 18곳 `delete raw.projects.ruined_apothecary` 추가, 직전 라운드 "v18 저장은..." 테스트에 남아있던 암묵적 버전 의존 버그가 착수 시점에 고쳐졌는지 재확인. 비블로킹 발견 1건(자체 공개): 이번 라운드가 새로 추가한 "v19 저장은..." 테스트 자체도 같은 잠재적 버전 의존 특성을 갖고 있음(D029가 이미 예측·기록한 구조적 패턴 — 다음(재봉) 라운드가 착수 시점에 고쳐야 함, 이번 리뷰를 막지 않음). `npm test` 139/139·`npm run build`·`npm run simulate`(`npx tsx`)·`git diff --check`·`git merge-tree`(target가 merge-base와 동일 — 순수 fast-forward, 충돌 없음) 재실행. **구현 세션보다 한 단계 더 나아간 브라우저 검증**: v18 스타일 저장(아홉 구역 완료)을 실제 엔진으로 만들어 라이브로 디코딩해 v19로 정상 이전됨을 확인한 뒤, 같은 저장에 `'apothecary'`를 몰래 끼워 넣어 반대 방향 검증(D026)이 거부함을 확인 — 그리고 목공 라운드에서 확립한 제작류 스킬 전용 검증(재료 부족 시 `begin()`이 `false`를 반환하는지)을 이번엔 `berry_tonic`에 대해 재확인. CI 미추적(GitHub Actions 0건, 재확인) 확인. 리뷰 근거는 `.git/worktrees/kingdom-ruined-apothecary/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- `39cb17f`: feature/kingdom-ruined-apothecary를 main에 병합(사용자 명시 요청 "1번 진행해줘"[리뷰하고 문제없으면 머지 후 재봉 계속 진행], 리뷰 PASS·충돌 없음·원격 SHA 일치 확인 후, fast-forward). 병합 상태에서 npm test 139/139·npm build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-16).
- 남은 작업: 신규 액티브 스킬 4종 중 채집·목공·조제 3개 완료 — 재봉 1개가 남아 있다(마지막). 재봉은 전체 방향 문서 나열 순서상 뚜렷한 "쌍" 파트너가 없어(마법 바로 앞) 재료 의존 체인/스킬 형태를 재봉 라운드 착수 시점에 별도 판단해야 한다(DECISIONS.md D029 caution 참고). 사용자 지시에 따라 다음은 재봉(아래 참고).

### 신규 액티브 스킬 2/4 — 목공 — 아홉 번째 왕국 구역 무너진 제재소 — 완료, main 병합됨(`5708cb1`)

- 요청: 사용자 명시 요청("리뷰하고 문제없으면 머지하고 다음 개발 진행해줘" — 채집/무성해진 숲길 리뷰+병합 뒤 이어서). 신규 액티브 스킬 4종 중 두 번째.
- 신규 저장 수용 기준: 아홉 번째 구역 "무너진 제재소"가 나머지 여덟 구역과 함께 처음부터 조사 가능 상태로 노출(하드 선행조건 없음, 기존 방침 유지) → 조사 → 정리(110초, 확정 회수품 나무 11/돌 9) → 나무 24·산딸기 14 부분 납품 → 복원(130초) → 목공 영구 해금, 복원도 9. 목공은 채집(D027, 레시피 없음)과 달리 **대장작업과 같은 제작류 스킬**(레시피 있음)로 설계했다 — 벌목의 4개 티어(나무/참나무/단단한 나무/마법 나무)를 소비해 4개의 판자류 가공품(나무 판자/참나무 판자/단단한 들보/마법 골조)을 제작. 벌목의 레벨 구조(1/10/30/50)와 정확히 1:1로 대응.
- 재료 의존 체인: 직전 구역(무성해진 숲길)이 연 채집의 실제 산출물(산딸기)을 납품 자재로 사용 — D024/D025/D027 원칙 재적용.
- 기존 저장 수용 기준: `ProjectDef.introducedVersion`(무너진 제재소=18) 패턴 재사용 — 이 브랜치 이전의 v17(여덟 구역 모두 있는) 저장을 불러오면 여덟 구역 상태는 그대로 유지하고, 제재소는 자동 완료 없이 미시작으로 이전한다. v1~v9(왕국 시스템 자체가 없던) 저장은 아홉 구역 모두 이미 완료된 것으로 이전(기존 원칙 유지).
- 수용 기준: (1) 신규 저장에서 제재소가 조사 가능 상태로 노출 (2) 정리 완료 시 회수품 1회 지급 (3) 자재 분할 납품 (4) 전 자재 납품 전엔 복원 불가 (5) 복원 완료 시 목공 영구 해금·복원도 9, 사이드바에 목공 탭 등장 (6) 기존 v17(여덟 구역 모두 있는) 저장은 상태 보존 + 제재소는 미시작으로 이전 (7) v1~v9 저장은 아홉 구역 모두 완료 상태로 이전(회귀) (8) 완료 카드 문구 정확 (9) 기존 회귀 전부 유지 (10) 신규 목공 4개 레시피가 재료 소비·레벨 게이팅·경험치·도구 배속을 기존 제작류 스킬(대장작업)과 동일하게 따른다.
- 구현: 조사→정리(110초, 확정 회수품 나무 11/돌 9)→부분 자재 납품(나무 24/산딸기 14)→복원(130초)→목공 영구 해금·복원도 9. `introducedVersion: 18` 적용, 저장 v17→v18. **엔진(`model.ts`)·UI(`App.tsx`/`KingdomView.tsx`/`ProductionView.tsx`/`ToolsView.tsx`) 코드 변경 없음** — 이번엔 채집(레시피 없음)과 달리 레시피가 있는 제작류 스킬이었지만, `ProductionView`가 이미 `r.recipe` 존재 여부로 "제작 시작"/"채집 시작" 문구와 재료 비용 표시를 전부 데이터로 분기하도록 일반화돼 있어 역시 무변경으로 흡수됐다(9번째 구역이자 이 패턴의 9번째 재사용 확인, 제작류 스킬 경로 첫 실사용 확인). `save.ts`의 스킬/도구 복원 루프 두 곳에 `if (version < 18 && id === 'woodworking') continue;` 추가. D022~D027 체크리스트(신규 프로젝트 키 삭제 + boundary 테스트 버전 명시 고정, D027이 새로 추가한 교훈 포함)를 착수 시점부터 함께 적용해 `npm test` 137/137을 **첫 실행에서** 통과했다 — D027에서 발견됐던 두 버그 유형(암묵적 버전 의존, 결정론적 시드 픽스처 파급) 중 전자는 직전 라운드의 "v17 저장은..." 테스트에 남아있던 것을 이번 라운드 시작 시점에 먼저 찾아 고쳤고, 후자는 목공의 신규 자원이 전부 레시피가 있어(`generateDailyQuests`의 `!r.recipe` 필터에서 자동 제외) 애초에 영향이 없었다. 설계 근거는 DECISIONS.md D028 참고.
- 브라우저 QA: `gameState.mutate()`로 여덟 구역을 완료 처리한 뒤, 실제 엔진 함수로 제재소를 조사→정리→납품→복원까지 진행해 "무너진 제재소 복원 완료 · 목공 해금" 알림·복원도 9 갱신을 확인. 사이드바에 "목공" 항목이 새로 등장했고, 클릭 시 4개 레시피가 재료 비용·보유량과 함께 "제작 시작" 버튼으로 올바르게 렌더링됨을 확인(채집 화면과 달리 레시피 비용 줄이 실제로 표시됨). "제작 시작" 클릭 후 엔진 시계를 8.5초 진행시켜 나무 판자 2개 제작·나무 6개 소비·경험치 60 획득을 실제 상태와 DOM 양쪽에서 일치함을 확인. 콘솔 오류 없음.
- 검증: `npm test` 137/137, `npm run build` PASS, `npm run simulate`(`npx tsx`) PASS — 목공 4종의 시간당 효율이 대장작업과 유사한 수치로 출력됨을 확인.
- 독립 리뷰(자체 리뷰, 구현 세션과 독립적으로 diff 재검토): merge-base(`74f6578`)~HEAD 전체 diff 확인, blocking 발견 없음. `kingdom.test.ts`(235줄 diff)의 16곳 `delete raw.projects.ruined_sawmill` 추가, 직전 라운드 "v17 저장은..." 테스트에 남아있던 암묵적 버전 의존 버그가 이번 라운드 착수 시점에 고쳐졌는지, 신규 테스트 2개와 복원도 9 확장을 하나하나 재확인. `npm test` 137/137·`npm run build`·`npm run simulate`(`npx tsx`)·`git diff --check`·`git merge-tree`(target가 merge-base와 동일 — 순수 fast-forward, 충돌 없음) 재실행. **구현 세션보다 한 단계 더 나아간 브라우저 검증**: v17 스타일 저장(여덟 구역 완료)을 실제 엔진으로 만들어 라이브로 디코딩해 v18로 정상 이전됨을 확인한 뒤, 같은 저장에 `'woodworking'`을 몰래 끼워 넣어 반대 방향 검증(D026)이 거부함을 확인 — 그리고 채집과 달리 이번엔 제작류 스킬이라는 점에 특화된 검증도 추가로 수행: 목공 해금 후 `inventory.wood = 0`으로 설정하고 `begin(state, 'plank')`을 호출해 재료 부족 시 정상적으로 `false`를 반환하고 생산을 시작하지 않음을 확인(범용 `afford()` 재료 게이트가 신규 레시피에도 적용됨을 증명). CI 미추적(GitHub Actions 0건, 재확인) 확인. 리뷰 근거는 `.git/worktrees/kingdom-ruined-sawmill/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- `5708cb1`: feature/kingdom-ruined-sawmill을 main에 병합(사용자 명시 요청 "1번 진행해줘"[리뷰하고 문제없으면 머지 후 조제 계속 진행], 리뷰 PASS·충돌 없음·원격 SHA 일치 확인 후, fast-forward). 병합 상태에서 npm test 137/137·npm build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-16).
- 남은 작업: 신규 액티브 스킬 4종 중 채집·목공 2개 완료 — 조제/재봉 2개가 남아 있다. 사용자 지시에 따라 다음은 조제(아래 참고).

### 신규 액티브 스킬 1/4 — 채집 — 여덟 번째 왕국 구역 무성해진 숲길 — 완료, main 병합됨(`b183c24`)

- 요청: 사용자 명시 요청("1번 개발 진행해줘" — 이전에 제시한 두 선택지 중 "신규 액티브 스킬 4종(야외 채집/목공/조제/재봉)"). 전체 방향(5번 항목)의 액티브 10개 중 계획된 왕국 구역 7개(D017~D025)로 이미 8개 스킬이 갖춰져 있어, 나머지 4개는 D017의 "왕국 복원이 주 진행" 원칙에 따라 각각 새 왕국 구역을 하나씩 추가해 여는 것으로 범위를 정했다 — 4개를 한 번에 넣지 않고 지금까지의 구역 추가 리듬과 동일하게 하나씩(원자적 작업) 진행한다.
- 신규 저장 수용 기준: 여덟 번째 구역 "무성해진 숲길"이 나머지 일곱 구역과 함께 처음부터 조사 가능 상태로 노출(하드 선행조건 없음, 기존 방침 유지) → 조사 → 정리(100초, 확정 회수품 나무 10/돌 8) → 나무 22·스톤급 마법부여석 6 부분 납품 → 복원(120초) → 채집 영구 해금, 복원도 8. 채집 자체는 벌목/채광/낚시와 동일한 레시피 없는 순수 채집 스킬(4단계: 산딸기 Lv1/들버섯 Lv10/산약초 Lv30/영지버섯 Lv50, 수치는 벌목의 나무/참나무/단단한 나무/마법 나무와 동일하게 맞춤).
- 재료 의존 체인: 직전 구역(쓰러진 마법탑)이 연 마법의 실제 산출물(스톤급 마법부여석)을 납품 자재로 사용 — D024/D025 원칙 재적용.
- 기존 저장 수용 기준: `ProjectDef.introducedVersion`(무성해진 숲길=17) 패턴 재사용 — 이 브랜치 이전의 v16(일곱 구역 모두 있는) 저장을 불러오면 일곱 구역 상태는 그대로 유지하고, 숲길은 자동 완료 없이 미시작으로 이전한다. v1~v9(왕국 시스템 자체가 없던) 저장은 여덟 구역 모두 이미 완료된 것으로 이전(기존 원칙 유지).
- 수용 기준: (1) 신규 저장에서 숲길이 조사 가능 상태로 노출 (2) 정리 완료 시 회수품 1회 지급 (3) 자재 분할 납품 (4) 전 자재 납품 전엔 복원 불가 (5) 복원 완료 시 채집 영구 해금·복원도 8, 사이드바에 채집 탭 등장 (6) 기존 v16(일곱 구역 모두 있는) 저장은 상태 보존 + 숲길은 미시작으로 이전 (7) v1~v9 저장은 여덟 구역 모두 완료 상태로 이전(회귀) (8) 완료 카드 문구 정확 (9) 기존 회귀 전부 유지 (10) 신규 채집 4단계 원재료가 레벨 게이팅·경험치·도구 배속을 기존 스킬과 동일하게 따른다.
- 구현: 조사→정리(100초, 확정 회수품 나무 10/돌 8)→부분 자재 납품(나무 22/스톤급 마법부여석 6)→복원(120초)→채집 영구 해금·복원도 8. `introducedVersion: 17` 적용, 저장 v16→v17. **엔진(`model.ts`)·UI(`App.tsx`/`KingdomView.tsx`/`ProductionView.tsx`/`ToolsView.tsx`) 코드 변경 없음** — `SkillId`/`ResourceDB`/`playable`/`skillNames`/`toolNames`에 채집을 추가하는 것만으로 사이드바·생산 화면·도구 제작 화면이 자동으로 동작함을 재확인(8번째 구역이자 이 패턴의 8번째 재사용 확인). `save.ts`의 스킬/도구 복원 루프 두 곳에 `if (version < 17 && id === 'foraging') continue;` 추가(마법 도입 때와 동일 패턴, v17 이전 저장엔 해당 키 자체가 없음). D022~D025 체크리스트(모든 낮은-버전 마이그레이션·검증 테스트에 새 프로젝트 키 삭제를 구현 착수 시점에 미리 적용)를 네 번째로 적용했으나, 이번엔 두 가지를 추가로 발견·수정했다: (a) "v16 저장은..." boundary 테스트가 `raw.version`을 명시적으로 고정하지 않고 `encodeSave`의 암묵적 현재 버전에 의존하고 있어 버전을 올리자 이름과 실제 검증이 조용히 어긋난 것을 발견·`raw.version = 16` 명시 고정으로 수정, (b) `guild-quest.test.ts`의 결정론적 일일 퀘스트 시드 테스트가 신규 레벨1 원재료(산딸기) 추가로 후보 풀이 넓어지며 실제 추첨 결과가 바뀌어 깨진 것을 발견·특정 재료 이름에 의존하지 않는 `.not.toEqual` 비교로 교체. 최종 `npm test` 135/135 PASS. 설계 근거·두 발견의 상세는 DECISIONS.md D027 참고.
- 브라우저 QA: `localStorage` 직접 주입은 `gameLoop.ts`의 `pagehide` 자동 저장 플러시가 새로고침 직전 메모리 상 이전 상태로 덮어써 신뢰할 수 없음을 확인 — 살아있는 앱의 `state`를 `mutate()`로 직접 변형하는 방식으로 우회해 일곱 구역을 완료 처리한 뒤, 실제 엔진 함수로 숲길을 조사→정리→납품→복원까지 진행해 "무성해진 숲길 복원 완료 · 채집 해금" 알림·복원도 8 갱신을 확인. 사이드바에 "채집" 항목이 새로 등장했고, 클릭 시 4단계 원재료와 레벨 게이팅이 올바르게 렌더링됨을 확인. 타이머 기반 생산 완료(수량 증가)는 백그라운드 탭 타이머 스로틀링으로 화면 갱신까지 재확인하지 못했으나, 동일 생산 로직이 기존 7개 스킬에서 이미 충분히 검증돼 있고 엔진 코드 변경이 없어 회귀 위험은 낮다고 판단.
- 검증: `npm test` 135/135, `npm run build` PASS, `npm run simulate`(`npx tsx`) PASS — 채집 4종의 시간당 효율이 벌목과 동일하게 출력됨을 확인.
- 독립 리뷰(자체 리뷰, 구현 세션과 독립적으로 diff 재검토): merge-base(`fffdcc7`)~HEAD 전체 diff 확인, blocking 발견 없음. `kingdom.test.ts`(215줄 diff)의 8곳 `delete raw.projects.overgrown_trail` 추가와 신규 `raw.version = 16` 명시 고정 수정을 하나하나 재확인. `npm test` 135/135·`npm run build`·`npm run simulate`(`npx tsx`)·`git diff --check`·`git merge-tree`(target가 merge-base와 동일 — 순수 fast-forward, 충돌 없음) 재실행. **구현 세션보다 한 단계 더 나아간 브라우저 검증**: v16 스타일 저장(일곱 구역 완료)을 실제 엔진으로 만들어 라이브로 디코딩해 v17로 정상 이전됨을 확인한 뒤, 같은 저장에 `'foraging'`을 `unlockedSkills`에 몰래 끼워 넣어 반대 방향 검증(D026)이 신규 구역에도 수정 없이 그대로 맞물려 거부함을 확인 — 그리고 실제 UI에서 "채집 시작" 클릭 후 엔진 시계를 진행시켜 인벤토리·경험치·DOM이 모두 일치하는 실제 타이머 생산까지 확인(구현 세션이 백그라운드 탭 스로틀링으로 확인 못했던 부분). CI 미추적(GitHub Actions 0건, 재확인) 확인. 리뷰 근거는 `.git/worktrees/kingdom-overgrown-trail/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- `b183c24`: feature/kingdom-overgrown-trail을 main에 병합(사용자 명시 요청 "리뷰하고 문제없으면 머지하고 다음 개발 진행해줘", 리뷰 PASS·충돌 없음·원격 SHA 일치 확인 후, fast-forward). 병합 상태에서 npm test 135/135·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-16).
- 남은 작업: 신규 액티브 스킬 4종 중 채집만 완료 — 목공/조제/재봉 3개가 남아 있다. 사용자 지시에 따라 다음은 목공(아래 참고).

### 반대 방향 저장 검증 — 완료, main 병합됨(`fd99c2f`)

- 요청: 사용자 명시 요청("반대방향 저장검증 진행해줘"). 왕국 구역 로드맵이 D025에서 완결돼 `unlockedGame()`이 미리 여는 모든 스킬·기능이 전부 실제 프로젝트로 설명 가능해진 시점을 이용해, D020/D021이 보류해 온 검증을 추가한다.
- 배경(빈틈): 현재 `save.ts`는 프로젝트별로 "이 프로젝트가 완료됐으면 → 이 프로젝트가 주는 스킬/기능이 해금 목록에 있어야 한다"만 검사하고, 그마저도 `projectIdsAtVersion`(그 저장 버전에 이미 도입된 프로젝트)으로 범위를 제한한다. 그래서 예를 들어 v14 저장(대장간~밭까지만 존재, `fallen_tower`는 `introducedVersion: 16`이라 그 버전엔 아예 검증 대상이 아님)의 `unlockedSkills`에 `'magic'`을 몰래 끼워 넣어도 지금은 걸러지지 않는다 — 체크섬은 재계산 가능한 평문 해시라(README에 명시) 이것만으로 막히지 않는다.
- 구현 방향: 프로젝트별 루프가 끝난 뒤, **버전 필터 없이 `ProjectDB` 전체**를 대상으로 완료된(`s.projects[id].phase === 'complete'`) 프로젝트가 주는 스킬/기능을 모아 "해금 가능한 전체 집합"을 만들고, `unlockedSkills`/`unlockedFeatures`의 모든 항목이 시작 스킬/기능이거나 이 집합에 포함되는지 검사한다. 아직 도입되지 않은 프로젝트는 `s.projects`가 `initial()`의 미완료 기본값을 그대로 유지하므로(그 프로젝트는 애초에 raw에서 읽히지 않음) 자동으로 "미완료"로 취급돼 별도 분기 없이 정확히 동작한다.
- 안전성 사전 검증(구현 전 확인): `unlockedGame()`을 직접 v10+ 상태로 왕복하는 기존 테스트(예: `food.test.ts`/`guild-quest.test.ts`/`accessory.test.ts`/`milestone.test.ts`/`save.test.ts`)가 여럿 있는데, `unlockedGame()`은 `projectStates(true)`로 **정의된 모든 프로젝트를 완료 처리**하므로 지금(7개 구역 모두 존재) 그 완료 프로젝트들이 주는 스킬/기능 합집합이 정확히 `playable`/`featureIds` 전체와 일치한다 — 그래서 이 검증과 충돌하지 않을 것으로 예상된다(D020 때는 프로젝트가 2개뿐이라 요리/농사/목장/마법을 설명할 프로젝트가 없어 충돌했음). 실제 `npm test` 실행으로 재확인한다.
- 저장 스키마 변경 아님: 새 필드를 추가하지 않고 기존 v10+ 검증을 강화하는 것이라 `Model.version` 증가는 불필요.
- 수용 기준: (1) 정상적으로 완료된 프로젝트로 설명되는 모든 v10+ 저장은 계속 통과 (2) 아직 도입되지 않은 프로젝트의 스킬/기능이 `unlockedSkills`/`unlockedFeatures`에 섞여 있으면 거부 (3) 이미 도입된 프로젝트라도 완료되지 않은 채 그 프로젝트의 스킬/기능만 몰래 추가돼 있으면 거부(기존 per-project 검사가 이미 담당, 회귀 확인) (4) `unlockedGame()` 기반 기존 테스트 전부(다른 파일 포함) 회귀 없이 통과 (5) v1~v9 마이그레이션(이 검증을 타지 않는 경로)은 영향 없음.
- 구현: `save.ts`의 프로젝트별 루프 뒤에 버전 무관 전체 `ProjectDB` 순회로 "완료된 프로젝트가 주는 스킬·기능 + 시작 스킬/기능" 집합을 만들고, `unlockedSkills`/`unlockedFeatures`의 모든 항목이 이 집합에 포함되는지 검사하는 코드를 추가했다(`starterFeatures`를 `save.ts`에 새로 import). 저장 스키마 변경 없음(`Model.version` 불변). `kingdom.test.ts`에 신규 테스트 2개 추가: (1) v14 저장에 도입 전 구역(`fallen_tower`)의 스킬·기능을 직접 끼워 넣으면 거부되는지, (2) `unlockedGame()`(전 프로젝트 완료 처리로 만들어짐) 왕복이 계속 무손실 통과하는지. 첫 시도에서 (1)의 raw 픽스처 자체에 버그가 있어(v14인데 다리~밭까지의 정상 프로젝트 키까지 실수로 지워버림) 정상 케이스 단언이 엉뚱한 이유로 실패한 걸 직접 발견·수정 — 최종적으로 133/133 첫 실행 통과. 설계 근거는 DECISIONS.md D026 참고.
- 독립 리뷰(자체 리뷰, 구현 세션과 독립적으로 diff 재검토): merge-base(`dc93906`)~HEAD 전체 diff 확인, blocking 발견 없음. `save.ts`의 새 검증 블록을 한 줄씩 직접 추적해 기존 프로젝트별 이중조건 검사와 충돌(거짓 거부) 가능성이 없음을 확인, 신규 테스트 2개의 v14 픽스처가 실제로 정확한지(아직 도입 안 된 구역만 지웠는지) 재확인. `npm test` 133/133·`npm run build`·`npm run simulate`(`npx tsx`)·`git diff --check`·`git merge-tree`(target가 merge-base와 동일 — 순수 fast-forward, 충돌 없음) 재실행. **구현 세션보다 한 단계 더 나아간 브라우저 검증**: 새 저장에서 `localStorage`에 직접 `'magic'`을 `unlockedSkills`에 주입하고 체크섬을 지운 뒤 새로고침해, 앱이 이 저장을 실제로 거부하고 진짜 새 저장 상태(`unlockedSkills`가 다시 `["logging","mining"]`뿐)로 안전하게 대체됨을 직접 확인 — 유닛 테스트뿐 아니라 실제 구동 중인 앱에서도 이 검증이 실효가 있음을 증명. CI 미추적(GitHub Actions 0건, 재확인) 확인. 리뷰 근거는 `.git/worktrees/reverse-unlock-validation/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- `fd99c2f`: feature/reverse-unlock-validation을 main에 병합(사용자 명시 요청 "리뷰하고 문제없으면 머지해줘", 리뷰 PASS·충돌 없음·원격 SHA 일치 확인 후, fast-forward). 병합 상태에서 npm test 133/133·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-16).

### 왕국 복원 일곱 번째 구역(계획된 마지막 구역) — 쓰러진 마법탑 — 완료, main 병합됨(`6f8af09`)

- 요청: `docs/implementation_plan.md` §4 순서(낡은 축사 완료 → 쓰러진 마법탑 → …)를 따라, 기존 여섯 구역과 동일한 조사→정리→부분 납품→복원 패턴으로 일곱 번째(계획된 마지막) 왕국 프로젝트 "쓰러진 마법탑"을 구현해 마법(`magic`)과 장신구 화면(`equipment` 기능)을 영구 해금한다.
- 배경: content_spec.md §11의 원래 계획대로 이 구역은 스킬 하나와 기능 하나를 동시에 연다(길드 회관이 기능만, 나머지가 스킬만 연 것과 다름) — `unlockSkills: ['magic']`, `unlockFeatures: ['equipment']`. `equipment`는 `FeatureId`에 이미 존재하고 장신구 화면 전체(이미 구현·병합됨)가 `featureUnlocked(s,'equipment')`로 게이트돼 있지만, 지금까지 어떤 프로젝트도 부여하지 않아 신규 저장에서는 도달 불가능했다(길드 회관 이전의 `guild`와 같은 상황).
- 재료 의존 체인: 양털(`wool`, 목장 산출물)을 납품 자재에 포함시켜 축사를 먼저 끝내야 실제로 완료할 수 있게 유도 — D024가 다시 이은 체인을 한 번 더 잇는다(밀 이후 두 번째 실제 산출물 연결). 마법 자체의 산출물(마법부여석)은 이 구역이 열기 전까지 존재할 수 없으므로 자기 자신을 참조할 수 없다 — 항상 "직전에 새로 해금된" 스킬의 산출물을 쓰는 기존 원칙과 일치.
- 신규 저장 수용 기준: 일곱 구역 모두 처음부터 함께 조사 가능 상태로 노출(하드 선행조건 없음, D020~D024 방침 유지) → 조사 → 정리(90초, 확정 회수품 나무 9/돌 7) → 나무 20·양털 12 부분 납품 → 복원(110초) → 마법 영구 해금 + 장신구 화면(`equipment`) 영구 해금, 복원도 7.
- 기존 저장 수용 기준: `ProjectDef.introducedVersion`(쓰러진 마법탑=16) 패턴을 그대로 재사용 — 이 브랜치 이전의 v15(대장간+다리+식당+길드 회관+밭+축사만 있는) 저장을 불러오면 여섯 구역 상태는 그대로 유지하고, 마법탑은 자동 완료 없이 미시작으로 이전한다. v1~v9(왕국 시스템 자체가 없던) 저장은 일곱 구역 모두 이미 완료된 것으로 이전(기존 원칙 유지).
- 예상 구현 범위: 엔진(`model.ts`)의 프로젝트 상태머신은 변경 불필요 — 콘텐츠(`projects.ts`) 추가 + 저장 버전 v15→v16 + 회귀 테스트만 필요할 것으로 예상. **D022~D024의 교훈 적용**: `kingdom.test.ts`의 모든 기존 저-버전 마이그레이션·검증 테스트(v10, v11×2, v12, v13, v14, v15)에서 `raw.projects.fallen_tower`(또는 실제 id)를 구현 착수 시점에 미리 지운다.
- 별도 후속 과제(이번 범위 밖, DECISIONS.md D024 참고): 이 구역이 완료되면 `unlockedGame()`이 미리 열어주는 모든 스킬(요리/농사/목장/마법)과 모든 기능(도구/장신구/길드)이 전부 실제 프로젝트로 설명 가능해진다 — D020/D021이 보류해 온 반대 방향 저장 검증을 처음으로 안전하게 추가할 수 있는 시점이지만, 이는 콘텐츠 추가가 아니라 `save.ts` 핵심 검증 로직 변경이라 별도 원자적 작업으로 분리한다. 사용자가 명시적으로 요청할 때 진행한다.
- 수용 기준: (1) 신규 저장에서 마법탑이 조사 가능 상태로 노출 (2) 정리 완료 시 회수품 1회 지급 (3) 자재 분할 납품 (4) 전 자재 납품 전엔 복원 불가 (5) 복원 완료 시 마법 영구 해금 + 장신구 화면 영구 해금·복원도 7, 사이드바에 마법·장신구 탭 등장 (6) 기존 v15(대장간+다리+식당+길드 회관+밭+축사만 있는) 저장은 여섯 구역 상태 보존 + 마법탑은 미시작으로 이전 (7) v1~v9 저장은 일곱 구역 모두 완료 상태로 이전(회귀) (8) 완료 카드 문구가 프로젝트별로 정확함(스킬+기능 동시 해금 문구 검증) (9) 기존 회귀(대장간/다리/식당/길드 회관/밭/축사/온라인-오프라인 동치성/저장 왕복) 전부 유지.
- 구현: 조사→정리(90초, 확정 회수품 나무 9/돌 7)→부분 자재 납품(나무 20/양털 12)→복원(110초)→마법 영구 해금 + 장신구 화면 영구 해금·복원도 7. 양털(목장 산출물)로 축사 우선 완료를 자연히 유도 — 마법 자체 산출물은 이 구역이 열리기 전까지 존재할 수 없어 자기 참조가 불가능하므로 기존 원칙대로 직전 구역(목장) 산출물을 썼다. `introducedVersion: 16` 적용, 저장 v15→v16. **엔진(`model.ts`)·UI(`KingdomView.tsx`) 코드 변경 없음.** D022~D024 체크리스트(모든 낮은-버전 마이그레이션 테스트에서 새 프로젝트 키 삭제 확인)를 구현 착수 시점에 세 번째로 미리 적용 — 기존 v10/v11(무너진 식당 도입 전)/v11(대장간+다리 시절)/v12/v13/v14/v15(낡은 축사 도입 전) 테스트 7곳 전부에 `delete raw.projects.fallen_tower`를 선제적으로 추가한 뒤 실행, 131/131을 첫 실행에서 통과(세 번 연속 무결함 적용). 이 구역으로 `docs/implementation_plan.md` §4에 계획된 왕국 구역 로드맵이 완결됐다. 설계 근거는 DECISIONS.md D025 참고.
- 독립 리뷰(자체 리뷰, 구현 세션과 독립적으로 diff 재검토): merge-base(`b176256`)~HEAD 전체 diff 확인, blocking 발견 없음. `kingdom.test.ts`(256줄, 지금까지 가장 큰 diff)에서 `encodeSave`로 `raw`를 만드는 모든 위치(13곳, grep으로 전수 확인)를 하나하나 점검해 **세 번 연속으로 결함 없음**을 재확인. 양털이 실제로 올바른 재료 선택인지(마법 자체 산출물은 순환 참조라 쓸 수 없음, D025가 D024의 부정확한 서술을 정정) 별도로 검증. `npm test` 131/131·`npm run build`·`npm run simulate`(`npx tsx`)·`git diff --check`·`git merge-tree`(target가 merge-base와 동일 — 순수 fast-forward, 충돌 없음) 재실행, 별도 dev 서버로 해당 워크트리를 직접 가리킨 브라우저 QA로 마법탑 자체의 조사→정리 전환까지 재확인. CI 미추적(GitHub Actions 0건, 재확인) 확인. 리뷰 근거는 `.git/worktrees/kingdom-fallen-tower/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- `6f8af09`: feature/kingdom-fallen-tower를 main에 병합(사용자 명시 요청 "리뷰하고 문제없으면 머지하고 반대방향 저장검증 진행해줘", 리뷰 PASS·충돌 없음·원격 SHA 일치 확인 후, fast-forward). 병합 상태에서 npm test 131/131·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-16). **계획된 왕국 구역 7개 로드맵 완결.**
- 사용자 지시에 따른 다음 작업: 반대 방향 저장 검증 추가(D025) — 대화 중 사용자에게 개념(체크섬은 재계산 가능해 조작 방지의 마지막 방어선이 아니며, 아직 도입 안 된 프로젝트의 스킬/기능은 지금 전혀 검증되지 않는 빈틈이 있음)을 설명했고, 사용자가 "몰래 해금됐다고 넣는걸 방지하는 로직"으로 이해를 확인한 뒤 진행 요청.

### 왕국 복원 여섯 번째 구역 — 낡은 축사 — 완료, main 병합됨(`eae66f8`)

- 요청: `docs/implementation_plan.md` §4 순서(버려진 밭 완료 → 낡은 축사 → …)를 따라, 기존 다섯 구역과 동일한 조사→정리→부분 납품→복원 패턴으로 여섯 번째 왕국 프로젝트 "낡은 축사"를 구현해 목장(`ranching`)을 영구 해금한다.
- 배경(재료 의존 체인 재개): D023에서 버려진 밭(직전 구역이 서비스형 기능만 열어 산출물이 없었음)으로 인해 끊겼던 재료 의존 체인을, 밀(농사 산출물, `wheat`)을 납품 자재에 포함시켜 다시 이어간다 — 밀은 이미 목장 시스템에서 닭·양의 사료로 쓰이는 기존 아이템이라 자연스럽다. 밭을 먼저 끝내야 실제로 납품을 완료할 수 있게 유도(하드 선행조건은 여전히 없음).
- 신규 저장 수용 기준: 여섯 구역 모두 처음부터 함께 조사 가능 상태로 노출(하드 선행조건 없음, D020~D023 방침 유지) → 조사 → 정리(80초, 확정 회수품 나무 8/돌 6) → 나무 18·밀 10 부분 납품 → 복원(100초) → 목장 영구 해금, 복원도 6.
- 기존 저장 수용 기준: `ProjectDef.introducedVersion`(낡은 축사=15) 패턴을 그대로 재사용 — 이 브랜치 이전의 v14(대장간+다리+식당+길드 회관+밭만 있는) 저장을 불러오면 다섯 구역 상태는 그대로 유지하고, 낡은 축사는 자동 완료 없이 미시작으로 이전한다. v1~v9(왕국 시스템 자체가 없던) 저장은 여섯 구역 모두 이미 완료된 것으로 이전(기존 원칙 유지).
- 예상 구현 범위: 엔진(`model.ts`)의 프로젝트 상태머신은 변경 불필요 — 콘텐츠(`projects.ts`) 추가 + 저장 버전 v14→v15 + 회귀 테스트만 필요할 것으로 예상. **D022/D023의 교훈 적용**: `kingdom.test.ts`의 모든 기존 저-버전 마이그레이션 테스트(v10, v11×2, v12, v13, v14)에서 `raw.projects.worn_out_barn`(또는 실제 id)을 구현 착수 시점에 미리 지운다 — 테스트를 먼저 돌려 실패로 발견하지 않는다.
- 수용 기준: (1) 신규 저장에서 낡은 축사가 조사 가능 상태로 노출 (2) 정리 완료 시 회수품 1회 지급 (3) 자재 분할 납품 (4) 전 자재 납품 전엔 복원 불가 (5) 복원 완료 시 목장 영구 해금·복원도 6, 사이드바에 목장 탭 등장 (6) 기존 v14(대장간+다리+식당+길드 회관+밭만 있는) 저장은 다섯 구역 상태 보존 + 낡은 축사는 미시작으로 이전 (7) v1~v9 저장은 여섯 구역 모두 완료 상태로 이전(회귀) (8) 완료 카드 문구가 프로젝트별로 정확함 (9) 기존 회귀(대장간/다리/식당/길드 회관/밭/온라인-오프라인 동치성/저장 왕복) 전부 유지.
- 구현: 조사→정리(80초, 확정 회수품 나무 8/돌 6)→부분 자재 납품(나무 18/밀 10)→복원(100초)→목장 영구 해금·복원도 6. 밀(농사 산출물이자 기존 목장 사료 아이템)로 밭 우선 완료를 자연히 유도 — D023에서 끊겼던 재료 의존 체인을 다시 이었다. `introducedVersion: 15` 적용, 저장 v14→v15. **엔진(`model.ts`)·UI(`KingdomView.tsx`) 코드 변경 없음.** D022/D023 체크리스트(모든 낮은-버전 마이그레이션 테스트에서 새 프로젝트 키 삭제 확인)를 구현 착수 시점에 다시 미리 적용 — 기존 v10/v11(무너진 식당 도입 전)/v11(대장간+다리 시절)/v12/v13/v14(버려진 밭 도입 전) 테스트 6곳 전부에 `delete raw.projects.worn_out_barn`을 선제적으로 추가한 뒤 실행, 128/128을 첫 실행에서 통과(두 번 연속 무결함 적용). 설계 근거는 DECISIONS.md D024 참고.
- 독립 리뷰(자체 리뷰, 구현 세션과 독립적으로 diff 재검토): merge-base(`31662b3`)~HEAD 전체 diff 확인, blocking 발견 없음. `kingdom.test.ts`(213줄, 지금까지 가장 큰 diff)를 D022/D023/D024가 반복 경고한 마이그레이션 테스트 정확도 문제에 집중해 검토 — 기존 저-버전 픽스처 6곳 모두 `delete raw.projects.worn_out_barn`이 빠짐없이 추가됐고, **두 번 연속으로 결함이 발견되지 않아** 이 체크리스트가 일회성이 아니라 안정적으로 재현 가능한 절차임을 재확인. 밀(축사의 재료 의존 체인 연결점)이 실제로 `animals.ts`의 닭·양 사료 아이템인지 독립적으로 재확인(억지로 끼워 맞춘 재료가 아님). `npm test` 128/128·`npm run build`·`npm run simulate`(`npx tsx`)·`git diff --check`·`git merge-tree`(target가 merge-base와 동일 — 순수 fast-forward, 충돌 없음) 재실행, 별도 dev 서버로 해당 워크트리를 직접 가리킨 브라우저 QA로 축사 자체의 조사→정리 전환까지 재확인(정리 80초+복원 100초 완주는 동일 코드 경로가 이미 다섯 구역에서 라이브 검증됐고 축사 자체 완료는 신규 단위테스트 3개로 정밀 검증돼 있어, 라이브 완주 반복은 생략 — 적정 수준으로 판단). CI 미추적(GitHub Actions 0건, 재확인) 확인. 리뷰 근거는 `.git/worktrees/kingdom-worn-barn/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- `eae66f8`: feature/kingdom-worn-barn을 main에 병합(사용자 명시 요청 "리뷰하고 문제없으면 머지하고 다음 개발 진행해줘", 리뷰 PASS·충돌 없음·원격 SHA 일치 확인 후, fast-forward). 병합 상태에서 npm test 128/128·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-16).

### 왕국 복원 다섯 번째 구역 — 버려진 밭 — 완료, main 병합됨(`d50c478`)

- 요청: `docs/implementation_plan.md` §4 순서(길드 회관 완료 → 버려진 밭 → …)를 따라, 기존 네 구역과 동일한 조사→정리→부분 납품→복원 패턴으로 다섯 번째 왕국 프로젝트 "버려진 밭"을 구현해 농사(`farming`)를 영구 해금한다.
- 배경(재료 의존 체인의 예외): D020~D022가 확립한 "납품 자재에 직전 구역이 새로 여는 스킬의 산출물을 넣어 순서를 자연히 유도"하는 패턴은, 직전 구역(길드 회관)이 스킬이 아니라 `guild`라는 서비스형 기능만 해금해서 재사용할 산출물이 없다 — 길드는 교환/퀘스트 시스템이지 채집·가공 산출물을 만들지 않는다. 따라서 이번 구역은 이 체인이 처음으로 끊기는 지점이며, 첫 구역(대장간)과 마찬가지로 나무·돌만 사용하는 일반 자재로 되돌아간다. 이 예외는 DECISIONS.md에 별도 결정으로 기록한다.
- 신규 저장 수용 기준: 다섯 구역 모두 처음부터 함께 조사 가능 상태로 노출(하드 선행조건 없음, D020/D021/D022 방침 유지) → 조사 → 정리(70초, 확정 회수품 나무 7/돌 5) → 나무 16·돌 12 부분 납품 → 복원(90초) → 농사 영구 해금, 복원도 5.
- 기존 저장 수용 기준: `ProjectDef.introducedVersion`(버려진 밭=14) 패턴을 그대로 재사용 — 이 브랜치 이전의 v13(대장간+다리+식당+길드 회관만 있는) 저장을 불러오면 네 구역 상태는 그대로 유지하고, 버려진 밭은 자동 완료 없이 미시작으로 이전한다. v1~v9(왕국 시스템 자체가 없던) 저장은 다섯 구역 모두 이미 완료된 것으로 이전(기존 원칙 유지).
- 예상 구현 범위: 엔진(`model.ts`)의 프로젝트 상태머신은 변경 불필요 — 콘텐츠(`projects.ts`) 추가 + 저장 버전 v13→v14 + 회귀 테스트만 필요할 것으로 예상. **D022의 교훈 적용**: `kingdom.test.ts`의 모든 기존 저-버전 마이그레이션 테스트(v10, v11×2, v12, v13)에서 `raw.projects.abandoned_field`를 빠짐없이 지웠는지 구현 중 직접 점검한다.
- 수용 기준: (1) 신규 저장에서 버려진 밭이 조사 가능 상태로 노출 (2) 정리 완료 시 회수품 1회 지급 (3) 자재 분할 납품 (4) 전 자재 납품 전엔 복원 불가 (5) 복원 완료 시 농사 영구 해금·복원도 5, 사이드바에 농사 탭 등장 (6) 기존 v13(대장간+다리+식당+길드 회관만 있는) 저장은 네 구역 상태 보존 + 버려진 밭은 미시작으로 이전 (7) v1~v9 저장은 다섯 구역 모두 완료 상태로 이전(회귀) (8) 완료 카드 문구가 프로젝트별로 정확함 (9) 기존 회귀(대장간/다리/식당/길드 회관/온라인-오프라인 동치성/저장 왕복) 전부 유지.
- 구현: 조사→정리(70초, 확정 회수품 나무 7/돌 5)→부분 자재 납품(나무 16/돌 12)→복원(90초)→농사 영구 해금·복원도 5. 직전 구역(길드 회관)이 서비스형 기능만 열어 재사용할 산출물이 없어, D020부터 이어진 재료 의존 체인이 처음으로 끊기고 첫 구역(대장간)과 같은 나무·돌 자재로 되돌아갔다. `introducedVersion: 14` 적용, 저장 v13→v14. **엔진(`model.ts`)·UI(`KingdomView.tsx`) 코드 변경 없음.** D022가 남긴 체크리스트(모든 낮은-버전 마이그레이션 테스트에서 새 프로젝트 키 삭제 확인)를 구현 착수 시점에 미리 적용 — 기존 v10/v11(무너진 식당 도입 전)/v11(대장간+다리 시절)/v12/v13 테스트 5곳 전부에 `delete raw.projects.abandoned_field`를 선제적으로 추가한 뒤 실행해, 이번엔 되돌아가 고치지 않고 처음부터 통과시켰다. 설계 근거는 DECISIONS.md D023 참고.
- 독립 리뷰(자체 리뷰, 구현 세션과 독립적으로 diff 재검토): merge-base(`b773d22`)~HEAD 전체 diff 확인, blocking 발견 없음. `kingdom.test.ts`(176줄 diff)를 D022/D023이 경고한 마이그레이션 테스트 정확도 문제에 특히 집중해 검토 — 기존 저-버전 픽스처 5곳 모두 `delete raw.projects.abandoned_field`가 빠짐없이 추가됐고, "v13" 경계 테스트도 반응적 수정이 아니라 처음부터 `raw.version = 13`으로 명시 고정돼 있음을 확인. `npm test` 125/125·`npm run build`·`npm run simulate`(`npx tsx`)·`git diff --check`·`git merge-tree`(target가 merge-base와 동일 — 순수 fast-forward, 충돌 없음) 재실행, 별도 dev 서버로 해당 워크트리를 직접 가리킨 브라우저 QA로 밭 자체의 조사→정리 전환까지 재확인(정리 70초+복원 90초 완주는 동일 코드 경로가 이미 네 구역에서 라이브 검증됐고 밭 자체 완료는 신규 단위테스트 3개로 정밀 검증돼 있어, 라이브 완주 반복은 생략 — 적정 수준으로 판단). CI 미추적(GitHub Actions 0건, 재확인) 확인. 리뷰 근거는 `.git/worktrees/kingdom-abandoned-field/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- `d50c478`: feature/kingdom-abandoned-field를 main에 병합(사용자 명시 요청 "리뷰하고 문제없으면 머지하고 다음 개발 진행해줘", 리뷰 PASS·충돌 없음·원격 SHA 일치 확인 후, fast-forward). 병합 상태에서 npm test 125/125·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-16).

### 왕국 복원 네 번째 구역 — 먼지 쌓인 길드 회관 — 완료, main 병합됨(`d5b0f6f`)

- 요청: `docs/implementation_plan.md` §4 순서(무너진 식당 완료 → 길드 회관 → …)를 따라, 기존 세 구역과 동일한 조사→정리→부분 납품→복원 패턴으로 네 번째 왕국 프로젝트 "먼지 쌓인 길드 회관"을 구현해 이미 구현돼 있지만 아직 어떤 프로젝트도 열어주지 않는 `guild` 기능(길드 환전·일일 퀘스트·마일스톤)을 신규 저장에서도 도달 가능하게 만든다.
- 배경: `guild`는 `FeatureId`에 이미 존재하고 `featureUnlocked(s,'guild')`로 길드 화면 전체(환전/일일 퀘스트/마일스톤, `feature/guild-daily-quests`·`feature/guild-milestones`에서 이미 구현·병합됨)가 게이트돼 있지만, `starterFeatures`가 비어 있고 지금까지 어떤 `ProjectDef`도 `unlockFeatures: ['guild']`를 주지 않아 신규(v10+) 저장에서는 영원히 도달할 수 없었다. `unlockedGame()`(레거시 v1~v9 이전 기준)은 `featureIds` 전체를 이미 열어주므로 기존 저장은 원래부터 문제없이 길드를 써 왔다 — 이번 구역은 새 기능을 만드는 게 아니라 신규 저장의 진행 경로에 기존 기능을 연결하는 작업이다.
- 신규 저장 수용 기준: 네 구역 모두 처음부터 함께 조사 가능 상태로 노출(하드 선행조건 없음, D020/D021 방침 유지). 납품 자재에 구운 생선(요리 산출물, `grilled_fish`)을 포함시켜 식당을 먼저 끝내야 실제로 완료할 수 있게 자연히 유도 → 조사 → 정리(60초, 확정 회수품 나무 6/돌 4) → 나무 14·구운 생선 5 부분 납품 → 복원(80초) → `unlockSkills` 없음, `guild` 기능 영구 해금, 복원도 4.
- 기존 저장 수용 기준: `ProjectDef.introducedVersion`(길드 회관=13) 패턴을 그대로 재사용 — 이 브랜치 이전의 v12(대장간+다리+식당만 있는) 저장을 불러오면 세 구역 상태는 그대로 유지하고, 길드 회관은 자동 완료 없이 미시작으로 이전한다. v1~v9(왕국 시스템 자체가 없던) 저장은 네 구역 모두 이미 완료된 것으로 이전(기존 원칙 유지, `unlockedGame()`이 이미 guild를 포함하므로 실질적으로 동작 변화 없음).
- 예상 구현 범위: 엔진(`model.ts`)의 프로젝트 상태머신은 이미 `ProjectDB`·`unlockSkills`/`unlockFeatures` 전체를 순회하도록 일반화돼 있어 변경 불필요 — 콘텐츠(`projects.ts`) 추가 + 저장 버전 v12→v13 + 회귀 테스트만 필요할 것으로 예상(다리·식당 때와 동일 패턴). `unlockSkills: []`인 프로젝트는 이번이 처음이라 `skillUnlocked`류 대신 `featureUnlocked(s,'guild')`만으로 완료를 검증하는 테스트가 필요하다.
- 수용 기준: (1) 신규 저장에서 길드 회관이 조사 가능 상태로 노출 (2) 정리 완료 시 회수품 1회 지급 (3) 자재 분할 납품 (4) 전 자재 납품 전엔 복원 불가 (5) 복원 완료 시 `guild` 기능 영구 해금(스킬 해금 없음)·복원도 4, 사이드바에 길드 탭 등장 (6) 기존 v12(대장간+다리+식당만 있는) 저장은 세 구역 상태 보존 + 길드 회관은 미시작으로 이전 (7) v1~v9 저장은 네 구역 모두 완료 상태로 이전되고 guild 기능도 계속 사용 가능(회귀) (8) 완료 카드 문구가 프로젝트별로 정확함(이미 일반화돼 있어 추가 UI 변경 불필요할 것) (9) 기존 회귀(대장간/다리/식당/온라인-오프라인 동치성/저장 왕복/길드 자체 기능) 전부 유지.
- 구현: 조사→정리(60초, 확정 회수품 나무 6/돌 4)→부분 자재 납품(나무 14/구운 생선 5)→복원(80초)→`guild` 기능 영구 해금·복원도 4. 구운 생선(요리 산출물)으로 식당 우선 완료를 자연히 유도. `introducedVersion: 13` 적용, 저장 v12→v13. **엔진(`model.ts`)·UI(`KingdomView.tsx`) 코드 변경 없음.** `unlockSkills: []`인 첫 프로젝트라 `featureUnlocked(s,'guild')`만으로 완료를 검증하는 신규 테스트 추가. 구현 중 자체 발견: 기존 v10/v11/v11(대장간+다리 시절) 마이그레이션 테스트 3개가 `raw.projects`에서 새로 생긴 `guild_hall` 키를 지우지 않은 채 낮은 버전으로 되돌리고 있어(이전 encodeSave 호출 시점의 "현재 버전" 4개 프로젝트가 모두 포함됨) 최종 성공 단언이 깨질 뻔한 잠재 결함을 발견·수정(테스트 정확도 문제, 실제 저장 로직 결함 아님). 설계 근거와 이 반복되는 실수 패턴에 대한 기록은 DECISIONS.md D022 참고.
- 독립 리뷰(자체 리뷰, 구현 세션과 독립적으로 diff 재검토): merge-base(`ba993a8`)~HEAD 전체 diff 확인, blocking 발견 없음(`KingdomView.tsx`/`App.tsx` diff 0줄, `unlockSkills: []`인 첫 프로젝트에 대해 `unlockSummary()`의 빈 배열 처리를 코드 검토로 별도 확인). `npm test` 122/122·`npm run build`·`npm run simulate`(`npx tsx`)·`git diff --check`·`git merge-tree`(target가 merge-base와 동일 — 순수 fast-forward, 충돌 없음) 재실행, 별도 dev 서버로 해당 워크트리를 직접 가리킨 브라우저 QA로 길드 회관 자체의 조사→정리 전환까지 재확인(정리 60초+복원 80초 완주는 동일 코드 경로가 이미 대장간·다리·식당에서 라이브 검증됐고 길드 회관 자체 완료는 신규 단위테스트 3개로 정밀 검증돼 있어, 라이브 완주 반복은 생략 — 적정 수준으로 판단). CI 미추적(GitHub Actions 0건, 재확인) 확인. 리뷰 근거는 `.git/worktrees/kingdom-guild-hall/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- `d5b0f6f`: feature/kingdom-guild-hall을 main에 병합(사용자 명시 요청 "리뷰하고 문제없으면 머지하고 다음 개발 진행해줘", 리뷰 PASS·충돌 없음·원격 SHA 일치 확인 후, fast-forward). 병합 상태에서 npm test 122/122·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-16).

### 왕국 복원 세 번째 구역 — 무너진 식당 — 완료, main 병합됨(`cedabbd`)

- 요청: `docs/implementation_plan.md` §4 순서(부서진 다리 완료 → 무너진 식당 → …)를 따라, 대장간·다리와 동일한 조사→정리→부분 납품→복원 패턴으로 세 번째 왕국 프로젝트 "무너진 식당"을 구현해 요리를 해금한다.
- 신규 저장 수용 기준: 세 구역 모두 처음부터 함께 조사 가능 상태로 노출(하드 선행조건 없음, D020 방침 유지). 납품 자재에 피라미(낚시 산출물)를 포함시켜 다리를 먼저 끝내야 실제로 완료할 수 있게 자연히 유도 → 조사 → 정리(시간, 확정 회수품) → 나무+피라미 부분 납품 → 복원(시간) → 요리 영구 해금, 복원도 3.
- 기존 저장 수용 기준: `ProjectDef.introducedVersion`(식당=12) 패턴을 그대로 재사용 — 이 브랜치 이전의 v11(대장간+다리만 있는) 저장을 불러오면 두 구역 상태는 그대로 유지하고, 식당은 자동 완료 없이 미시작으로 이전한다. v1~v9(왕국 시스템 자체가 없던) 저장은 세 구역 모두 이미 완료된 것으로 이전(기존 원칙 유지).
- 예상 구현 범위: 엔진(`model.ts`)의 프로젝트 상태머신은 이미 `ProjectDB` 전체를 순회하도록 일반화돼 있어 변경 불필요 — 콘텐츠(`projects.ts`) 추가 + 저장 버전 v11→v12 + 회귀 테스트만 필요할 것으로 예상(다리 때와 동일 패턴).
- 수용 기준: (1) 신규 저장에서 식당이 조사 가능 상태로 노출 (2) 정리 완료 시 회수품 1회 지급 (3) 자재 분할 납품 (4) 전 자재 납품 전엔 복원 불가 (5) 복원 완료 시 요리 영구 해금·복원도 3 (6) 기존 v11(대장간+다리만 있는) 저장은 두 구역 상태 보존 + 식당은 미시작으로 이전 (7) 완료 카드 문구가 프로젝트별로 정확함(이미 일반화돼 있어 추가 UI 변경 불필요할 것) (8) 기존 회귀(대장간/다리/온라인-오프라인 동치성/저장 왕복) 전부 유지.

### 왕국 복원 두 번째 구역 — 부서진 다리 (`feature/kingdom-broken-bridge`)

- 요청: `docs/implementation_plan.md` §4 후속 구역 순서(부서진 다리 → 무너진 식당 → …)를 따라, 대장간과 동일한 조사→정리→부분 납품→복원 패턴으로 두 번째 왕국 프로젝트 "부서진 다리"를 구현해 낚시를 해금한다.
- 신규 저장 수용 기준: 부서진 다리는 대장간과 함께 처음부터 조사 가능 상태로 노출된다(구역 간 강제 선행조건 없음). 조사·정리는 재료 없이 가능하지만, 납품 자재에 벽돌(대장작업 산출물)을 포함시켜 대장간을 먼저 끝내야 실제로 완료할 수 있게 자연히 유도한다 → 조사 → 정리(시간, 확정 회수품) → 나무+벽돌 부분 납품 → 복원(시간) → 낚시 영구 해금, 복원도 2. (하드 선행조건 잠금 시스템은 이번 범위 밖 — 구역이 2개뿐이라 아직 필요치 않음, DECISIONS 참고)
- **기존 저장 수용 기준(신규 프로젝트 추가 특유의 규칙, 대장간 저장 이전과 반대 방향)**: 이 브랜치 이전의 v10 저장(대장간만 존재)을 불러오면 기존 해금·진행 상태는 그대로 유지하되, 새로 추가되는 부서진 다리는 **자동 완료 처리하지 않고 미시작(조사 가능) 상태로 나타나야 한다** — v1~v9→v10 이전이 "이미 열려 있던 기능을 잃지 않는다"였던 것과 달리, 신규 콘텐츠는 기존 플레이어도 직접 플레이해야 하는 대상이다. 저장 버전을 올리고, 스킬 추가(v3 cooking) 때와 같은 버전 게이팅 방식을 재사용한다.
- 리뷰에서 나온 비블로킹 발견 반영: `KingdomView.tsx`의 완료 카드 문구를 프로젝트별 데이터(`unlockSkills`/`unlockFeatures`) 기반으로 일반화한다(지금은 대장간 문구가 하드코딩되어 있어 다리 완료 시에도 "대장작업"이라고 잘못 표시됨).
- 수용 기준: (1) 신규 저장에서 다리가 조사 가능 상태로 노출 (2) 정리 완료 시 회수품 1회 지급 (3) 자재 분할 납품 (4) 전 자재 납품 전엔 복원 불가 (5) 복원 완료 시 낚시 영구 해금·복원도 2 (6) 기존 v10(대장간만 있는) 저장은 대장간 상태 보존 + 다리는 미시작으로 이전 (7) 완료 카드 문구가 프로젝트별로 정확함 (8) 기존 회귀(대장간/온라인-오프라인 동치성/저장 왕복) 전부 유지.

## Completed

- f6a7472: TypeScript/Vite/Solid 기반, 벌목·채광·가공·판매·도구 제작, 경과 시간 정산.
- c44ea7e: 낚시터/요리 각 3종, 음식 효과, 저장 v3 및 v1/v2 이전, 생산/도구/보관함 UI 분리.
- 검증: 2026-09-11 18:10 KST, c44ea7e 코드/설정에 npm test 25/25 PASS, npm run build PASS.
- 기존 브라우저 검증 기록: 음식 섭취 시 낚시 3.5초→3.2초, 재접속 복원, 음식 5개로 50분 연장. 이번 문서 작업에서는 재실행하지 않음.

- 07b6f9a: Development Workflow 1.1.0 적용 완료. 다섯 운영 문서와 README 연결 검증 PASS, 원격 보존 확인(2026-09-11 18:15 KST).
- a4522c9: feature/development-workflow를 main에 병합(사용자 명시 요청). 게임 코드 변경 없음, npm test 25/25·npm run build PASS 재확인 후 origin/main 푸시·원격 SHA 일치 확인(2026-09-11 19:17 KST).

- 농사 최소 루프(feature/farming-loop): 작물 3종(밀 Lv1/감자 Lv10/당근 Lv25) 구매→파종→성장(액티브 작업·화면 이동과 병행, 접속 종료 중에도 진행)→수확. 저장 v4로 밭 상태 포함, v1/v2/v3 이전 지원. 상세는 docs/implementation_status.md 참고.
- 독립 리뷰 3라운드(자체 리뷰, code-review 스킬 high): 1라운드 5건(표시 버그 1건 확정, 방어 로직 누락 2건, UI 중복 로직, 테스트 커버리지) → 전부 수정 후 2라운드 PASS(비블로킹 1건 신규 발견: 저장 검증 비대칭) → 수정 후 3라운드 PASS(발견 없음). 기록은 `.git/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- 510386f: feature/farming-loop를 main에 병합(사용자 명시 요청, 3라운드 PASS 확인 후). npm test 36/36·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-11 19:5x KST).
- 08c37b0: 병합 완료 상태를 HANDOFF/PROGRESS에 기록, origin/main 푸시·원격 SHA 일치 확인.

- 목장 최소 루프(feature/ranching-loop): 동물 3종(닭 Lv1/500G, 양 Lv15/2,000G, 소 Lv30/5,000G) 구매 → 사료(밀/당근, 농사 산출물 재사용) 보유 시 접속 여부와 무관하게 주기마다(30/45/60분) 자동 산출, 수동 확정 없음 → 사료 부족 시 주기 1회분에서 대기(적체·손해 없음), 보충 시 즉시 재개. 저장 v5로 사육 상태 포함, v1~v4 이전 지원. `farming`/`ranching`을 아우르는 `passiveSkills` 목록으로 begin()/저장 검증/화면 라우팅을 일반화(농사 리뷰 finding 6과 같은 부류의 문제를 사전에 차단). 상세는 docs/implementation_status.md, 설계 근거는 DECISIONS.md D007/D008 참고.
- 독립 리뷰 2라운드(자체 리뷰, code-review 스킬 high): 1라운드 비블로킹 3건(부분 사료 감당 분기 테스트 공백, 사료 공유 시 중복 소비 방지 미검증 — 이 작업의 수용 기준에 명시된 항목, UI 중복 계산) → 전부 수정(두 시나리오를 손으로 계산해 테스트로 고정, `RanchingView` 중복 호출 통합) → 2라운드 PASS(발견 없음).
- a95abbe: feature/ranching-loop를 main에 병합(사용자 명시 요청, 2라운드 PASS 확인 후). npm test 47/47·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-11 20:19 KST).
- f31a032: 병합 완료 상태를 HANDOFF/PROGRESS에 기록, origin/main 푸시·원격 SHA 일치 확인.

- 농사·목장 → 요리 연결(feature/farm-ranch-cooking): 감자·당근·달걀·우유를 재료로 쓰는 요리 5종(야채죽/나무꾼 도시락/광부의 스튜/대장장이 정식/축제 요리) 추가. 기존 재료/저장/버프 엔진 그대로 재사용(엔진 코드 변경 없음). "전 스킬" 버프는 `playable` 목록을 그대로 재사용해 패시브 스킬(농사/목장)까지 자동 적용됨을 브라우저에서 실증. 기획서의 달걀찜(경험치 배율)은 새 버프 타입이 필요해 이번 작업 범위에서 제외 — 설계 근거는 DECISIONS.md D009.
- 독립 리뷰 2라운드(자체 리뷰, code-review 스킬 high): 1라운드 비블로킹 1건(`playable` 배열을 복사 없이 그대로 참조 — 현재는 무해하나 잠재적 공유 변경 위험) → `[...playable]`로 수정 → 2라운드 PASS(발견 없음).
- 355cafe: feature/farm-ranch-cooking을 main에 병합(사용자 명시 요청, 2라운드 PASS 확인 후). npm test 53/53·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-11 20:37 KST).
- 7224cc3: 병합 완료 상태를 HANDOFF/PROGRESS에 기록, origin/main 푸시·원격 SHA 일치 확인.

- 길드 원재료 구매/환전(feature/guild-exchange): 이미 해금한 원재료(가공품 제외) 골드 구매, 상위 티어 원재료를 같은 스킬 최하위(Lv1) 재료로 환전(티어 차이당 1.5배 복리). 길드 등급 3단계(골드 후원)로 환전 가능 깊이 확대. 퀘스트 시스템은 범위 밖(길드 레벨업을 골드 후원으로 단순화). `buyCrop`을 `buyResource`로 일반화(모든 원재료에 적용, 스킬 레벨 검증을 엔진 단에 추가 — 기존엔 UI에서만 확인). 저장 v6로 길드 등급 포함, v1~v5 이전 지원. 설계 근거·순환 거래 방지 검증은 DECISIONS.md D010 참고.
- 독립 리뷰 2라운드(자체 리뷰, code-review 스킬 high): 1라운드 비블로킹 1건(골드가 정확히 비용과 같을 때의 경계 테스트 누락) → 경계 테스트 2건 추가 → 2라운드 PASS(발견 없음).
- cf02d55: feature/guild-exchange를 main에 병합(사용자 명시 요청, 2라운드 PASS 확인 후). npm test 65/65·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-11 21:02 KST).

- 마법부여·장신구(feature/enchanting-accessories): 농사 약초 4종·채광 마나석/금 광석·금 주괴·마법 스킬/부여석 4등급의 공급 루프, 공용 장신구 3슬롯, 스톤→구리→철→금 승급과 옵션 보존, 재질 상한 내 확률 재정규화 리롤, 전 스킬 속도/경험치/판매가 효과, 저장 v7 및 v1~v6 이전, 제작·승급·리롤 UI를 구현. 상세는 docs/implementation_status.md와 DECISIONS.md D011 참고.
- 독립 전체 diff 리뷰 PASS(자체 리뷰): feature `5481634`, target `60e0b52`, 발견 없음. `npm test` 75/75·`npm run build`·`git diff --check` PASS, 대상 브라우저 상호작용/시각 검증 PASS. 추적 CI 없음, GitHub Actions/check/status 0건, `main` 보호 비활성·필수 체크 off/empty·ruleset 0건을 확인. 증적은 `.git/development-workflow-review.md`.
- 71850e6: feature/enchanting-accessories를 main에 병합(사용자 명시 요청, 리뷰 PASS 및 병합 직전 원격/정책/충돌 게이트 확인 후). 통합 상태에서 npm test 75/75·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 및 feature ancestry 확인(2026-09-11 22:49 KST).

- 세이브 백업/복원(feature/save-backup-restore): 설정 화면에서 현재 저장을 JSON 파일로 내려받거나 클립보드로 복사하고, 파일을 선택해 복원할 수 있다. 복원은 확인 후 `decodeSave`로 검증하며 실패 시 기존 저장을 그대로 두고 오류만 표시하고, 성공 시 `advance()`로 백업 시점 이후 경과 시간을 정산해 밀린 생산을 안내한다. 저장 손상으로 자동 저장이 막힌 상태(`blocked`)도 유효한 백업 복원으로 풀린다. 백업에는 체크섬(FNV-1a, 키 정렬 해시)이 함께 저장되어 텍스트 편집기로 값을 바꾼 뒤 그대로 복원하면 거부된다(암호학적 보안이 아닌 부주의한 편집 방지용 마찰). 엔진/저장 스키마 변경 없음(v7 그대로). 설계 근거는 DECISIONS.md D012·D013 참고.
- 독립 리뷰 3라운드(자체 리뷰, code-review 스킬 high): 1라운드 8건 → 6건 수정(복원이 `advance()`를 건너뛰던 문제, 다운로드 앵커 DOM 미부착 등)·2건은 기존 폴백으로 충분해 의도적 미수정 → 2라운드 PASS. 이후 사용자 피드백(다운로드 대화상자 억제 불가 안내+클립보드 복사 추가, 체크섬 도입)을 반영한 뒤 전체 diff 기준 3라운드째 독립 리뷰에서 6건 추가 발견(체크섬이 문자열이 아니면 검증을 건너뛰던 버그 확정 등) → 전부 수정 또는 의도적 미수정으로 정리 → PASS(최종 리뷰 SHA `b5cae00`). 증적은 `.git/development-workflow-review.md`.
- f6c2705: feature/save-backup-restore를 main에 병합(사용자 명시 요청 "리뷰 진행하고 문제없으면 머지 진행해줘", 최종 리뷰 PASS·충돌 없음(`git merge-tree`)·원격 SHA 일치 확인 후). 병합 상태에서 npm test 81/81·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 및 feature ancestry 확인(2026-09-12).

- 다중 탭 저장 충돌 방지(feature/multi-tab-sync): 여러 탭에서 이 게임을 동시에 열어두면, 오래 방치된(비활성) 탭의 주기적 자동 저장이 실제로 최신 진행 상황이 담긴 저장을 조용히 덮어써 진행 상황을 잃을 수 있다 — 개발 중 실제로 목격했고 README/DECISIONS에 "다중 탭 동시 플레이 미지원"으로 기록된 바로 그 문제. `storage` 이벤트로 다른 탭의 저장 갱신을 감지해, 보이지 않는(방치된) 탭만 그 내용을 받아들이고 보이는(사용 중인) 탭은 자신의 상태를 다시 저장해 지킨다. 완벽한 동시 편집 병합(진짜 멀티탭 지원)은 범위 밖 — "충돌 방지"이지 "동시 플레이 지원"이 아님을 명확히 한다. 설계 근거는 DECISIONS.md D014 참고.
- 독립 리뷰(자체 리뷰, code-review 스킬 high, 8앵글): 7건 발견. 가장 심각한 것은 확정 버그 — 첫 구현은 방향 구분이 없어, 방치된 탭의 오래된 자동 저장이 오히려 "사용 중인" 탭의 최신 조작을 덮어쓸 수 있었다(특히 탭 전환 시 `visibilitychange` 플러시와 겹쳐 드물지 않게 발생 가능). `document.visibilityState`로 방향을 구분해 수정: 보이는 탭은 받지 않고 자신을 재저장, 보이지 않는 탭만 받아들임 — 이 수정으로 `blocked` 보호가 보이는 탭에서 조용히 풀리는 부수 문제도 함께 해소됨. `decodeSave`+`advance()` 3중 중복(이미 한 곳에서 표류해 알림 누락을 유발) → `decodeAndAdvance()` 헬퍼로 통합. 비블로킹 3건은 의도적으로 미수정(저장 삭제/clear 시 다른 탭이 되살릴 수 있는 점 — 리셋 기능 자체가 없어 구체화되지 않음, 리스너 정리/중복 방지 부재 — 이 프로젝트의 HMR 설정상 위험 미확인, 테스트 커버리지 없음 — 기존 관례와 동일). PASS(최종 리뷰 SHA `2395277`, 이후 문서 커밋(`655eacd`)이 코드 변경 없음을 재확인하여 그대로 유지). 증적은 `.git/development-workflow-review.md`.
- 0448120: feature/multi-tab-sync를 main에 병합(사용자 명시 요청 "리뷰 하고 문제없으면 머지 진행해줘", 리뷰 PASS·충돌 없음(`git merge-tree`)·원격 SHA 일치 확인 후). 병합 상태에서 npm test 81/81·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 및 feature ancestry 확인(2026-09-12).

## Latest completed integrations

- 성장 곡선 시뮬레이터: 실제 `ResourceDB`·도구·공유 경험치/속도 공식을 읽어 8개 스킬의 Lv10·30·50·99 도달 시간, 모든 자원의 시간당 효율, 돌/구리/철 도구와 첫 구운 생선의 재귀 생산 경로를 `npm run simulate`로 출력한다. `feature/progression-simulator`의 정확한 `12465d3`은 91/91 테스트·빌드·시뮬레이션 PASS 리뷰 후 `main`에 fast-forward 병합되고 원격 일치가 확인됐다.
- 길드 일일 퀘스트 통합(`feature/guild-daily-quests`): 매 UTC 날짜에 해금된 원재료 중 서로 다른 3종을 결정론적으로 뽑고, 5~15개 납품 시 판매가의 2배와 판매 장신구 보너스를 반영한 골드를 지급한다. 다음 날 완료 여부와 무관하게 교체되며 이월·불이익은 없다. 저장 v8은 정확히 3개의 서로 다른 해금 원재료와 유효 수량을 검증하며 v1~v7을 실제 복원 스킬 레벨 기준으로 이전한다.
- 길드 수용 기준: (1) 매일 3개 자동 갱신·같은 날 유지, (2) 해금 원재료만 선정, (3) 재료 부족·완료 후 재납품·날짜 경계에서 다른 퀘스트 오완료 방지, (4) 온라인/오프라인 및 과거 시각 호출의 동일 날짜 기준, (5) v1~v8 저장 이전·왕복·손상 거부.
- 통합 해결: 성장 브랜치의 공유 `experienceToNextLevel()`·`getSpeedMultiplier()`와 길드 브랜치의 Model v8·일일 퀘스트 상태를 함께 유지한다. 초기 스킬 경험치 상한도 공유 공식으로 설정한다. 문서 상태는 두 브랜치의 최종 구현을 함께 기록한다.
- 통합 결과: `main` `12465d3`의 공유 성장 공식과 길드 저장 v8을 함께 유지하도록 충돌을 해소했다. 정확한 `faf158a` 리뷰에서 마이그레이션 고레벨 생성과 날짜 경계 오완료 방지 테스트가 각각 간접 검증·조건부 검증이라 결함을 놓칠 수 있음을 발견했고, `43930ff`에서 실제 결과를 반드시 단언하도록 보강했다. 후속 문서 검토에서 D015의 이전 설명이 수정 전 동작을 가리키는 문제를 발견해 실제 v8 마이그레이션·검증 규칙과 맞췄다. 최종 `f684474` 리뷰 PASS 후 `main`에 fast-forward 병합했으며, 원격 `main`·기능 브랜치 SHA 일치와 통합 상태의 `npm test` 99/99, `npm run build`, `npm run simulate` 통과를 확인했다. 브라우저에서는 3개 의뢰 표시, 구리 10개 구매(150G), 6개 납품(36G), 재고 10→4, 완료 상태와 재접속 보존, 경고/오류 없음까지 확인했다.

## Latest completed integration — 길드 마일스톤 퀘스트

- 요청 결과: 기획 순서상 길드의 남은 온보딩 기능인 1회성 마일스톤 퀘스트를 `feature/guild-milestones`에서 구현하고, 사용자 요청에 따라 리뷰 후 `main`에 병합했다.
- 범위: `content_spec.md`의 예시 10개 중 현재 승인된 시스템으로 의미가 있는 9개(첫 채집, 아무 스킬 Lv10, 첫 도구 제작, 첫 요리, 첫 동물 입주, 첫 농사 수확, 첫 환전, 아무 스킬 Lv50, 첫 마법부여석 제작). 도감·정착민 첫 등록은 해당 시스템 자체가 미승인·미구현이므로 제외한다.
- 보상 방향: 기존 일일 퀘스트와 길드 등급 단순화를 따라 별도 길드 경험치나 새 재화를 만들지 않고, 단계별 고정 골드를 한 번만 직접 수령한다. 정확한 금액은 플레이테스트 전 임시값이다.
- 상태 방향: 레벨·도구·요리/농사/마법 경험치·목장 보유처럼 이미 영구 보존되는 상태에서 달성을 계산하고, 수령한 id만 저장한다. 과거 이용 여부를 복원할 수 없는 첫 환전만 성공 시 영구 플래그를 기록한다. 저장 v9로 올리고 v1~v8은 기존 진행을 유지한 채 수령 목록 비움·환전 플래그 false로 이전한다.
- 수용 기준: (1) 9개 목표가 문서 순서대로 표시되고 미달성/수령 가능/수령 완료를 구분한다. (2) 조건을 충족하기 전에는 수령할 수 없고, 충족 후 정확한 골드를 한 번만 지급한다. (3) 실패한 환전은 진척으로 기록되지 않고 성공한 환전만 기록된다. (4) 기존 진행으로 판별 가능한 목표는 이전 저장에서도 즉시 수령 가능하다. (5) v1~v9 이전·왕복·알 수 없는/중복 수령 id·잘못된 플래그를 검증한다. (6) 기존 일일 퀘스트, 환전, 저장, 온라인/오프라인 정산을 회귀시키지 않는다.
- 원자적 결과: 1) 마일스톤 콘텐츠 정의, 엔진 판정/수령, 환전 플래그, 저장 v9와 회귀 테스트 — Stable 체크포인트 `b265677`. 2) 길드 화면 상태/수령 UI, 문서, 브라우저 QA — Stable 체크포인트 `dafe8cd`. 3) 병합 전 리뷰에서 오래된 상태 문구를 바로잡은 `48dbf68`을 정확한 HEAD로 재검토해 PASS한 뒤 `main`에 fast-forward하고 원격 일치를 확인했다.
- 구현 결과: 길드 화면이 9개 목표를 기획 순서대로 표시하고 미달성/달성/수령 완료 상태를 구분한다. 첫 채집 실사용에서 100G를 정확히 한 번 지급하고 새로고침 후 상태를 복원했으며, 기능·통합 상태 모두 `npm test` 105/105·빌드·성장 시뮬레이터·diff 검사가 통과했다. 보상 수치와 실제 장기 온보딩 체감은 플레이테스트 전 임시다.

## Planned

아래는 개발 재개 시 선택할 제안 순서. 작업 전 원격 최신성 및 기획을 확인하고, 수용 기준을 구체화한다.

### 왕국 복원 기반 — 완료, main 병합됨(`6a0b194`)

1. ~~기획/상태 계약~~ — Stable `b8ea1d6`: 왕국 중심 진행, 스킬 구성, 자동화·정착민 충돌을 문서에서 정리했다.
2. ~~프로젝트 엔진/저장 v10~~ — Stable `fbe89f0`: 프로젝트 데이터, 액티브 작업 판별식, 조사·정리·부분 납품·복원·해금, v1~v9 비손실 이전과 회귀 테스트 8개.
3. ~~왕국 화면/온보딩~~ — Stable `6a0b194`: 왕국 기본 페이지, 3단계 프로젝트 보드, 현재 작업 헤더, 잠긴 스킬/시설 숨김과 복원 직후 해금. 빈 저장에서 전체 대장간 흐름, 작업 전환 시 진행률 보존, 부분 납품, 재접속 보존, 390×844 반응형 화면과 오류 로그를 확인했다.
4. ~~독립 리뷰~~ — PASS(자체 리뷰, 이 세션이 구현 세션과 독립): 병합 전 merge-base(`13e5317`)~HEAD(`6a0b194`) 전체 diff, `npm test` 113/113·`npm run build`·`git diff --check`·`git merge-tree` 재실행, 별도 dev 서버로 해당 워크트리를 직접 가리킨 브라우저 QA(신규 저장 사이드바 왕국/벌목/채광/보관함/설정만 노출, 조사→정리 시작→작업 전환→재개→완료→회수품 1회 지급→부분 납품까지 실사용, 오류 로그 없음)로 재검증. 비블로킹 1건 발견(`KingdomView.tsx`의 완료 카드 문구가 대장간 전용으로 하드코딩됨 — 두 번째 구역 추가 시 수정 필요, 지금은 무해). 리뷰 근거는 `.git/worktrees/kingdom-restoration-foundation/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
5. `6a0b194`: feature/kingdom-restoration-foundation을 main에 병합(사용자 명시 요청 "리뷰 진행하고 문제없으면 머지 진행해줘", 리뷰 PASS·충돌 없음(`git merge-tree`)·원격 SHA 일치 확인 후, fast-forward). 병합 상태에서 npm test 113/113·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-15).
6. 후속 기능 브랜치: ~~부서진 다리(낚시)~~ · ~~무너진 식당(요리)~~ · ~~길드 회관(guild 기능 연결)~~ · ~~버려진 밭(농사)~~ · ~~낡은 축사(목장)~~ · ~~쓰러진 마법탑(마법/장신구, 계획된 마지막 왕국 구역)~~ — 완료, main 병합됨(각각 아래 참고). **계획된 왕국 구역 로드맵 완결.**
7. ~~반대 방향 저장 검증~~ — 완료, main 병합됨(`fd99c2f`, 아래 참고).
8. 신규 액티브 스킬 4종(사용자 지시로 착수, "1번 개발 진행해줘"): ~~채집(무성해진 숲길, 여덟 번째 구역)~~ — 완료, main 병합됨(`b183c24`). ~~목공(무너진 제재소, 아홉 번째 구역)~~ — 완료, main 병합됨(`5708cb1`). ~~조제(무너진 약방, 열 번째 구역)~~ — 완료, main 병합됨(`39cb17f`). 재봉(무너진 재봉소, 열한 번째 구역, 마지막) — 구현 완료, 리뷰/병합 대기(위 "신규 액티브 스킬 4/4" 참고). **이 항목으로 전체 방향의 액티브 10개 로드맵이 완결.** 남은 대안(여전히 미확정): 전체 생산망 밸런스 조정.

### 왕국 복원 두 번째 구역 — 부서진 다리 — 완료, main 병합됨(`6971fee`)

- 구현: 대장간과 동일한 조사→정리(40초, 확정 회수품 나무 4/돌 2)→부분 자재 납품(나무 10/벽돌 6)→복원(60초)→낚시 영구 해금·복원도 2. `ProjectDef.introducedVersion`(대장간=10, 다리=11)으로 저장 검증이 "그 버전에 있어야 할 구역 집합"을 동적으로 계산하도록 확장 — 새 구역은 v10 저장에 없는 게 정상이고, v11로 이전할 때 자동 완료되지 않고 미시작으로 나타난다. `KingdomView.tsx`의 완료/복원 힌트 문구를 프로젝트별 하드코딩에서 `unlockSkills`/`unlockFeatures` 기반으로 일반화(직전 리뷰의 비블로킹 발견 수정). 저장 v11. 설계 근거는 DECISIONS.md D020 참고.
- 검토했다가 되돌린 것: 해금 목록의 반대 방향 검증("모든 unlockedSkills/Features는 시작 스킬이거나 완료된 프로젝트가 실제로 줬어야 한다")을 저장 검증에 추가하려 했으나, 요리/농사/목장/마법을 미리 다 열어주는 기존 테스트 전용 기준 상태(`unlockedGame()`)와 정면 충돌해(그 스킬들을 여는 구역이 아직 없음) 테스트 10여 개가 깨져 되돌렸다 — 나머지 구역이 다 구현된 뒤 재검토 대상(D020 참고).
- 독립 리뷰(자체 리뷰, 구현 세션과 독립적으로 diff 재검토): merge-base(`ec515bc`)~HEAD 전체 diff 재확인 중 `save.ts`의 v11 반영 전 남은 stale 주석("v10 신규 저장만") 1건 발견·수정(`6971fee`, 주석만, 로직 변경 없음) → 수정 후 HEAD 기준으로 PASS. `npm test` 116/116·`npm run build`·`npm run simulate`·`git diff --check`·`git merge-tree`(충돌 없음) 재실행, 별도 dev 서버로 해당 워크트리를 직접 가리킨 브라우저 QA로 다리 자체의 조사→정리 전환까지 재확인(정리 60초+복원 60초 완주는 동일 코드 경로가 이미 대장간에서 라이브 검증됐고 다리 자체 완료는 신규 단위테스트 3개로 정밀 검증돼 있어, 라이브 완주 반복은 생략 — 적정 수준으로 판단). CI 미추적(GitHub Actions 0건, 재확인) 확인. 리뷰 근거는 `.git/worktrees/kingdom-broken-bridge/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- `6971fee`: feature/kingdom-broken-bridge를 main에 병합(사용자 명시 요청 "리뷰하고 문제없으면 머지하고 다음 개발 진행해줘", 리뷰 PASS·충돌 없음·원격 SHA 일치 확인 후, fast-forward). 병합 상태에서 npm test 116/116·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-15).

### 왕국 복원 세 번째 구역 — 무너진 식당 — 완료, main 병합됨(`cedabbd`)

- 구현: 조사→정리(50초, 확정 회수품 나무 5/돌 3)→부분 자재 납품(나무 12/피라미 8)→복원(70초)→요리 영구 해금·복원도 3. 피라미(낚시 산출물)로 다리 우선 완료를 자연히 유도(다리의 벽돌과 동일 설계). `introducedVersion: 12` 적용, 저장 v11→v12. **엔진(`model.ts`)·UI(`KingdomView.tsx`) 코드 변경 없음** — 콘텐츠 항목 추가와 저장 버전 배열 확장만으로 완료, D020의 패턴이 그대로 재사용됨을 확인. 기존 v11(대장간+다리만 있는) 저장 이전 테스트, v12 구역 집합 검증 테스트, 완료·복원도 3 테스트 4개 추가. 설계 근거는 DECISIONS.md D021 참고(D020이 남긴 "반대 방향 검증 추가 가능 여부" 질문에 "아직 아니다 — 농사/목장/마법 구역까지 구현된 뒤 재검토"로 답함).
- 독립 리뷰(자체 리뷰, 구현 세션과 독립적으로 diff 재검토): merge-base(`a20dcd7`)~HEAD 전체 diff 확인, blocking 발견 없음(`KingdomView.tsx`/`App.tsx` diff 0줄로 패턴 재사용 확인). `npm test` 119/119·`npm run build`·`npm run simulate`·`git diff --check`·`git merge-tree`(target가 merge-base와 동일 — 순수 fast-forward, 충돌 없음) 재실행, 별도 dev 서버로 해당 워크트리를 직접 가리킨 브라우저 QA로 식당 자체의 조사→정리 전환까지 재확인(정리 50초+복원 70초 완주는 동일 코드 경로가 이미 대장간·다리에서 라이브 검증됐고 식당 자체 완료는 신규 단위테스트 4개로 정밀 검증돼 있어, 라이브 완주 반복은 생략 — 적정 수준으로 판단). CI 미추적(GitHub Actions 0건, 재확인) 확인. 리뷰 근거는 `.git/worktrees/kingdom-restaurant/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- `cedabbd`: feature/kingdom-restaurant를 main에 병합(사용자 명시 요청 "리뷰하고 문제없으면 머지하고 다음 개발 진행해줘", 리뷰 PASS·충돌 없음·원격 SHA 일치 확인 후, fast-forward). 병합 상태에서 npm test 119/119·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-16).

1. ~~농사 최소 루프~~ — 완료, main 병합됨(510386f). 남은 확장: 다중 플롯, 비료 3종, 자동 파종/수확.
2. ~~목장 최소 루프~~ — 완료, main 병합됨(a95abbe). 남은 확장: 마릿수 상한, 비료류 보조 아이템.
3. ~~농사·목장 산출물 요리 연결~~ — 완료, main 병합됨(355cafe). 남은 확장: 경험치 배율형 음식(달걀찜).
4. ~~길드 안내·하위 재료 구매/교환~~ — 완료, main 병합됨(cf02d55). ~~일일 퀘스트~~ — 완료, main 병합됨(f684474). ~~마일스톤 퀘스트~~ — 완료, main 병합됨(48dbf68). 남은 확장: 임의 티어 쌍 환전.
5. ~~마법부여·장신구~~ — 완료, main 병합됨(71850e6). 남은 확장: 광맥 발견, 장신구 다중 옵션/고유 옵션 풀, 밸런스 플레이테스트.
6. 성장 곡선·온보딩·편의 기능: ~~첫 만렙 시간 시뮬레이터~~·~~레시피/도구 생산 경로~~(완료, main 병합됨 — 12465d3), 레벨업·골드를 포함한 전체 진행 경로 확장, ~~세이브 백업/복원~~(완료, main 병합됨 — f6c2705), ~~다중 탭 충돌 방지~~(완료, main 병합됨 — 0448120). 독립 작업으로 분리.
7. (미확정, 사용자 언급만 있음) 계정 기반 클라우드 저장 동기화: 이 프로젝트는 현재 백엔드가 전혀 없는 정적 프론트엔드(Vite/Solid, `localStorage`만 사용)다. 실제로 하려면 인증·저장소·호스팅을 포함한 백엔드 신설이 필요한 별도의 큰 아키텍처 결정이며, 이번 세션에서는 착수하지 않았다. 당장은 세이브 백업 파일을 사용자가 직접 OneDrive/Google Drive 등 동기화되는 폴더에 저장하는 것으로 기기 간 이전을 대체할 수 있다.

## Milestones

- 첫 생산 루프: 구현 완료, 전체 게임 완료 아님.
- 낚시·요리 루프: 구현 완료, 수치 임시.
- Development Workflow: main에 병합 완료(a4522c9).
- 농사 최소 루프: main에 병합 완료(510386f). 수치 임시.
- 목장 최소 루프: main에 병합 완료(a95abbe). 수치 임시.
- 농사·목장 요리 연결: main에 병합 완료(355cafe). 수치 임시.
- 길드 원재료 구매/환전: main에 병합 완료(cf02d55). 퀘스트 미구현.
- 마법부여·장신구: main에 병합 완료(71850e6). 비용/효과 수치 임시.
- 세이브 백업/복원: main에 병합 완료(f6c2705). 체크섬은 부주의한 편집 방지용, 실제 보안 아님.
- 다중 탭 저장 충돌 방지: main에 병합 완료(0448120). 충돌 방지이지 동시 플레이 지원 아님.
- 성장 곡선 시뮬레이터: main에 병합 완료(12465d3). 실제 콘텐츠·엔진 공식을 사용하며 밸런스 수치는 임시.
- 길드 일일 퀘스트: main에 병합 완료(f684474). 저장 v8, UTC 일일 갱신, 보상 수치는 임시.
- 길드 마일스톤 퀘스트: main에 병합 완료(48dbf68). 저장 v9, 9개 1회성 직접 수령 목표, 보상 수치는 임시.
- 왕국 복원 기반(대장간): main에 병합 완료(`6a0b194`). 저장 v10 도입. 기존 저장은 전부 이미 해금된 상태로 이전.
- 왕국 복원 두 번째 구역(부서진 다리): main에 병합 완료(`6971fee`). 저장 v11, 낚시 영구 해금·복원도 2.
- 왕국 복원 세 번째 구역(무너진 식당): main에 병합 완료(`cedabbd`). 저장 v12, 요리 영구 해금·복원도 3.
- 왕국 복원 네 번째 구역(먼지 쌓인 길드 회관): main에 병합 완료(`d5b0f6f`). 저장 v13, `guild` 기능(환전·일일 퀘스트·마일스톤) 영구 해금·복원도 4 — 신규 저장에서 길드 화면에 처음 도달 가능해짐.
- 왕국 복원 다섯 번째 구역(버려진 밭): main에 병합 완료(`d50c478`). 저장 v14, 농사 영구 해금·복원도 5.
- 왕국 복원 여섯 번째 구역(낡은 축사): main에 병합 완료(`eae66f8`). 저장 v15, 목장 영구 해금·복원도 6.
- 왕국 복원 일곱 번째 구역(쓰러진 마법탑, 계획된 마지막 왕국 구역): main에 병합 완료(`6f8af09`). 저장 v16, 마법 영구 해금 + 장신구 화면 영구 해금·복원도 7. **계획된 왕국 구역 7개가 모두 구현·병합됐다.** 신규 저장은 대장간+다리+식당+길드 회관+밭+축사+마법탑까지 전체 로드맵을 진행할 수 있음.
- 반대 방향 저장 검증: main에 병합 완료(`fd99c2f`). D020/D021이 보류하고 D025가 안전하다고 판단한 검증을 실제로 추가·검증 완료(D026).
- 신규 액티브 스킬 1/4(채집, 여덟 번째 왕국 구역 무성해진 숲길): main에 병합 완료(`b183c24`). 저장 v17.
- 신규 액티브 스킬 2/4(목공, 아홉 번째 왕국 구역 무너진 제재소): main에 병합 완료(`5708cb1`). 저장 v18.
- 신규 액티브 스킬 3/4(조제, 열 번째 왕국 구역 무너진 약방): main에 병합 완료(`39cb17f`). 저장 v19.
- 신규 액티브 스킬 4/4(재봉, 열한 번째 왕국 구역 무너진 재봉소, 마지막): `feature/kingdom-ruined-tailor`에서 구현 완료, 저장 v20. 리뷰/병합은 사용자 지시 대기. **이 구역으로 액티브 10개(벌목/채광/낚시/야외 채집/대장작업/목공/요리/조제/재봉/마법) 로드맵이 완결됨.**
- 도감/업적: 미구현.
