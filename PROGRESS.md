# Project progress

## Original requirements and acceptance criteria

- 목적: Melvorlike 왕국 재건. 전투 없는 채집·제작 방치형으로 구 프로토타입을 재작성한다.
- 사용자 방향: 내구도/환생/마스터리 풀 제외, 관리 피로 최소화, 플레이어가 스킬 만렙·수집 완주 등 목표 선택.
- 장비 방향: 스킬별 고정 성능 주도구, 공용 장신구 3개와 마법부여석 리롤. 재질 승급 시 옵션 유지, 재질별 옵션 등급 상한. 장신구 최소 루프는 구현 완료.
- 현재 구현 수용 기준: 재료 획득/차감, 레벨 제한, 도구 효과, 음식 효과/만료, 재접속 정산 및 이전 저장 호환이 테스트로 검증되어야 한다.
- 기획 출처: docs/game_design.md, docs/content_spec.md. 이 문서의 '확정' 표현 중 제안만 된 내용이 섞여 있다. 실제 구현 범위/임시 수치는 docs/implementation_status.md와 코드를 확인한다. 정착민 및 상세 밸런스를 새 사용자 승인으로 간주하지 않는다.
- 이번 요청: Development Workflow 1.1.0 초기 적용. 다섯 운영 문서, CLAUDE import, 실제 프로젝트 명령/상태, 기능 브랜치 체크포인트 및 별도 리뷰/명시적 병합 정책을 제공한다. 게임 코드는 바꾸지 않는다.

## Completed

- f6a7472: TypeScript/Vite/Solid 기반, 벌목·채광·가공·판매·도구 제작, 경과 시간 정산.
- c44ea7e: 낚시터/요리 각 3종, 음식 효과, 저장 v3 및 v1/v2 이전, 생산/도구/보관함 UI 분리.
- 검증: 2026-09-11 18:10 KST, c44ea7e 코드/설정에 npm test 25/25 PASS, npm run build PASS.
- 기존 브라우저 검증 기록: 음식 섭취 시 낚시 3.5초→3.2초, 재접속 복원, 음식 5개로 50분 연장. 이번 문서 작업에서는 재실행하지 않음.

- 07b6f9a: Development Workflow 1.1.0 적용 완료. 다섯 운영 문서와 README 연결 검증 PASS, 원격 보존 확인(2026-09-11 18:15 KST).
- a4522c9: feature/development-workflow를 main에 병합(사용자 명시 요청). 게임 코드 변경 없음, npm test 25/25·npm run build PASS 재확인 후 origin/main 푸시·원격 SHA 일치 확인(2026-09-11 19:17 KST).

- 농사 최소 루프(feature/farming-loop): 작물 3종(밀 Lv1/감자 Lv10/당근 Lv25) 구매→파종→성장(액티브 작업·화면 이동과 병행, 접속 종료 중에도 진행)→수확. 저장 v4로 밭 상태 포함, v1/v2/v3 이전 지원. 상세는 docs/implementation_status.md 참고.
- 독립 리뷰 3라운드(자체 리뷰, code-review 스킬 high): 1라운드 5건(표시 버그 1건 확정, 방어 로직 누락 2건, UI 중복 로직, 테스트 커버리지) → 전부 수정 후 2라운드 PASS(비블로킹 1건 신규 발견: 저장 검증 비대칭) → 수정 후 3라운드 PASS(발견 없음). 기록은 `.git/development-workflow-review.md`(워크트리 밖, 커밋 대상 아님).
- 510386f: feature/farming-loop를 main에 병합(사용자 명시 요청, 3라운드 PASS 확인 후). npm test 36/36·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-11 19:5x KST).
- 08c37b0: 병합 완료 상태를 HANDOFF/PROGRESS에 기록, origin/main 푸시·원격 SHA 일치 확인.

- 목장 최소 루프(feature/ranching-loop): 동물 3종(닭 Lv1/500G, 양 Lv15/2,000G, 소 Lv30/5,000G) 구매 → 사료(밀/당근, 농사 산출물 재사용) 보유 시 접속 여부와 무관하게 주기마다(30/45/60분) 자동 산출, 수동 확정 없음 → 사료 부족 시 주기 1회분에서 대기(적체·손해 없음), 보충 시 즉시 재개. 저장 v5로 사육 상태 포함, v1~v4 이전 지원. `farming`/`ranching`을 아우르는 `passiveSkills` 목록으로 begin()/저장 검증/화면 라우팅을 일반화(농사 리뷰 finding 6과 같은 부류의 문제를 사전에 차단). 상세는 docs/implementation_status.md, 설계 근거는 DECISIONS.md D007/D008 참고.
- 독립 리뷰 2라운드(자체 리뷰, code-review 스킬 high): 1라운드 비블로킹 3건(부분 사료 감당 분기 테스트 공백, 사료 공유 시 중복 소비 방지 미검증 — 이 작업의 수용 기준에 명시된 항목, UI 중복 계산) → 전부 수정(두 시나리오를 손으로 계산해 테스트로 고정, `RanchingView` 중복 호출 통합) → 2라운드 PASS(발견 없음).
- a95abbe: feature/ranching-loop를 main에 병합(사용자 명시 요청, 2라운드 PASS 확인 후). npm test 47/47·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-11 20:19 KST).
- f31a032: 병합 완료 상태를 HANDOFF/PROGRESS에 기록, origin/main 푸시·원격 SHA 일치 확인.

- 농사·목장 → 요리 연결(feature/farm-ranch-cooking): 감자·당근·달걀·우유를 재료로 쓰는 요리 5종(야채죽/나무꾼 도시락/광부의 스튜/대장장이 정식/축제 요리) 추가. 기존 재료/저장/버프 엔진 그대로 재사용(엔진 코드 변경 없음). "전 스킬" 버프는 `playable` 목록을 그대로 재사용해 패시브 스킬(농사/목장)까지 자동 적용됨을 브라우저에서 실증. 기획서의 달걀찜(경험치 배율)은 새 버프 타입이 필요해 이번 작업 범위에서 제외 — 설계 근거는 DECISIONS.md D009.
- 독립 리뷰 2라운드(자체 리뷰, code-review 스킬 high): 1라운드 비블로킹 1건(`playable` 배열을 복사 없이 그대로 참조 — 현재는 무해하나 잠재적 공유 변경 위험) → `[...playable]`로 수정 → 2라운드 PASS(발견 없음).
- 355cafe: feature/farm-ranch-cooking을 main에 병합(사용자 명시 요청, 2라운드 PASS 확인 후). npm test 53/53·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-11 20:37 KST).
- 7224cc3: 병합 완료 상태를 HANDOFF/PROGRESS에 기록, origin/main 푸시·원격 SHA 일치 확인.

- 길드 원재료 구매/환전(feature/guild-exchange): 이미 해금한 원재료(가공품 제외) 골드 구매, 상위 티어 원재료를 같은 스킬 최하위(Lv1) 재료로 환전(티어 차이당 1.5배 복리). 길드 등급 3단계(골드 후원)로 환전 가능 깊이 확대. 퀘스트 시스템은 범위 밖(길드 레벨업을 골드 후원으로 단순화). `buyCrop`을 `buyResource`로 일반화(모든 원재료에 적용, 스킬 레벨 검증을 엔진 단에 추가 — 기존엔 UI에서만 확인). 저장 v6로 길드 등급 포함, v1~v5 이전 지원. 설계 근거·순환 거래 방지 검증은 DECISIONS.md D010 참고.
- 독립 리뷰 2라운드(자체 리뷰, code-review 스킬 high): 1라운드 비블로킹 1건(골드가 정확히 비용과 같을 때의 경계 테스트 누락) → 경계 테스트 2건 추가 → 2라운드 PASS(발견 없음).
- cf02d55: feature/guild-exchange를 main에 병합(사용자 명시 요청, 2라운드 PASS 확인 후). npm test 65/65·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 확인(2026-09-11 21:02 KST).

- 마법부여·장신구(feature/enchanting-accessories): 농사 약초 4종·채광 마나석/금 광석·금 주괴·마법 스킬/부여석 4등급의 공급 루프, 공용 장신구 3슬롯, 스톤→구리→철→금 승급과 옵션 보존, 재질 상한 내 확률 재정규화 리롤, 전 스킬 속도/경험치/판매가 효과, 저장 v7 및 v1~v6 이전, 제작·승급·리롤 UI를 구현. 상세는 docs/implementation_status.md와 DECISIONS.md D011 참고.
- 독립 전체 diff 리뷰 PASS(자체 리뷰): feature `5481634`, target `60e0b52`, 발견 없음. `npm test` 75/75·`npm run build`·`git diff --check` PASS, 대상 브라우저 상호작용/시각 검증 PASS. 추적 CI 없음, GitHub Actions/check/status 0건, `main` 보호 비활성·필수 체크 off/empty·ruleset 0건을 확인. 증적은 `.git/development-workflow-review.md`.
- 71850e6: feature/enchanting-accessories를 main에 병합(사용자 명시 요청, 리뷰 PASS 및 병합 직전 원격/정책/충돌 게이트 확인 후). 통합 상태에서 npm test 75/75·npm run build PASS 재확인, origin/main 푸시·원격 SHA 일치 및 feature ancestry 확인(2026-09-11 22:49 KST).

## In progress

- 세이브 백업/복원(feature/save-backup-restore, Planned 항목 6의 일부만 분리): 설정 화면에서 현재 저장을 JSON 파일로 내려받고(`encodeSave`), 파일에서 복원할 수 있다. 복원은 확인 후 기존 `decodeSave`로만 검증하며 실패 시 기존 저장을 그대로 두고 오류만 표시한다. 저장 손상으로 자동 저장이 막힌 상태(`blocked`)도 유효한 백업 복원으로 풀린다. 엔진/저장 스키마 변경 없음(v7 그대로). 설계 근거는 DECISIONS.md D012 참고. 온보딩 시간 시뮬레이션·다중 탭 충돌 방지는 이번 작업 범위 밖(Planned 항목 6의 나머지, 각각 별도 원자 작업으로 이후 진행).
- 독립 리뷰 2라운드(자체 리뷰, code-review 스킬 high): 1라운드 8건(모두 CONFIRMED/PLAUSIBLE) → 복원이 `advance()`를 건너뛰어 오래된 백업 복원 시 조용히 밀린 생산이 반영되는 문제(확정), 백업 다운로드가 DOM에 붙지 않은 앵커를 클릭하고 URL을 동기적으로 해제하는 문제(확정) 등 6건 수정 → 2라운드 PASS(비블로킹 2건 남김: `blocked` 해제가 복원에만 결합된 점, `saveGame` 저장 실패 시에도 성공을 반환하는 점 — 둘 다 기존 폴백 동작으로 충분하다고 판단해 의도적으로 미수정, DECISIONS.md D012 참고). 증적은 `.git/development-workflow-review.md`.
- b59a1ff: 리뷰 1라운드 수정 커밋. `npm test` 77/77 PASS, `npm run build` PASS. 브라우저 재검증: 오래된 백업(활성 벌목 작업 포함) 복원 시 "백업에서 복원했습니다. 자리를 비운 동안 33회 작업했습니다." 표시 및 레벨업 정확히 반영, 잘못된 JSON 파일은 한글 오류 메시지로 거부, 버전 불일치 파일도 거부, 다운로드 버튼 콘솔 오류 없음, 확인 취소 시 무변화(2026-09-12). origin에 푸시·원격 SHA 일치 확인.
- 사용자 피드백 반영(같은 날): (1) 다운로드 시 뜨는 저장 대화상자는 브라우저 자체 동작이라 앱에서 끌 수 없음을 안내하고, 대안으로 "클립보드에 복사" 버튼을 다운로드와 나란히 추가. (2) 백업 파일을 텍스트 편집기로 값만 바꿔 그대로 복원하는 것을 막기 위해, `encodeSave`가 체크섬(FNV-1a, 키 정렬 후 해시)을 함께 저장하고 `decodeSave`는 체크섬이 있을 때만(v7 이전 저장·기존 테스트 픽스처는 없으므로 그대로 통과) 값을 검증해 하나라도 바뀌면 거부한다. 암호학적 보안이 아니라 "메모장으로 슬쩍 편집" 수준의 마찰이 목적임을 사용자에게 명시적으로 설명함 — 체크섬까지 재계산해 넣거나 필드째로 지우면 여전히 우회 가능(이 한계는 테스트로 고정). 설계 근거는 DECISIONS.md D013 참고. `npm test` 78/78 PASS, `npm run build` PASS, 브라우저로 정상 복원·변조 거부 재확인(클립보드 복사는 자동화 브라우저의 권한 제약으로 실패 메시지 경로만 확인, 정상 브라우저에서는 동작 예상).
- 클라우드 동기화는 사용자가 방향성으로 질문(향후 계정 기반 저장). 이번 세션에서는 구현하지 않음 — 백엔드가 전혀 없는 프로젝트라 별도의 큰 아키텍처 결정이 필요함을 답변만 하고 Planned에 후보로 기록(아래).
- 병합 대기: 코드 변경분에 대한 독립 리뷰(1라운드, PASS)까지 완료. 추가 변경(체크섬/클립보드)은 자체 검증(테스트+빌드+브라우저)으로 확인했으며 별도 8앵글 리뷰는 진행하지 않음(변경 범위가 작고 검증이 충분하다고 판단). `/merge` 등 사용자의 명시적 병합 요청이 있어야 main 통합 진행(AGENTS.md 명시 게이트).

## Planned

아래는 개발 재개 시 선택할 제안 순서. 작업 전 원격 최신성 및 기획을 확인하고, 수용 기준을 구체화한다.

1. ~~농사 최소 루프~~ — 완료, main 병합됨(510386f). 남은 확장: 다중 플롯, 비료 3종, 자동 파종/수확.
2. ~~목장 최소 루프~~ — 완료, main 병합됨(a95abbe). 남은 확장: 마릿수 상한, 비료류 보조 아이템.
3. ~~농사·목장 산출물 요리 연결~~ — 완료, main 병합됨(355cafe). 남은 확장: 경험치 배율형 음식(달걀찜).
4. ~~길드 안내·하위 재료 구매/교환~~ — 완료, main 병합됨(cf02d55). 남은 확장: 일일/마일스톤 퀘스트, 임의 티어 쌍 환전.
5. ~~마법부여·장신구~~ — 완료, main 병합됨(71850e6). 남은 확장: 광맥 발견, 장신구 다중 옵션/고유 옵션 풀, 밸런스 플레이테스트.
6. 성장 곡선·온보딩·편의 기능: 만렙까지 시간 시뮬레이션, ~~세이브 백업/복원~~(구현·리뷰 완료, main 병합 대기 — feature/save-backup-restore), 다중 탭 충돌 방지. 독립 작업으로 분리.
7. (미확정, 사용자 언급만 있음) 계정 기반 클라우드 저장 동기화: 이 프로젝트는 현재 백엔드가 전혀 없는 정적 프론트엔드(Vite/Solid, `localStorage`만 사용)다. 실제로 하려면 인증·저장소·호스팅을 포함한 백엔드 신설이 필요한 별도의 큰 아키텍처 결정이며, 이번 세션에서는 착수하지 않았다. 당장은 세이브 백업 파일을 사용자가 직접 OneDrive/Google Drive 등 동기화되는 폴더에 저장하는 것으로 기기 간 이전을 대체할 수 있다.

## Milestones

- 첫 생산 루프: 구현 완료, 전체 게임 완료 아님.
- 낚시·요리 루프: 구현 완료, 수치 임시.
- Development Workflow: main에 병합 완료(a4522c9).
- 농사 최소 루프: main에 병합 완료(510386f). 수치 임시.
- 목장 최소 루프: main에 병합 완료(a95abbe). 수치 임시.
- 농사·목장 요리 연결: main에 병합 완료(355cafe). 수치 임시.
- 길드 원재료 구매/환전: main에 병합 완료(cf02d55). 퀘스트 미구현.
- 마법부여·장신구: main에 병합 완료(71850e6). 비용/효과 수치 임시.
- 도감/업적: 미구현.
